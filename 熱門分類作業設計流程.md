# 熱門分類作業設計流程

## 1. 需求背景

- 熱門分類（L1/L2/L3）由使用者透過 UI 點擊，意圖已明確，無需進入 LLM 聊天推理。
- 過去熱門分類仍沿用 `_legacy_chat_flow()`，導致 `_filter_items_by_primary_l3` 等優化不當篩掉商品，也浪費 LLM token。
- 目標：熱門分類必須走「純搜尋」工作流，確保商品清單完整且回應更快。

## 2. 前端工作流

1. **L1 / L2點擊**
   - 仍走聊天流程（`sendChat()`），請求 `/api/chat` 以取得下一層分類建議。
2. **L3 點擊**
   - 立即切換到搜尋模式：
     - 組裝 payload `{ query, category_hierarchy:{L1,L2,L3}, from_hot_category:true, prefer_special_first:true, page_size:24 }`
     - 呼叫 `/api/search`，載入商品卡（`window.renderList()`）
     - 網格上方顯示 `#searchSummary`，聊天視圖不再插入商品清單。

## 3. 後端工作流

### 3.1 /api/chat 熱門分類短路（`backend/chat_router_goods_action.py`）

1. `ChatReq` 增加 `action`、`flags` 欄位。
2. `_extract_hot_category_click(req)` 解析熱門分類 action：
   - 支援 action type `hot_category_click` / flag `from_hot_category`。
   - 正規化 hierarchy（允許多種欄位名稱），確保 L1/L2/L3 都存在。
3. 命中時呼叫 `handle_hot_category_click()`：
   - 使用 `catalog_service.get_dataframe()` + `search_products_with_hierarchy()` 取得商品。
   - 若沒有資料，改從 `catalog_service.snapshot()` 過濾。
   - 透過 `_compose_structured_reply()` 產出商品卡回覆，metadata 包含 `hot_category:True`、`category_hierarchy` 等。
4. 直接回傳 `ChatResponse`，不再進入 LLM 或 fallback。

### 3.2 /api/search（`backend/app.py`）

1. `SearchReq` 已有 `from_hot_category` 欄位。
2. 套用 `_filter_by_hierarchy(records, hierarchy, from_hot_category)`：
   - `from_hot_category=True` 且 L1/L2/L3 皆有 → 走 ⚡⚡ 超快速路徑，只保留 CateName_L3 完全匹配的商品。
   - L3 only → ⚡ 快速路徑。
   - 其他情境 → 逐層驗證。
3. 回傳的 `items` 直接用於前端商品網格。

## 4. 資料流程摘要

```
前端 L3 click
  ↓（payload: from_hot_category=true, hierarchy）
/api/search                                 /api/chat
  ↑                                          ↑
  │  (有傳 query + category)                │  (有 action/flags)
  │  → _filter_by_hierarchy 超快速           │  → handle_hot_category_click()
  │  → 6~24 筆商品                          │  → 呼叫 search_products_with_hierarchy
  │  → renderList → 單純商品網格            │  → 回傳商品卡，不進 LLM
```

## 5. 後續檢查清單

- [ ] 前端 L3 payload 確保 `L1/L2/L3` 皆帶齊、`from_hot_category: true`。
- [ ] 部署後檢查 `/api/search` log 是否顯示「⚡⚡ 超快速路徑」。
- [ ] 確認聊天區沒有再插入舊式文字清單，`#searchSummary` 顯示商品總數。
- [ ] 特殊類型（空結果/錯誤）仍會顯示友善訊息並允許返回聊天模式。

