# sample development env for backend
ADMIN_TOKEN=testtoken123
LOG_LEVEL=INFO
DATA_PATH=/app/data/VIEW_GOODS_enhanced.csv

# during development you can allow admin endpoints without providing ADMIN_TOKEN
# set ALLOW_DEV_ADMIN=1 to bypass token checks (only for local/dev use)
ALLOW_DEV_ADMIN=1
