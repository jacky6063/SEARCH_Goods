# 環境變數統一設定指南

## 🎯 目的
確保本地開發環境與生產環境的行為一致，避免「本地測試 OK，生產環境卻不同」的困擾。

---

## 📂 檔案結構

```
SEARCH_Goods/
├── .env.template          # 環境變數範本（版本控制）
├── .env.dev              # 開發環境設定（不納入版本控制）
├── .env.test             # 測試環境設定（不納入版本控制）
└── backend/
    └── app.py            # 讀取環境變數
```

---

## 🚀 快速開始

### 步驟 1：建立本地環境設定檔

```bash
# 複製範本檔案
cp .env.template .env.dev

# 編輯開發環境設定
nano .env.dev
```

### 步驟 2：設定關鍵參數

**必須立即設定的參數：**

```bash
# ⚠️ 最重要！影響查詢結果數量
HOT_CATEGORY_PAGE_SIZE=6

# 一般查詢的預設大小
DEFAULT_PAGE_SIZE=30

# 管理員 Token（用於 CSV 上傳等管理功能）
ADMIN_TOKEN=your-secure-token-here
```

### 步驟 3：啟動服務並驗證

```bash
cd backend
source .venv/bin/activate  # 啟動虛擬環境

# 使用 .env.dev 設定啟動
export $(cat ../.env.dev | grep -v '^#' | xargs) && uvicorn app:app --reload
```

### 步驟 4：測試查詢結果

開啟瀏覽器測試熱門分類查詢：
- 點擊「籃球鞋」按鈕
- **預期結果：顯示 6 筆商品**（與生產環境一致）

---

## 🔧 配置說明

### 分頁參數對照表

| 參數 | 本地開發建議值 | 生產環境建議值 | 影響範圍 |
|------|---------------|---------------|---------|
| `DEFAULT_PAGE_SIZE` | 30 | 6-12 | 一般搜尋、聊天推薦 |
| `HOT_CATEGORY_PAGE_SIZE` | **6** | **6** | 首頁熱門分類按鈕 |

**關鍵原則：** `HOT_CATEGORY_PAGE_SIZE` 必須在本地與生產環境保持一致！

### 為什麼分開設定？

1. **DEFAULT_PAGE_SIZE=30**（開發環境）
   - 便於查看更多測試資料
   - 不影響熱門分類功能
   - 適合功能驗證

2. **HOT_CATEGORY_PAGE_SIZE=6**（本地+生產）
   - 保證測試環境與生產環境一致
   - 避免「本地 24 筆，生產 6 筆」的困擾
   - 符合 UI 設計預期

---

## 🏗️ 在 app.py 中讀取環境變數

### 修改建議

在 `backend/app.py` 檔案開頭加入：

```python
import os
from dotenv import load_dotenv

# 載入環境變數（優先使用 .env.dev，其次 .env）
load_dotenv(dotenv_path=os.path.join(os.path.dirname(__file__), '..', '.env.dev'))
load_dotenv(dotenv_path=os.path.join(os.path.dirname(__file__), '..', '.env'))

# 讀取分頁設定
DEFAULT_PAGE_SIZE = int(os.getenv("DEFAULT_PAGE_SIZE", "30"))
HOT_CATEGORY_PAGE_SIZE = int(os.getenv("HOT_CATEGORY_PAGE_SIZE", "6"))
```

### 在搜尋端點使用

```python
@app.post("/api/search")
async def search(req: SearchRequest):
    # 決定分頁大小
    if req.from_hot_category:
        # 來自熱門分類按鈕，使用專用設定
        page_size = min(req.page_size or HOT_CATEGORY_PAGE_SIZE, 50)
    else:
        # 一般搜尋，使用預設設定
        page_size = min(req.page_size or DEFAULT_PAGE_SIZE, 50)
    
    # ... 其他搜尋邏輯
```

---

## 🌐 生產環境設定（Render）

### 在 Render Dashboard 設定環境變數

1. 登入 https://dashboard.render.com
2. 選擇你的 Backend Service
3. 點擊 **Environment** 標籤
4. 新增以下環境變數：

```
HOT_CATEGORY_PAGE_SIZE = 6
DEFAULT_PAGE_SIZE = 12
ADMIN_TOKEN = [強密碼]
USE_LLM_EXPAND = False
USE_LLM_RERANK = False
```

### 觸發重新部署

設定完環境變數後，Render 會自動重新部署服務。

---

## 🧪 驗證環境一致性

### 測試清單

執行以下測試確保環境一致：

```bash
# 1. 檢查環境變數是否載入
cd backend
python3 -c "
import os
from dotenv import load_dotenv
load_dotenv('../.env.dev')
print('HOT_CATEGORY_PAGE_SIZE:', os.getenv('HOT_CATEGORY_PAGE_SIZE'))
print('DEFAULT_PAGE_SIZE:', os.getenv('DEFAULT_PAGE_SIZE'))
"

# 2. 執行診斷工具
python3 diagnostic_environment_check.py

# 3. 測試熱門分類查詢
curl -X POST http://localhost:8000/api/search \
  -H "Content-Type: application/json" \
  -d '{
    "query": "籃球鞋",
    "from_hot_category": true,
    "page_size": 24,
    "L1": "服飾配件",
    "L2": "鞋子",
    "L3": "籃球鞋"
  }'
```

**預期結果：** 無論 `page_size` 設為 24，實際回傳應為 6 筆（受 `HOT_CATEGORY_PAGE_SIZE` 限制）

---

## 📋 環境變數優先順序

載入順序（後者覆蓋前者）：

1. 系統環境變數（`export` 或 Render Environment）
2. `.env` 檔案
3. `.env.dev` 檔案
4. 程式碼中的預設值

**建議實務：**
- 本地開發：使用 `.env.dev`
- 生產環境：使用 Render Environment Variables
- 版本控制：只追蹤 `.env.template`

---

## 🔒 安全性注意事項

### .gitignore 設定

確保以下檔案不納入版本控制：

```gitignore
# 環境變數檔案（包含敏感資訊）
.env
.env.dev
.env.test
.env.local
.env.production

# 保留範本檔案（無敏感資訊）
!.env.template
```

### ADMIN_TOKEN 強度建議

```bash
# 生成安全的 Token（至少 32 字元）
python3 -c "import secrets; print(secrets.token_urlsafe(32))"
```

---

## 🐛 常見問題排查

### 問題 1：環境變數沒有生效

**檢查步驟：**
```bash
# 確認檔案存在
ls -la .env.dev

# 確認內容正確
cat .env.dev | grep HOT_CATEGORY_PAGE_SIZE

# 確認 python-dotenv 已安裝
pip list | grep python-dotenv
```

**解決方案：**
```bash
pip install python-dotenv
```

### 問題 2：本地與生產環境結果仍不一致

**診斷步驟：**
1. 執行 `diagnostic_environment_check.py` 比對 Hash 值
2. 在 Render Shell 執行 `echo $HOT_CATEGORY_PAGE_SIZE`
3. 檢查 `app.py` 是否正確讀取環境變數

**快速驗證：**
```python
# 在 app.py 加入暫時的日誌
import logging
logging.info(f"HOT_CATEGORY_PAGE_SIZE={HOT_CATEGORY_PAGE_SIZE}")
```

### 問題 3：環境變數顯示 NOT_SET

**原因：** 診斷工具直接讀取 `os.environ`，不會載入 `.env.dev`

**解決方案：**
```bash
# 方法 1：手動匯出環境變數
export $(cat .env.dev | grep -v '^#' | xargs)
python3 diagnostic_environment_check.py

# 方法 2：修改診斷工具支援 .env.dev（已在工具中實作）
```

---

## 📚 相關文件

- `.env.template` - 環境變數範本檔案
- `PRODUCTION_DIAGNOSTIC_GUIDE.md` - 生產環境診斷指南
- `diagnostic_environment_check.py` - 環境一致性診斷工具

---

## 🎯 最佳實務總結

1. **統一配置**：本地與生產環境的 `HOT_CATEGORY_PAGE_SIZE` 必須一致
2. **版本控制**：只追蹤 `.env.template`，敏感設定使用 `.env.dev`
3. **定期驗證**：每次部署後執行診斷工具確認環境一致性
4. **文件更新**：環境變數異動時同步更新 `.env.template`

---

**建立時間**：2025-11-25  
**維護者**：開發團隊  
**相關問題**：「本地測試 OK，生產環境結果不同」
