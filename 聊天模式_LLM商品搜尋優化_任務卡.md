# 聊天模式 LLM 商品搜尋優化任務卡（分類層級判斷修正）

## 不在範圍
- 不重構專案架構、路由設計、目錄結構。
- 不改 LLM 模型與金鑰設置；不硬編 API Key。

## 禁止事項（務必遵守）
- **禁止重構整個專案**：不得主動改動架構、路由、目錄，除非任務卡明確要求。
- **禁止修改既有商業邏輯**：不得更動商品搜尋/訂單/會員/價格/庫存與 DB schema。
- **禁止硬編 API Key / 機敏資訊**：所有金鑰一律用環境變數，提示詞/程式碼不可寫死。
- **一次只執行一張任務卡**：不得偷跑下一張卡的內容。
- **不確定時少改**：無法判斷時，優先加註解/提議，不要大改。
- **不執行的命令**：不得使用 `git reset --hard`、`git checkout -- .`、`rm -rf /` 等破壞性指令；不得清除非本任務相關檔案。

## 背景問題

### 核心問題
查詢「有伴手禮盒嗎」時，LLM 意圖解析將 L2 分類誤判為 L1：
- ❌ 錯誤輸出: `{L1: "伴手好禮", L2: "", L3: ""}`
- ✅ 正確輸出: `{L1: "常溫食品", L2: "伴手好禮", L3: "伴手好禮"}`

### 實際資料結構
```
L1: 常溫食品 (514 個商品)
  └─ L2: 伴手好禮 (2 個商品)
      └─ L3: 伴手好禮 (2 個商品)
          ├─ 經典油品禮盒-冷壓初榨橄欖油2入
          └─ 經典油品禮盒-冷壓橄欖油+葵花籽油
```

### 影響範圍
1. 性能劣化: 分類索引落空 → 降級全表搜尋，性能從 ~5ms → ~200ms (40 倍變慢)
2. 結果不精準: 返回不相關商品（如 XO 醬）而非禮盒商品
3. 系統性風險: 所有 L2/L3 分類都可能被誤判為 L1

### 根本原因
LLM 提示詞缺乏明確的層級標記，導致無法區分「伴手好禮」是 L1 還是 L2。

## 目標
- 讓 LLM 正確識別分類層級 (L1/L2/L3)，避免層級錯位。
- 維持索引路徑性能 (~5ms)，避免降級到全表 (~200ms)。
- 查詢「伴手禮盒」時返回 2 筆禮盒，無不相關商品混入。

## 工作項目
1) 提示詞補強 (_build_category_hierarchy_prompt)
   - 在分類提示中加入明確的層級標記 [L1]/[L2]/[L3]，對「伴手禮盒」同義明確標註：L1=常溫食品，L2/L3=伴手好禮。
   - 提示中說清楚：若查詢含「伴手禮盒/禮盒/伴手」，請填入對應層級；缺層級時至少填 L1/L2。
2) 輸出完整性與降級策略
   - 意圖解析後若信心低或 L2/L3 為空，觸發澄清或使用映射（伴手禮盒→伴手好禮 L2/L3）。
   - 分類優先順序：有 category_hierarchy 用索引；無時再用 category_filter 寬鬆比對，避免降級全表。
3) 提示詞規則補充
   - 在 system prompt 中加入層級規則：不可將 L2 誤判為 L1；伴手禮盒/禮盒/伴手 → 映射到 {L1:"常溫食品", L2/L3:"伴手好禮"}；有 L3 必須有 L2/L1。
4) 驗證與回歸
   - 案例：「有伴手禮盒嗎」→ 2 禮盒；「有禮盒嗎」無倒退。
   - 檢查 structured_filters、category_hierarchy、suggestion_ids，確認無 XO 醬等非禮盒商品，性能維持索引路徑。
   - 提交前需跑完整套 pytest（非僅局部），確保分類導覽/概覽等路徑無退化。
5) 補充優化項目（納入實作）
   - 分類映射表：維護「用戶詞 → 標準層級」對照（例：伴手禮盒/伴手禮 → L1=常溫食品、L2/L3=伴手好禮），低信心或缺層級時優先套用。
   - 分類優先順序：有 category_hierarchy 時先用索引；無時再用 category_filter 寬鬆比對，避免模糊詞導致全表降級。
   - 必須詞寬嚴分離：核心必須詞（禮盒/伴手）要求命中名稱或分類；次要詞可降權，避免過嚴必須詞排除正確禮盒。
   - 觀測與回歸：在 `/api/chat` 回應檢查 structured_filters、category_hierarchy、suggestion_ids，確保「有伴手禮盒嗎」不降級，並與「有禮盒嗎」對照無倒退。
   - 提示動態性：若分類有新增/調整，提示詞需同步或改用動態生成，避免再次過時。

## 交付
- 更新後的提示詞與相關程式碼（遵守範圍/禁止事項），附回歸結果（category_hierarchy、structured_products 驗證）。
