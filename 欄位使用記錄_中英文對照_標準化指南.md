# 📋 商品資料欄位 中英文對照表 & 標準化指南

**文檔版本**：1.0  
**最後更新**：2025年11月8日  
**維護範圍**：整個 SEARCH_Goods 系統  

---

## 🎯 概述

整個程式開發**已建立統一標準**，通過以下機制確保欄位命名的一致性：

| 機制 | 文件 | 說明 |
|------|------|------|
| **欄位定義** | `column_definitions.json` | 規範標準欄位名和別名 |
| **欄位訪問器** | `field_utils.py` (FieldAccessor) | 統一的欄位取值方法 |
| **搜尋服務** | `goods_search_service.py` | 分類和數值欄位的映射 |
| **驗證工具** | `validate_field_standardization.py` | 檢測非標準欄位使用 |

---

## 📊 完整欄位對照表

### 核心欄位

| 標準名稱（英文） | 中文名稱 | CSV 欄位名 | 別名 | 資料類型 | 長度 |
|---|---|---|---|---|---|
| **GoodIden** | 商品編號 | 商品編號 | id, barcode, 條碼, sku | String | 15-20 |
| **Name** | 商品名稱 | 商品名稱 | name, title, 商品名 | String | 50-200 |
| **CateName_L1** | 大分類名稱 | 大分類名稱 | category_l1 | String | 20-50 |
| **CateName_L2** | 中分類名稱 | 中分類名稱 | category_l2 | String | 20-50 |
| **CateName_L3** | 小分類名稱 | 小分類名稱 | category_l3 | String | 20-50 |
| **Price** | 售價 | 售價 | price, 價格 | Integer/Float | 1-8 |
| **SpecialOffer** | 特價 | 特價 | special_price, 優惠價 | Integer/Float | 1-8 |
| **Size** | 規格 | 規格 | size, specification | String | 10-100 |
| **BRAND_Name** | 品牌 | 品牌 | brand, Brand | String | 20-100 |
| **DESCRIPTION** | 商品描述 | 描述 | description, desc | Text | 100-2000 |
| **Goods_Link1** | 購物網址 | 購物網址 | shop_url, link | URL | 50-500 |
| **Goodspic_Link1** | 商品圖片網址 | 商品圖片網址 | image_url, pic_url | URL | 50-500 |
| **REMARK** | 備註 | 備註 | remark, note | Text | 0-1000 |
| **stock** | 庫存量 | 庫存量 | inventory | Integer | 1-8 |
| **youtube** | Youtube 影片介紹 | Youtube 影片介紹 | video_url | URL | 50-500 |

---

## 🔗 欄位別名完整列表

### 根據 column_definitions.json

```python
# 標準欄位名 → 所有接受的別名（包括中英文）

"GoodIden": ["商品編號", "id", "goodiden", "barcode", "條碼", "sku"]
"Name": ["商品名稱", "name", "title", "商品名"]
"CateName_L1": ["大分類名稱", "catename_l1", "category_l1"]
"CateName_L2": ["中分類名稱", "catename_l2", "category_l2"]
"CateName_L3": ["小分類名稱", "catename_l3", "category_l3"]
"Size": ["規格", "size", "specification"]
"Price": ["售價", "price", "價格"]
"SpecialOffer": ["特價", "specialoffer", "special_price"]
"BRAND_Name": ["品牌", "brand", "Brand"]
"DESCRIPTION": ["描述", "description", "Description", "desc"]
"Goods_Link1": ["購物網址", "shop_url", "link"]
"Goodspic_Link1": ["商品圖片網址", "商品圖片網址1", "image_url", "pic_url"]
"REMARK": ["備註", "remark", "note"]
"stock": ["庫存量", "inventory"]
"youtube": ["Youtube 影片介紹", "video_url"]
```

### 根據 FieldAccessor (field_utils.py)

```python
FIELD_MAPPINGS = {
    "product_id": ["GoodIden", "商品編號", "id", "goodiden", "barcode", "條碼", "sku"],
    "name": ["Name", "商品名稱", "name", "title", "商品名"],
    "category": ["CateName", "CateName_L3", "CateName_L2", "CateName_L1", 
                 "分類名稱", "小分類名稱", "中分類名稱", "大分類名稱", "category", "catename"],
    "brand": ["BRAND_Name", "品牌", "brand", "Brand"],
    "description": ["DESCRIPTION", "描述", "description", "Description", "desc"],
    "price": ["Price", "售價", "price", "價格"],
    "special_price": ["SpecialOffer", "特價", "specialoffer", "special_price"],
    "size": ["Size", "規格", "size", "specification"],
    "stock": ["庫存量", "stock", "inventory"],
    "image_url": ["Goodspic_Link1", "商品圖片網址1", "image_url", "pic_url"],
    "shop_url": ["Goods_Link1", "購物網址", "shop_url", "link"],
    "video_url": ["Youtube 影片介紹", "video_url", "youtube"],
    "remark": ["REMARK", "備註", "remark", "note"]
}
```

### 根據 goods_search_service.py

```python
# 分類欄位
"L1": ["大分類名稱", "CateName_L1", "L1", "category_l1"]
"L2": ["中分類名稱", "CateName_L2", "L2", "category_l2"]
"L3": ["小分類名稱", "CateName_L3", "L3", "category_l3"]

# 數值欄位
"Price": ["價格", "價錢", "價位", "price", "售價"]
"SpecialOffer": ["特價", "優惠價", "促銷價", "折扣價", "specialoffer", "special_offer"]
```

---

## ✅ 整個系統的統一標準實施情況

### 1️⃣ 數據輸入層（CSV 讀取）

**文件**：`backend/app.py` (行 344-410)  
**機制**：FieldAccessor 統一訪問

```python
# ❌ 不推薦：直接存取欄位
row.get("商品名稱")  # 欄位名可能變動
row.get("Name")     # 只能找到一種情況

# ✅ 推薦：使用 FieldAccessor
FieldAccessor.get_name(row)        # 自動查找所有別名
FieldAccessor.get_price(row)       # 自動轉換和容錯
FieldAccessor.get_category_l3(row) # 明確指定層級
```

### 2️⃣ 搜尋層（查詢處理）

**文件**：`backend/goods_search_service.py` (行 40-120)  
**機制**：category mapping 和 field aliases

```python
# 分類過濾
get_all_column_variants("L3")  # 返回所有 L3 別名
get_primary_column("L3")       # 返回主要欄位名

# 數值比較
NUMERIC_FIELD_ALIASES["Price"]         # 所有價格欄位別名
NUMERIC_FIELD_ALIASES["SpecialOffer"]  # 所有特價欄位別名
```

### 3️⃣ 回應層（數據返回）

**文件**：`backend/app.py` (行 954-980)  
**機制**：format_for_chat 統一格式化

```python
# 統一的回應格式（無論輸入欄位如何）
{
    "商品編號": "...",
    "商品名稱": "...",
    "商品描述": "...",
    "商品價格": "...",
    "商品特價": "...",
    "商品購物網址": "...",
    "商品圖片網址": "..."
}
```

---

## 🐛 已識別的不一致之處

### 問題 1：直接欄位存取混雜

**位置**：`backend/app.py` 行 258, 354-369  
**問題**：
```python
# 問題代碼
item.get("SpecialOffer")    # 應用 FieldAccessor
row.get("商品名稱")          # 混用中英文欄位名
```

**改進方案**：統一使用 FieldAccessor
```python
# 改進後
FieldAccessor.get_special_price(item)
FieldAccessor.get_name(row)
```

---

**狀態**：✅ **系統已有統一標準架構**

整個程式開發遵循 `column_definitions.json` 定義的標準，通過：
- ✅ `FieldAccessor` 類統一欄位訪問
- ✅ `COLUMN_NAME_MAP` 動態對應
- ✅ 多層別名支援（容錯機制）
- ⚠️ 部分代碼仍有直接欄位存取（可優化）

---

## 📌 規範使用指南

### ✅ 推薦做法

```python
# 1. 讀取商品資料時使用 FieldAccessor
from field_utils import FieldAccessor

item = df.to_dict()  # CSV 行資料
name = FieldAccessor.get_name(item)
price = FieldAccessor.get_price(item)
category = FieldAccessor.get_category_l3(item)

# 2. 篩選分類時使用 get_all_column_variants
from goods_search_service import get_all_column_variants

l3_columns = get_all_column_variants("L3")
# 返回 ["小分類名稱", "CateName_L3", "L3", "category_l3"]

# 3. 回應格式化使用 format_for_chat
from goods_search_service import format_for_chat

items = format_for_chat(records, slim_mode=False)
# 自動使用中文欄位名
```

### ❌ 避免做法

```python
# 1. 直接硬編碼欄位名
row.get("商品名稱")     # ❌ 如果 CSV 欄位改名就崩潰
row.get("Price")        # ❌ 忽視了中文別名

# 2. 混用欄位名稱
name = row.get("Name")
name = row.get("商品名稱")  # ❌ 不一致

# 3. 假設欄位存在
price = int(row["Price"])  # ❌ 可能拋異常

# 4. 忽視數值轉換
price = row.get("Price")   # ❌ 可能是字符串、None 或包含符號
```

---

## 🔄 CSV 欄位變更影響分析

### 場景：如果 CSV 的欄位名改為 "商品編號 (ID)"

#### ❌ 沒有使用標準的代碼（會崩潰）
```python
id = row.get("GoodIden")  # ❌ 無法找到
id = row.get("商品編號")   # ❌ 無法找到（名稱改了）
```

#### ✅ 使用標準機制的代碼（自動適配）
```python
id = FieldAccessor.get_product_id(row)  # ✅ 仍能工作

# 或者只需更新 column_definitions.json
"GoodIden": ["商品編號 (ID)", "商品編號", "id", ...]
```

---

## 📚 參考文件

| 文件 | 位置 | 用途 |
|------|------|------|
| column_definitions.json | `backend/` | 欄位定義主配置 |
| FieldAccessor 類 | `backend/field_utils.py` | 統一欄位訪問 |
| COLUMN_NAME_MAP | `backend/goods_search_service.py` | 動態欄位對應 |
| validate_field_standardization.py | `backend/` | 欄位使用驗證 |
| constants.py | `backend/` | 全局欄位常數 |

---

## 🎓 最佳實踐

### DO（推薦）

```python
# ✅ 1. 優先使用 FieldAccessor
name = FieldAccessor.get_name(item)

# ✅ 2. 使用欄位常數而非字符串
from constants import get_primary_column
l3_col = get_primary_column("L3")

# ✅ 3. 新欄位先加入 column_definitions.json
# ✅ 4. 使用容錯/容納機制
price = FieldAccessor.get_price(item) or 0

# ✅ 5. 測試多個別名
```

### DON'T（避免）

```python
# ❌ 1. 硬編碼欄位名
row["商品名稱"]

# ❌ 2. 假設欄位存在
int(row["Price"])

# ❌ 3. 混用中英文
if "Name" in row and "商品名稱" not in row

# ❌ 4. 忽視別名
df[df["Price"] == 100]  # 如果有 "售價" 欄位會漏掉

# ❌ 5. 新欄位未加入配置直接使用
row.get("新欄位名")
```

---

## 🚀 改進建議

### 短期（立即實施）

1. **統一所有直接欄位存取**
   ```python
   # app.py 行 258: item.get("SpecialOffer") → FieldAccessor.get_special_price()
   ```

2. **增加類型檢查**
   ```python
   # 確保 FieldAccessor 返回值類型一致
   ```

### 中期（1-2 週）

3. **增強驗證工具**
   ```python
   # validate_field_standardization.py 擴展為自動修復
   ```

4. **建立 CI/CD 檢查**
   ```bash
   # 提交前自動掃描非標準欄位使用
   ```

### 長期（未來版本）

5. **遷移到 ORM 或 schema 驗證**
   ```python
   # 使用 Pydantic 或 SQLAlchemy 完全規範化
   ```

---

## 📞 常見問題

### Q1: 新增一個欄位需要修改什麼？

**A**：修改 3 個地方：

```
1. CSV 新增欄位列
2. column_definitions.json 新增對應
3. field_utils.py 新增 getter 方法（可選）
```

### Q2: 為什麼有的代碼直接用欄位名？

**A**：系統在演進，部分舊代碼還沒完全遷移到 FieldAccessor。標準化進度：

```
✅ 完成：goods_search_service.py、format_for_chat()
⚠️  進行中：app.py 搜尋邏輯
❌ 待完成：admin 端點、chat 路由
```

### Q3: FieldAccessor 的容錯機制如何工作？

**A**：按優先順序查找：

```python
# 例：get_name() 的尋找順序
1. 檢查 "Name"
2. 檢查 "商品名稱"
3. 檢查 "name"
4. 檢查 "title"
5. 檢查 "商品名"
6. 都沒找到 → 返回 "未知商品"
```

---

**文檔版本控制**

| 版本 | 日期 | 改動 |
|------|------|------|
| 1.0 | 2025-11-08 | 初始版本，完整記錄現狀 |

