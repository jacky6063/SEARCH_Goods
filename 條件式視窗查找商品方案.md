# 條件式視窗查找商品方案

## 目標
- 降低反覆對話，優先用現有商品列表讓使用者挑選。
- 當結果過多時，再以「條件視窗」引導縮小範圍。
- 價格區間為可選條件，但若使用者一開始有輸入，需帶入視窗。

## 核心規則
1) 先查商品，後必要才詢問。
2) 結果數量是是否開視窗的主要門檻。
3) 價格區間不強迫輸入，但可在視窗內調整。

## 完整查找邏輯
1) 使用者輸入需求
   - 可能包含：品類、用途、品牌、價格區間、其他條件。
2) LLM 意圖與分類判讀
   - 解析成：分類候選（L2/L3）、價格區間（若有）、關鍵詞。
3) 立即進行商品查詢（不先跳視窗）
   - 使用判讀結果作為初始查詢條件。
4) 依結果數量分流
   - 若結果數量 <= 20：直接顯示商品清單，結束。
   - 若結果數量 > 20：開啟「查詢條件視窗」。
5) 查詢條件視窗內容
   - 分類多選：顯示 LLM 判讀的分類候選（可多選、可調整）。
   - 價格區間：
     - 若使用者原始輸入含價格，預設帶入。
     - 若無價格，欄位為空白可選填。
   - LLM 推薦的可選條件（供使用者勾選/調整）：
     - 例如：顏色、尺寸、材質、容量、規格、用途等。
6) 使用者按「確認」
   - 以視窗選擇的條件做最終查詢。
   - 顯示精準商品結果。

## 範例流程
- 使用者：我要買咖啡
- 系統初查結果：45 筆 (>20)
- 系統開啟視窗：
  - 分類：咖啡豆、濾掛咖啡、即溶咖啡（可多選）
  - 價格：空白（可選填）
  - 條件：烘焙度、風味、產地、容量
- 使用者選擇條件後 → 再查詢 → 顯示結果

## 設計章節
### 觸發條件
- 初次查詢結果數量 > 20 才開啟條件視窗。
- 結果數量 <= 20 不開啟視窗，直接顯示商品。

### 視窗欄位設計
- 分類多選：顯示 LLM 判讀出的分類候選，可勾選多個。
- 價格區間：可選填；若使用者原始輸入含價格，預設帶入。
- 其他條件：由 LLM 提供候選（顏色、尺寸、材質、規格等），使用勾選或下拉。

### 查詢流程設計
1) 使用者輸入 → LLM 判讀 → 先查商品。
2) 若結果過多 → 顯示視窗讓使用者精修條件。
3) 使用者確認 → 帶條件再查詢 → 顯示結果。

### 回傳欄位設計（後端提供）
- `meta.result_count`：查詢命中數量，用於視窗觸發判斷。
- `meta.category_candidates`：分類候選清單，用於視窗預設值。
- `meta.price_range`：解析出的價格區間，用於視窗預填。

### 使用者體驗要點
- 視窗僅在「結果太多」時出現，避免打斷流程。
- 價格區間不強制輸入，但可做為縮小範圍工具。
- 分類預設用聯集查詢，避免過度收斂。

### 前端 UI 元件草圖（文字版）
```
[標題] 請協助縮小範圍
[說明] 目前結果較多，請勾選條件以便精準挑選

分類（可多選）
[ ] 經典側/斜背包  [ ] 經典後背包  [x] 經典手提包
[ ] 羊皮手提包    [ ] 輕量A4大容量包 ...

價格區間（可選填）
最低價 [____]  最高價 [____]

其他條件（可多選）
顏色: [ ] 黑 [ ] 米白 [ ] 棕
材質: [ ] 皮革 [ ] 尼龍 [ ] 帆布
尺寸: [ ] 小 [ ] 中 [ ] 大

[取消]   [確認並查詢]
```

### UI 行為要點
- 視窗開啟時自動帶入 `meta.category_candidates` 與 `meta.price_range`（若有）。
- 使用者按取消則回到原結果，不做二次查詢。
- 使用者按確認才觸發二次查詢。

### 元件命名與事件設計（不改現有程式碼）
- 容器：`#filter-modal`（遮罩＋對話框）
- 分類區塊：`#filter-categories`（多選 checkbox 群組）
- 價格區間：`#filter-price-min`、`#filter-price-max`
- 條件區塊：`#filter-attributes`（顏色/材質/尺寸等）
- 操作按鈕：`#filter-cancel`、`#filter-confirm`
- 觸發事件：
  - `openFilterModal(meta)`：初始化視窗內容（分類/價格/條件）。
  - `closeFilterModal()`：關閉視窗，回到原結果。
  - `applyFilterAndSearch()`：收集條件並觸發二次查詢。

### L1 白名單產生機制（更新商品資料時）
- 目的：避免即時條件建議過度發散，限制 LLM 僅輸出 L1 對應的可用條件。
- 觸發時機：每次更新 `VIEW_GOODS_enhanced` 後執行一次。
- 生成方式：對每個 L1 抽樣商品描述，讓 LLM 產出可用條件白名單。
- 輸出格式（範例）：
  ```json
  {
    "時尚女性": ["顏色","材質","尺寸","功能","容量"],
    "常溫食品": ["口味","風味","產地","規格","成分"]
  }
  ```
- 使用方式：即時條件分析僅允許輸出白名單欄位，其他欄位一律忽略。

### 條件式查詢效能優化（參考熱門分類流程）
- 查詢路徑改為：先用 L3 分類索引縮小候選，再做描述條件與價格區間過濾。
- 效果：避免全表掃描，速度接近熱門分類查詢。
- 快取策略：針對相同條件組合啟用短期快取，減少重複計算。

### 技術實作細節（效能優化）
- 索引過濾：透過 L3 分類索引取得候選集合。
- 條件過濾：在候選集合內做描述欄關鍵詞匹配。
- 價格過濾：以價格欄位做區間判斷。
- 快取：以條件組合作為 key，短期快取結果。

### 驗收項目（效能優化）
- 條件式查詢不再全表掃描。
- L3 分類能快速縮小候選集合。
- 相同條件組合可命中快取並顯著加速。

### 對應程式碼位置
- 後端索引查詢：`backend/goods_search_service.py`（CategoryIndex）
- 條件式查詢 API：`backend/app.py`（`/api/search/filtered`）
- 前端確認查詢：`frontend/index.html`（`applyFilterAndSearch`、`doStructuredSearchWithPagination`）

## 優化任務卡
### 任務名稱
條件式視窗查找商品方案

### 背景
目前對話式查詢在結果過多時容易反覆詢問。需要在「結果過多」時引導使用者使用條件視窗一次完成篩選。

### 目標
- 保留「先查商品」流程，降低不必要的問答。
- 在結果 > 20 時開啟條件視窗縮小範圍。
- 價格區間可選填，若使用者已輸入要帶入。

### 功能需求
1) 查詢分流
   - 結果 <= 20：直接顯示商品。
   - 結果 > 20：開啟條件視窗。
2) 條件視窗欄位
   - 分類多選（由 LLM 判讀提供）。
   - 價格區間（可選填，若原始輸入有價格需帶入）。
   - 其他可選條件（由 LLM 產生候選，如顏色/材質/尺寸/規格）。
3) 確認後查詢
   - 以使用者選擇的條件重新查詢並呈現結果。

### 變動範圍說明
- 本次優化以「插入條件視窗流程」為主，延續原先查詢方式，不做大規模重構。
- 原有 `/api/chat` 與 `/api/search` 的查詢流程保留，僅新增必要的回傳欄位與前端分流邏輯。
- 視窗確認後仍走既有查詢端點，只是加上條件參數。

### 後端回傳欄位與用途
- `meta.result_count`：本次查詢命中總數，前端用來判斷是否開啟條件視窗（>20 開啟）。
- `meta.category_candidates`：LLM 判讀的分類候選清單，作為視窗分類多選的預設值。
- `meta.price_range`：使用者原始輸入解析出的價格區間，若有則帶入視窗的價格起訖欄位。

### 邊界情況
- 若商品資料缺少某些欄位，該條件不應造成過度過濾，需提示使用者欄位可能缺失。
- 若使用者原始輸入沒有價格，價格欄位維持空白。
- 分類多選預設以「聯集」方式查詢（避免過度收斂）。

### 驗收標準
- 超過 20 筆結果必定開啟條件視窗。
- 低於或等於 20 筆結果不開啟視窗。
- 原始輸入含價格時，視窗自動帶入價格區間。
- 視窗確認後能正常查詢並顯示結果。

### 風險與注意事項
- LLM 產生的條件需可被後端理解，避免過度自由文本。
- 建議限制條件數量（例如最多 5 個）以避免 UI 複雜。

### 建議優先順序
1) 結果數量分流 + 視窗開啟邏輯
2) 分類多選 + 價格帶入
3) LLM 產生條件 + 查詢串接
