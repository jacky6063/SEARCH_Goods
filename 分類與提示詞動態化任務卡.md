# 任務卡：分類與提示詞動態化（含上傳流程）

## 目標
- 在管理頁新增 `goods_categories.csv` 上傳功能，與 `VIEW_GOODS_enhanced.csv` 一樣可由管理員上傳。
- 上傳任一檔案後，分類快取與提示詞快取自動失效，下一次聊天使用最新資料生成提示詞（或立即重建）。
- 保留降級機制：提示詞動態生成失敗時回退靜態版並記錄告警。

## 範圍
- 管理後端/管理頁：新增 `goods_categories.csv` 上傳入口與 API。
- 後端：上傳成功後的流程掛勾（清分類快取、清/重建提示詞快取）。
- 不改商品搜尋/訂單/會員/價格/庫存邏輯；不改前台熱門分類流程。

## 不在範圍
- 不重構專案架構、路由設計、目錄結構。
- 不改 LLM 模型與金鑰設置；不硬編 API Key。

## 禁止事項（務必遵守）
- **禁止重構整個專案**：不得主動改動架構、路由、目錄，除非任務卡明確要求。
- **禁止修改既有商業邏輯**：不得更動商品搜尋/訂單/會員/價格/庫存與 DB schema。
- **禁止硬編 API Key / 機敏資訊**：所有金鑰一律用環境變數，提示詞/程式碼不可寫死。
- **一次只執行一張任務卡**：不得偷跑下一張卡的內容。
- **不確定時少改**：無法判斷時，優先加註解/提議，不要大改。
- **不執行的命令**：不得使用 `git reset --hard`、`git checkout -- .`、`rm -rf /` 等破壞性指令；不得清除非本任務相關檔案。

## 需求摘要
1) 管理頁/後端新增 `goods_categories.csv` 上傳接口，與商品檔分開清楚標示。  
2) 上傳任一 CSV 成功後，必須：  
   - 清分類快取（categories cache）。  
   - 清除或立即重建提示詞快取（動態提示詞生成失敗需回退靜態版並記錄）。  
3) 確保動態提示詞使用最新分類與商品資料，並保留降級提示詞以防重建失敗。  

## 補強要點（依「新業種動態調整程式優化方案」）
- 提示詞生成必須資料驅動：從最新 `goods_categories.csv` 建 L1/L2/L3 樹，為每個 L3 抽 1–3 筆代表商品（出自 `VIEW_GOODS_enhanced.csv`），不可再硬寫靜態分類段落。  
- 長度與覆蓋控制：分段（規則 + L1/L2/L3 摘要 + 代表商品），長度超標時按優先級裁剪，但每個啟用 L3 至少保留 1 條摘要/範例，避免新業種被截斷。  
- 按需拼接（類 RAG）：對用戶查詢，優先拼入命中的 L1/L2/L3 片段與少量商品例子，不一次塞全量，以控長度並保留 LLM 效果。  
- 快取與管理：提示詞快取需有 TTL（建議 5 分鐘）並可由管理端強制清除；上傳任一 CSV 後同步讓分類快取與提示詞快取失效，必要時立即重建。  
- 降級與告警：動態生成失敗時回退靜態提示詞並記錄告警；提示詞重建失敗/超時需可觀測。  
- 覆蓋檢查：應有自動檢查提示詞是否包含最新 L1/L2/L3（例：磐鈺類別），避免資料更新但提示詞缺漏。  
- 重建模式與回滾：提示詞重建需支援「立即重建」與「延遲重建」兩種模式，皆搭配快取 TTL 管控與 smoke test 覆蓋檢查；若重建失敗或超時，必須自動回退靜態版並發出警示，確保快取不被污染且可安全回滾到上一版流程。  

## 驗收標準
- 管理頁可上傳 `goods_categories.csv`，上傳後 API 以新的分類檔為準。  
- 上傳任一 CSV 後，下一次聊天的提示詞源自最新資料（或立即重建），分類快取已刷新。  
- 動態提示詞失敗時能回退靜態版並有告警/記錄；提示詞長度控制與 L3 覆蓋（每 L3 至少 1 條摘要/範例）可驗證。  
- 無改動商品搜尋/訂單/會員/價格/庫存邏輯，前台熱門分類流程不受影響。  

## 待確認
- 提示詞重建方式：上傳後立即重建或僅清快取、由下一次請求延遲重建。  
- 快取 TTL 與警示機制：提示詞重建失敗如何通知/記錄；覆蓋檢查如何執行（自動 smoke test 或日誌）。  

## 風險與回滾
- 風險：動態提示詞生成失敗或超時 → 回退靜態版並記錄。  
- 回滾：保留靜態提示詞與舊版流程，必要時只清快取、不重建。  
