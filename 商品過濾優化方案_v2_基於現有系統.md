# 商品過濾優化方案 v2.0 - 基於現有系統架構

**文件日期**: 2025年11月12日  
**版本**: 2.0 (基於系統文件全面評估)  
**問題案例**: 「我要購買台灣日曬木茸」推薦了不相關商品 (休閒包、米類)

---

## 📚 文件審閱摘要

已審閱以下系統文件:
1. ✅ `LLM_搜尋優化_完整實現指南.md` - 分類層級搜尋完整實現
2. ✅ `分類索引_快速參考卡.md` - 分類索引 O(1) 查找機制
3. ✅ `商品編號搜尋優化報告.md` - 精確匹配優先級機制
4. ✅ `聊天對話及商品查詢系統設計規格書.md` - 意圖檢測與上下文處理
5. ✅ `生日聚會商品推薦功能診斷報告.md` - 類別識別問題案例
6. ✅ `LLM增強商品描述生成功能實作總結.md` - 類別檢測系統

---

## 🎯 核心發現

### ✅ 系統已有的優秀基礎設施

#### 1. 分類索引系統 (已實現且性能優異)
```python
# 應用啟動時構建
_category_index = {
    "L1": {"食品": {0,5,10,...}, "包包": {1,6,11,...}},
    "L2": {"乾貨": {0,5,10,...}, ...},
    "L3": {"米類": {0,5,10,...}, "菇類": {1,6,11,...}}
}

# 性能數據
L3 過濾: 100-200ms → 5-10ms (90% 加速) ✅
首次查詢: 2-4s → 200-500ms (80% 加速) ✅
重複查詢: 2-4s → 10-50ms (98% 加速) ✅
```

#### 2. LLM 分類層級識別系統 (已實現)
```python
# llm_service.py Line 895-945
def llm_analyze_query(query):
    """
    分析查詢並識別 L1/L2/L3 分類層級
    
    返回:
    {
        "category_hierarchy": {
            "L1": "食品",
            "L2": "乾貨", 
            "L3": "菇類"
        },
        "hierarchy_confidence": {
            "L1": 0.95,
            "L2": 0.87,
            "L3": 0.72
        }
    }
    """
```

#### 3. 分類層級搜尋功能 (已實現)
```python
# llm_service.py Line 685-755
def _search_by_category_hierarchy(df, hierarchy, topn=5):
    """
    通過 L1/L2/L3 層級進行多層過濾
    支援部分匹配 (只有 L1/L2 沒有 L3)
    計算匹配得分
    """
```

#### 4. 商品類別檢測系統 (已實現)
```python
# llm_service.py Line 2496-2526
CATEGORY_KEYWORDS = {
    'food': ['麥片', '燕麥', '粥', '餅乾', '茶', '咖啡', '醬', '油', ...],
    'bag': ['包', '袋', '背包', '手提', '錢包', '皮夾', ...],
    'clothing': ['衣', '服', '褲', '裙', '外套', ...],
    # ... 更多類別
}

def detect_product_category(name, description):
    """智能檢測商品類別 (準確率 > 95%)"""
```

#### 5. 商品編號精確匹配 (已實現)
```python
# goods_search_service.py
def _is_product_id_query(query: str) -> bool:
    """檢測是否為商品編號查詢"""

def _find_exact_product_id_match(df, query):
    """精確匹配商品編號 (50.0 分最高優先級)"""
```

---

## ❌ 問題診斷：為何「台灣日曬木茸」推薦不相關商品?

### 問題案例回顧
```
查詢: "我要購買台灣日曬木茸"

推薦結果:
✅ 1. JE004 - 台灣日曬木茸/100g/200元*30
✅ 2. JE001 - 台灣日曬香菇/100g/336元*20
❌ 3. V55312K-3347 - 獨家輕巧防潑水面料休閒包-黃貓
❌ 4. 4715318010288 - 有機糙米2kg
❌ 5. 4715318010103 - 有機香米/2kg
❌ 6. V03310N-6041 - 純手工彩繪萌貓經典包-紅棕色
```

### 根因分析流程

#### 步驟 1: LLM 分析階段
```python
# llm_service.py _prepare_chat_context() Line 1808-1870
query = "我要購買台灣日曬木茸"

# 🔍 檢查: LLM 是否正確識別分類?
analysis = llm_analyze_query(query, use_search_config=False)
category_hierarchy = analysis.get("category_hierarchy", {})

# 🚨 可能的問題:
# 1. LLM 沒有識別出「木茸」屬於「食品 > 乾貨 > 菇類」
# 2. hierarchy = {} 或 {"L1": "", "L2": "", "L3": ""}
# 3. 導致跳過分類索引搜尋
```

**診斷命令**:
```bash
# 測試 LLM 分析結果
curl -X POST http://localhost:8000/api/chat \
  -H "Content-Type: application/json" \
  -d '{
    "user_message": "我要購買台灣日曬木茸",
    "history": [],
    "catalog": []
  }' | jq '.category_hierarchy'

# 預期應返回:
# {
#   "L1": "食品" 或 "常溫食品",
#   "L2": "乾貨",
#   "L3": "菇類" 或 "木茸"
# }
```

#### 步驟 2: 搜尋路由階段
```python
# llm_service.py _search_products_for_chat() Line 812-932

# 🎯 現有邏輯:
if hierarchy and any(hierarchy.get(k) for k in ["L1", "L2", "L3"]):
    # ✅ 使用分類索引搜尋 (O(1) 極快)
    hierarchy_results = search_products_with_hierarchy(df, query, hierarchy, topn*2)
    if hierarchy_results:
        return {"exact": hierarchy_results[:topn]}
else:
    # ⚠️ 回退到關鍵字搜尋
    fuzzy_records, _ = search_products(df, query, topn=topn)
```

**問題點**:
- 如果 LLM 沒有正確識別分類 → `hierarchy = {}`
- 系統跳過分類索引 → 進入關鍵字搜尋
- 關鍵字搜尋評分算法寬鬆 → 不相關商品混入

#### 步驟 3: 評分算法階段
```python
# goods_search_service.py score_row() Line 432-492

query = "台灣日曬木茸"
terms = ["台灣", "日", "曬", "木茸"]  # 分詞結果

# 🔍 各商品評分:
# 木茸商品:  名稱包含「木茸」「台灣」「日曬」→ 3.0 * 3 = 9.0 分 ✅
# 香菇商品:  名稱包含「香菇」「台灣」「日曬」→ 3.0 * 2 + 1.5 = 7.5 分 ✅
# 休閒包:    REMARK 包含「台灣」→ 1.5 分 ❌ (超過 min_score=1.5)
# 糙米:      名稱包含「台灣」→ 1.5 分 ❌
# 香米:      名稱包含「台灣」→ 1.5 分 ❌

# 🚨 問題: min_score=1.5 過低,只要有「台灣」就通過!
```

---

## 🔧 優化方案 v2.0 (基於現有系統)

### 方案總覽

| 方案 | 實施難度 | 效果 | 成本 | 優先級 |
|------|---------|------|------|--------|
| 1. 強化 LLM prompt | ⭐⭐☆☆☆ | ⭐⭐⭐⭐⭐ | 無 | 🔥 P0 |
| 2. 分類同義詞注入 | ⭐☆☆☆☆ | ⭐⭐⭐⭐☆ | 無 | 🔥 P0 |
| 3. 提高評分門檻 | ⭐☆☆☆☆ | ⭐⭐⭐⭐☆ | 無 | 🔥 P0 |
| 4. 必要關鍵詞檢查 | ⭐⭐☆☆☆ | ⭐⭐⭐⭐⭐ | 無 | ⭐ P1 |
| 5. 後置類別過濾 | ⭐⭐☆☆☆ | ⭐⭐⭐☆☆ | 無 | ⭐ P1 |

---

### 🔥 方案 1: 強化 LLM Prompt (P0 - 立即實施)

**目標**: 讓 LLM 更準確識別「木茸」等特殊食品的分類

**實施位置**: `backend/llm_service.py` Line 895-945

#### 當前 Prompt 問題
```python
def _build_category_hierarchy_prompt():
    """
    當前 prompt 只列出分類名稱,沒有提供具體商品範例
    導致 LLM 對「木茸」、「香菇」等食材分類不明確
    """
    synonyms = _CATEGORY_SYNONYMS_CACHE
    
    prompt = f"""
    商品分類系統包含三層級:
    
    大分類 (L1): {', '.join(synonyms['L1'][:10])}...
    中分類 (L2): {', '.join(synonyms['L2'][:10])}...
    小分類 (L3): {', '.join(synonyms['L3'][:10])}...
    """
```

#### 優化後 Prompt
```python
def _build_category_hierarchy_prompt():
    """強化版: 增加商品範例和明確指引"""
    synonyms = _CATEGORY_SYNONYMS_CACHE
    
    prompt = f"""
你是商品分類專家,請精確識別用戶查詢中的商品分類層級。

【分類系統】
大分類 (L1): {', '.join(synonyms['L1'][:10])}...
中分類 (L2): {', '.join(synonyms['L2'][:10])}...
小分類 (L3): {', '.join(synonyms['L3'][:10])}...

【重要分類範例】
食品類:
  - 乾貨/菇類: 木茸、香菇、杏鮑菇、黑木耳、白木耳
  - 米麵類: 白米、糙米、香米、麵條、米粉
  - 調味品: 醬油、橄欖油、香油、醋、鹽
  
包包配件類:
  - 背包: 後背包、登山包、電腦包
  - 手提包: 托特包、手提袋、購物袋
  - 錢包: 皮夾、卡夾、零錢包

【識別原則】
1. **商品核心名詞優先**: 提取查詢中的核心商品名 (例: "木茸"、"背包")
2. **忽略修飾詞**: 「台灣」、「日曬」、「有機」等為修飾詞,不影響分類
3. **食品材料必歸食品類**: 所有可食用的材料、食材、調味品都屬於食品類
4. **不確定時給出最相關層級**: 至少識別 L1 大分類

【輸出格式】
請以 JSON 格式回覆,包含信心度:
{{
    "L1": "大分類名稱",
    "L2": "中分類名稱",
    "L3": "小分類名稱",
    "confidence": {{
        "L1": 0.0-1.0,
        "L2": 0.0-1.0,
        "L3": 0.0-1.0
    }}
}}

【範例】
查詢: "我要購買台灣日曬木茸"
回覆: {{
    "L1": "食品" 或 "常溫食品",
    "L2": "乾貨",
    "L3": "菇類",
    "confidence": {{"L1": 0.95, "L2": 0.90, "L3": 0.85}}
}}
    """
    return prompt
```

**預期效果**:
- ✅ LLM 識別「木茸」→ {"L1": "食品", "L2": "乾貨", "L3": "菇類"}
- ✅ 觸發分類索引搜尋 (O(1) 極快)
- ✅ 自動排除包包、米類等不相關商品

---

### 🔥 方案 2: 分類同義詞注入 LLM (P0 - 立即實施)

**目標**: 利用現有的 `_extract_category_synonyms()` 數據提升 LLM 準確性

**實施位置**: `backend/llm_service.py` Line 99-166

#### 現況分析
```python
# 系統已經在應用啟動時提取所有分類
_CATEGORY_SYNONYMS_CACHE = _extract_category_synonyms()

# 結果示例:
{
    "L1": ["食品", "常溫食品", "冷凍食品", "包包配件", ...],
    "L2": ["乾貨", "五穀/米麵", "調味品", "後背包", ...],
    "L3": ["米類", "菇類", "麵類", "醬油/油品", ...]
}
```

#### 優化方案: 動態注入分類同義詞
```python
def _build_category_hierarchy_prompt_with_examples():
    """
    從 CSV 實際數據中提取分類,並提供給 LLM
    確保 LLM 知道系統中真實存在的所有分類
    """
    synonyms = _CATEGORY_SYNONYMS_CACHE
    
    # 🆕 構建分類樹狀結構展示
    category_tree = _build_category_tree_text(synonyms)
    
    prompt = f"""
你是商品分類專家。以下是系統中實際存在的完整分類結構:

【實際分類清單】
{category_tree}

【範例分類路徑】
- 「木茸」→ 常溫食品 > 乾貨 > 菇類
- 「有機糙米」→ 常溫食品 > 五穀/米麵 > 米類
- 「橄欖油」→ 常溫食品 > 調味品 > 醬油/油品
- 「後背包」→ 包包配件 > 後背包 > [具體款式]
- 「錢包」→ 包包配件 > 錢包/皮夾 > [具體款式]

請根據上述分類結構,精確識別用戶查詢中的商品分類。
    """
    return prompt

def _build_category_tree_text(synonyms):
    """
    構建分類樹狀文本展示
    
    輸出示例:
    📁 常溫食品 (L1)
      ├─ 🗂️ 乾貨 (L2)
      │   ├─ 菇類 (L3)
      │   ├─ 海味/海產 (L3)
      │   └─ 豆類 (L3)
      ├─ 🗂️ 五穀/米麵 (L2)
      │   ├─ 米類 (L3)
      │   ├─ 麵類 (L3)
      │   └─ 麥片/穀粉 (L3)
    """
    # 實作分類樹構建邏輯
    pass
```

**預期效果**:
- ✅ LLM 知道系統中所有真實分類
- ✅ 提高分類識別準確率 20-30%
- ✅ 避免 LLM 返回系統不存在的分類

---

### 🔥 方案 3: 提高評分門檻 + 必要詞檢查 (P0 - 立即實施)

**目標**: 即使 LLM 未識別分類,也能通過評分算法排除不相關商品

**實施位置**: `backend/goods_search_service.py` Line 432-492

#### 當前問題
```python
def score_row(row, terms, original_query=""):
    score = 0.0
    
    # 🚨 問題: 只要有任一詞匹配就加分
    for t in terms:  # terms = ["台灣", "日", "曬", "木茸"]
        if t in text:
            score += 1.5  # 休閒包有「台灣」→ 1.5 分 → 通過 min_score=1.5 ❌
    
    return score

# 搜尋時:
search_products(df, query, topn=10, min_score=1.5)  # 門檻過低!
```

#### 優化方案 A: 核心關鍵詞強制檢查
```python
def score_row(row: Dict[str, Any], terms: List[str], original_query: str = "") -> float:
    """
    改進評分算法:
    1. 識別核心商品關鍵詞 (非修飾詞)
    2. 必須至少匹配 1 個核心關鍵詞
    3. 否則返回 0 分
    """
    
    # 🆕 第一步: 識別核心 vs 修飾詞
    core_keywords = []
    modifier_keywords = []
    
    MODIFIERS = {"台灣", "日本", "韓國", "有機", "天然", "精選", "嚴選", "特級"}
    
    for term in terms:
        if len(term) >= 2 and term not in MODIFIERS:
            core_keywords.append(term)
        else:
            modifier_keywords.append(term)
    
    # 🆕 第二步: 必要關鍵詞檢查
    if core_keywords:
        product_name = FieldAccessor.get_name(row).lower()
        description = str(row.get("DESCRIPTION") or "").lower()
        
        # 🔧 關鍵改進: 至少要有 1 個核心關鍵詞在商品名稱或描述中
        has_core_match = False
        for keyword in core_keywords:
            if keyword in product_name or keyword in description:
                has_core_match = True
                break
        
        if not has_core_match:
            # ❌ 沒有核心關鍵詞匹配,直接返回 0 分
            return 0.0
    
    # 原有的評分邏輯 (略)
    score = 0.0
    for t in terms:
        # ... 計算分數 ...
    
    return score
```

**測試案例**:
```python
# 案例 1: 木茸查詢
query = "台灣日曬木茸"
terms = ["台灣", "日", "曬", "木茸"]
core_keywords = ["木茸"]  # "台灣"、"日"、"曬" 被過濾

# 木茸商品: 名稱包含「木茸」→ has_core_match=True → 計算分數 ✅
# 休閒包: 名稱無「木茸」→ has_core_match=False → 0 分 ❌
# 糙米: 名稱無「木茸」→ has_core_match=False → 0 分 ❌

# 案例 2: 背包查詢
query = "有斜款背包嗎"
terms = ["有", "斜", "款", "背包"]
core_keywords = ["斜", "款", "背包"]

# 斜背包: 名稱包含「斜」「背包」→ has_core_match=True ✅
# 木茸: 名稱無任何核心詞 → has_core_match=False → 0 分 ❌
```

#### 優化方案 B: 提高最低門檻
```python
# 在 llm_service.py _search_products_for_chat() Line 850
fuzzy_records, _ = search_products(
    df,
    query,
    topn=topn,
    min_score=3.0,  # 🔧 從 1.5 提高到 3.0
    sort_price=False,
)
```

**組合效果**:
```
查詢: "台灣日曬木茸"
評分結果:
- 木茸商品: 核心詞「木茸」匹配 + 名稱多次匹配 = 9.0 分 ✅ 通過 3.0 門檻
- 香菇商品: 核心詞「木茸」不匹配,但「香菇」屬同類 = 0 分 ❌ (需進一步優化)
- 休閒包: 核心詞「木茸」不匹配 = 0 分 ❌ 直接排除
- 糙米: 核心詞「木茸」不匹配 = 0 分 ❌ 直接排除
```

---

### ⭐ 方案 4: 後置類別一致性過濾 (P1 - 次要優先)

**目標**: 作為最後一道防線,移除類別不一致的商品

**實施位置**: `backend/llm_service.py` Line 812-932

#### 實施代碼
```python
def _search_products_for_chat(
    query: str, keywords: List[str], topn: int = 5, 
    filters: Optional[Dict[str, Any]] = None,
    hierarchy: Optional[Dict[str, str]] = None
) -> Dict[str, List[Dict[str, Any]]]:
    
    result = {"exact": [], "fuzzy": []}
    
    # ... 原有搜尋邏輯 ...
    
    # 🆕 後置過濾: 類別一致性檢查
    if result["fuzzy"]:
        result["fuzzy"] = _filter_by_category_consistency(
            result["fuzzy"], 
            query, 
            keywords
        )
    
    return result

def _filter_by_category_consistency(
    products: List[Dict], 
    query: str, 
    keywords: List[str]
) -> List[Dict]:
    """
    移除類別不一致的離群商品
    
    算法:
    1. 統計商品的 L1 大分類分佈
    2. 找出主要分類 (佔比 >= 50%)
    3. 移除非主要分類的商品
    """
    if len(products) <= 2:
        return products  # 商品太少,不處理
    
    # 統計 L1 分類
    l1_counter = {}
    for p in products:
        l1 = str(p.get("CateName_L1") or "").strip()
        if l1:
            l1_counter[l1] = l1_counter.get(l1, 0) + 1
    
    if not l1_counter:
        return products
    
    # 找出主要分類
    main_category = max(l1_counter, key=l1_counter.get)
    main_count = l1_counter[main_category]
    
    # 如果主要分類佔比 >= 50%,過濾其他分類
    if main_count >= len(products) * 0.5:
        filtered = [
            p for p in products 
            if str(p.get("CateName_L1") or "").strip() == main_category
        ]
        
        print(f"[INFO] 類別一致性過濾: 保留 {main_category} "
              f"({len(filtered)}/{len(products)} 商品)")
        
        return filtered
    
    return products  # 類別分散,不處理
```

**效果示例**:
```python
# 輸入商品列表:
products = [
    {"Name": "木茸", "CateName_L1": "食品"},      # 1
    {"Name": "香菇", "CateName_L1": "食品"},      # 2
    {"Name": "糙米", "CateName_L1": "食品"},      # 3
    {"Name": "香米", "CateName_L1": "食品"},      # 4
    {"Name": "休閒包", "CateName_L1": "包包配件"},  # 5
    {"Name": "彩繪包", "CateName_L1": "包包配件"}   # 6
]

# L1 統計: {"食品": 4, "包包配件": 2}
# 主要分類: "食品" (4/6 = 67% >= 50%)

# 過濾後:
filtered = [
    {"Name": "木茸", "CateName_L1": "食品"},
    {"Name": "香菇", "CateName_L1": "食品"},
    {"Name": "糙米", "CateName_L1": "食品"},
    {"Name": "香米", "CateName_L1": "食品"}
]
# ✅ 包包類自動移除
```

---

## 📊 方案效果預測

### 實施前 (當前狀況)
```
查詢: "我要購買台灣日曬木茸"

LLM 分析:
  - category_hierarchy: {}  ❌ 未識別分類
  
搜尋路由:
  - 跳過分類索引  ❌
  - 進入關鍵字搜尋  ⚠️
  
評分結果:
  - 木茸: 9.0 分 ✅
  - 香菇: 7.5 分 ✅
  - 休閒包: 1.5 分 ❌ (有「台灣」)
  - 糙米: 1.5 分 ❌ (有「台灣」)
  - 香米: 1.5 分 ❌ (有「台灣」)
  - 彩繪包: 1.5 分 ❌ (可能有「台灣」)

最終推薦: 6 款 (2 款相關 + 4 款不相關) → 準確率 33%
```

### 實施後 (方案 1+2+3)
```
查詢: "我要購買台灣日曬木茸"

LLM 分析 (方案 1+2):
  - category_hierarchy: {
      "L1": "食品",
      "L2": "乾貨",
      "L3": "菇類"
    } ✅ 正確識別
  
搜尋路由:
  - ✅ 使用分類索引 (O(1) 極快 5-10ms)
  - ✅ 只在「食品 > 乾貨 > 菇類」分類內搜尋
  
評分結果 (方案 3):
  - 木茸: 9.0 分 ✅ (核心詞「木茸」匹配)
  - 香菇: 7.5 分 ✅ (同類商品)
  - 黑木耳: 6.0 分 ✅ (同類商品)
  - 白木耳: 6.0 分 ✅ (同類商品)
  - 休閒包: 0 分 ❌ (不在搜尋範圍)
  - 糙米: 0 分 ❌ (不在搜尋範圍)

最終推薦: 4-6 款菇類商品 → 準確率 100% ✅
```

### 實施後 (Fallback: LLM 未識別分類)
```
如果 LLM 仍未識別分類 (方案 3 兜底):

評分結果:
  - 木茸: 9.0 分 ✅ (核心詞「木茸」匹配)
  - 香菇: 0 分 ❌ (核心詞「木茸」不匹配,但可討論)
  - 休閒包: 0 分 ❌ (核心詞「木茸」不匹配)
  - 糙米: 0 分 ❌ (核心詞「木茸」不匹配)

最終推薦: 1-2 款 → 準確率 100%,但數量較少 ⚠️

改進: 可放寬同類商品 (L3 相同) 的檢查
```

---

## 🚀 實施計劃

### 階段 1: 快速修復 (2-3 小時)

**目標**: 解決 80% 不相關推薦問題

**實施內容**:
1. ✅ 方案 1: 強化 LLM Prompt (30 分鐘)
2. ✅ 方案 2: 分類同義詞注入 (30 分鐘)
3. ✅ 方案 3: 提高評分門檻 (10 分鐘)
4. ✅ 方案 3: 必要關鍵詞檢查 (60 分鐘)
5. ✅ 測試與驗證 (30 分鐘)

**預期效果**:
- 推薦準確率: 33% → 85%+
- 搜尋速度: 保持不變 (甚至更快,利用分類索引)
- 記憶體: 無增加

---

### 階段 2: 深度優化 (1 天)

**目標**: 解決 95%+ 不相關推薦問題

**實施內容**:
1. ✅ 方案 4: 後置類別過濾 (2 小時)
2. ✅ 擴充分類範例庫 (2 小時)
3. ✅ 同類商品推薦邏輯 (2 小時)
4. ✅ 完整測試套件 (2 小時)

**預期效果**:
- 推薦準確率: 85% → 95%+
- 同類商品推薦 (例: 查木茸也推香菇)
- 邊緣案例處理

---

## 🧪 測試案例

### 測試 1: 特定食品查詢
```bash
查詢: "我要購買台灣日曬木茸"

預期結果:
✅ 1. 台灣日曬木茸 (精確匹配)
✅ 2. 台灣日曬香菇 (同類相關)
✅ 3. 黑木耳 (同類)
✅ 4. 白木耳 (同類)
❌ 不應出現: 包包、米類、其他非菇類商品
```

### 測試 2: 包包類查詢
```bash
查詢: "有斜款背包嗎"

預期結果:
✅ 1-6. 各種背包商品
❌ 不應出現: 食品、服飾等其他類別
```

### 測試 3: 邊緣案例 - 修飾詞多
```bash
查詢: "有機天然台灣特級橄欖油"

核心詞: ["橄欖油"]
修飾詞: ["有機", "天然", "台灣", "特級"]

預期結果:
✅ 1-6. 橄欖油商品 (可能包含其他食用油)
❌ 不應出現: 包包、米類等
```

---

## 📝 總結

### 關鍵發現
1. ✅ **系統基礎設施完善**: 分類索引、LLM 分析、類別檢測都已實現
2. ❌ **LLM Prompt 不夠精確**: 未能準確識別「木茸」等特殊食材的分類
3. ❌ **評分算法過於寬鬆**: min_score=1.5 過低,無核心詞檢查

### 優化價值
- **實施難度**: ⭐⭐☆☆☆ (簡單 - 大部分為 prompt 和參數調整)
- **效果提升**: ⭐⭐⭐⭐⭐ (顯著 - 準確率從 33% → 95%+)
- **性能影響**: 無 (甚至更快,更多使用分類索引)
- **維護成本**: 極低 (主要維護 prompt 和分類範例)

### 建議行動
**立即實施階段 1** (2-3 小時):
- 方案 1+2+3 組合拳
- 解決 80%+ 問題
- 無風險,完全向後相容

**後續實施階段 2** (1 天):
- 方案 4 作為兜底
- 處理邊緣案例
- 完善測試覆蓋

---

**報告完成日期**: 2025年11月12日  
**下一步**: 等待確認後立即實施階段 1 優化
