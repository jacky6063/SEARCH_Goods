# 熱門分類商品查詢獨立設計

## 背景

- 熱門分類（L1/L2/L3）按鈕點擊時，使用者的需求已明確指定分類，不需要透過 LLM 推理。
- 目前熱門分類仍走 LLM 聊天流程，最近的 L3 優化（例如 `_filter_items_by_primary_l3`）會影響這些查詢，導致僅留下單一商品。
- 為確保熱門分類行為穩定並降低 LLM 耗用，需將熱門分類點擊流程獨立出來。

## 目標

1. 使用者點擊熱門分類時，系統直接依 L1/L2/L3 查詢並呈現完整商品清單。
2. 該流程不呼叫 `llm_service.chat_reply()`；自然語 LLM 查詢仍走現有 pipeline。

## 作業項目

1. **辨識熱門分類請求**  
   - 在 `_legacy_chat_flow()` 解析前端傳來的 action/flag。  
   - 若為熱門分類點擊，導向新的 handler。

2. **實作專用 Handler（建議命名 `handle_hot_category_click`）**  
   - 取得 L1/L2/L3 參數。  
   - 使用 `catalog_service` 或 `goods_search_service` 依層級撈出商品。  
   - 呼叫 `_compose_structured_reply()` 產生回覆與結構化資料。  
   - 回傳 `ChatResponse`（保持既有卡片格式）。

3. **LLM 流程維持現狀**  
   - `_filter_items_by_primary_l3()` 只作用於 LLM 推薦結果。  
   - 熱門分類流程與 LLM 聊天互不影響。

4. **測試**  
   - 熱門 L3 點擊應顯示完整列表，不受 LLM 優化影響。  
   - 一般 LLM 查詢仍可正確回傳商品卡。

## 預期效益

- 熱門分類行為穩定：點擊 L3 時不會再被 LLM 流程修改清單。
- 降低 LLM 使用量，縮短回應時間。
- 維持 LLM 自然語查詢的靈活性，同時保障熱門分類體驗。
