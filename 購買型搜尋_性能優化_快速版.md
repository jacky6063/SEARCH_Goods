# 🚀 購買型搜尋性能優化 - 5 分鐘版

## 📊 當前性能問題

| 指標 | 值 | 問題 |
|-----|-----|------|
| 平均搜尋時間 | 200-500ms | **太慢** (目標: < 100ms) |
| 全表計分 | 150ms | O(n*t) 複雜度 |
| 分類過濾 | 70ms | 逐行字符串匹配 |
| DataFrame 複製 | 4-5 次 | 不必要的內存開銷 |
| 排序複雜度 | O(n log n) | 多鍵排序降速 |

---

## 🎯 5 個優化方案速記

### ⭐ P0: 方案 1 - 分類索引 (快 20-30 倍)
```python
# 啟動時建立索引
class CategoryIndex:
    def search_l1(category) → [行索引]  # O(1) 而不是 O(n)
    def search_l2(category) → [行索引]  # O(1) 而不是 O(n)
```
- **改進**：分類搜尋 60ms → 2ms
- **啟動成本**：100ms (一次性)
- **代碼行數**：~50 行

### ⭐ P0: 方案 2 - 縮減計分範圍 (快 4-8 倍)
```python
# 先用索引篩選候選 (950 → 50-200 行)
# 再對候選計分 (而不是全表)
filtered_candidates = df.iloc[index.search_l1(...)]
filtered_candidates["score"] = filtered_candidates.apply(score_row)
```
- **改進**：計分時間 150ms → 30ms
- **組合 1+2 效果**：搜尋時間 200-500ms → 50-100ms

### ✅ P1: 方案 3 - 文本快取 (快 1.5-2 倍)
```python
# 啟動時預計算 _row_text() 到 df["__text_cache__"]
# 搜尋時直接用快取 (避免 3,800 次重複計算)
```
- **改進**：避免 50-100ms 浪費

### ✅ P1: 方案 4 - 單鍵排序 (快 2 倍)
```python
# 合併排序鍵: hits_name * 10000 + hits * 100 + score
# 單鍵排序比多鍵排序快
```
- **改進**：排序時間 40ms → 20ms

### ✅ P1: 方案 5 - 批量篩選 (快 3-5 倍)
```python
# 一個掩碼搭配所有條件 (複製 1 次而不是 4 次)
mask = required & ~excluded & price_range
filtered = df[mask]
```
- **改進**：篩選時間 30ms → 5ms

---

## 📈 性能提升幅度

| 優化組合 | 搜尋時間 | 性能倍數 | 推薦度 |
|--------|--------|--------|------|
| 當前 | **300ms** | 基線 | 當前狀態 |
| 方案 1+2 | **67ms** | **4.5 倍** ⬇️ | 🔥 推薦 |
| 方案 1+2+3+4+5 | **45ms** | **6.7 倍** ⬇️ | ⭐ 完全版 |

---

## 🎯 3 步實施計劃

### 第 1 步 (2-3 天) - P0 優化: 方案 1+2
```
目標: 搜尋時間 300ms → 67ms (快 4.5 倍)

□ 新建 CategoryIndex 類
□ 初始化索引 (啟動時)
□ 修改搜尋函數使用索引
□ 測試: 準確度 ✓, 性能 ✓
```

### 第 2 步 (3-5 天) - P1 優化: 方案 3+4+5
```
目標: 進一步優化到 45ms (累計快 6.7 倍)

□ 文本快取 (啟動時預計算)
□ 單鍵排序 (合併排序鍵)
□ 批量篩選 (一次掩碼)
□ 集成測試 ✓
```

### 第 3 步 (持續) - 監控與基準
```
□ 添加性能監控日誌
□ 設置告警 (搜尋 > 200ms 時)
□ 定期基準測試
□ 收集用戶反饋
```

---

## 💡 最重要的 2 個方案

如果時間有限，**優先實施方案 1 + 2**:

### 方案 1: 分類索引
```python
# goods_search_service.py

class CategoryIndex:
    def __init__(self, df):
        # 建立 {分類名稱 → [行索引...]}
        self.l1_index = {}  # L1: 大分類
        self.l2_index = {}  # L2: 中分類
        self.l3_index = {}  # L3: 小分類
        
        for col in ["CateName_L1", "大分類名稱"]:
            if col in df.columns:
                for idx, cat in enumerate(df[col]):
                    cat = str(cat or "").strip()
                    if cat:
                        self.l1_index.setdefault(cat, []).append(idx)

_category_index = None

def get_category_index():
    global _category_index
    if _category_index is None:
        df = load_data(str(DEFAULT_DATA_PATH))
        _category_index = CategoryIndex(df)
    return _category_index
```

### 方案 2: 使用索引
```python
# llm_service.py _search_products_for_chat()

# 舊: 全表計分 O(n)
# results = search_products(df, query, topn=10)

# 新: 先篩選候選，再計分
if hierarchy:
    index = get_category_index()
    candidates = df.iloc[index.search_l1(hierarchy["L1"])]
else:
    candidates = df

results, terms = search_products(candidates, query, topn=10)
```

---

## 🧪 驗證清單

```
優化前後對比測試:

查詢: "我要買食品類的調味油"
CSV: 953 行商品

□ 搜尋結果準確度: 👍 (結果相同)
□ 搜尋時間: 
  - 優化前: 200-500ms
  - 優化後 (1+2): 50-100ms ✓
  - 優化後 (1+2+3+4+5): 30-80ms ✓
□ 啟動時間: < 1 秒 ✓
□ 邊界情況:
  □ 分類不匹配時 ✓
  □ 無結果時 ✓
  □ 混合查詢 ✓
```

---

## 📊 優化方案對標 (3 秒速查)

| 方案 | 難度 | 性能倍數 | 啟動成本 | 推薦 |
|-----|------|--------|--------|------|
| 1 (分類索引) | ⭐⭐ | 20x | 100ms | 🔥 必做 |
| 2 (縮減計分) | ⭐⭐ | 4-5x | 0ms | 🔥 必做 |
| 3 (文本快取) | ⭐ | 1.5-2x | 50ms | ✅ 推薦 |
| 4 (單鍵排序) | ⭐ | 2x | 0ms | ✅ 推薦 |
| 5 (批量篩選) | ⭐ | 3-5x | 0ms | ✅ 推薦 |

---

**詳細版**：見 `購買型商品搜尋_性能優化建議.md` (532 行)

