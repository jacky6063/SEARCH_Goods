# 🔧 購買型搜尋性能優化 - 實施指南

## 📋 本文檔概述

這份文檔提供了性能優化的**具體實施步驟**，包括：
- 完整代碼範例
- 測試驗證方式
- 集成檢查清單
- 常見問題解決

---

## 🚀 Phase 1: 實施方案 1 + 2 (核心優化)

### 步驟 1.1: 新建 CategoryIndex 類

**文件**：`backend/goods_search_service.py`

在文件末尾添加：
```python
class CategoryIndex:
    """
    分類層級索引，將 O(n) 掃描改為 O(1) 查詢
    
    用途：
    - 快速查詢某分類下的所有商品行索引
    - 支援多層級查詢 (L1/L2/L3)
    - 啟動時構建，查詢時 O(1)
    """
    
    def __init__(self, df: pd.DataFrame):
        """初始化索引"""
        self.l1_index = self._build_level_index(df, ["CateName_L1", "大分類名稱"])
        self.l2_index = self._build_level_index(df, ["CateName_L2", "中分類名稱"])
        self.l3_index = self._build_level_index(df, ["CateName_L3", "小分類名稱"])
        
        print(f"[INFO] CategoryIndex built: L1={len(self.l1_index)}, L2={len(self.l2_index)}, L3={len(self.l3_index)}")
    
    def _build_level_index(self, df: pd.DataFrame, col_variants: List[str]) -> Dict[str, List[int]]:
        """
        為指定的分類欄位構建索引
        
        返回: {分類名稱 → [行索引列表]}
        例如: {"食品": [0, 5, 10, ...], "衣物": [1, 3, 7, ...]}
        """
        # 找到存在的欄位
        col = None
        for variant in col_variants:
            if variant in df.columns:
                col = variant
                break
        
        if col is None:
            return {}
        
        # 構建索引字典
        index: Dict[str, List[int]] = {}
        for idx, row in df.iterrows():
            cat = str(row.get(col, "")).strip()
            if cat and cat != "":  # 跳過空值
                if cat not in index:
                    index[cat] = []
                index[cat].append(int(idx))
        
        return index
    
    def search_l1(self, category_name: str) -> List[int]:
        """O(1) 查詢 L1 分類"""
        category_name = str(category_name or "").strip()
        return self.l1_index.get(category_name, [])
    
    def search_l2(self, category_name: str) -> List[int]:
        """O(1) 查詢 L2 分類"""
        category_name = str(category_name or "").strip()
        return self.l2_index.get(category_name, [])
    
    def search_l3(self, category_name: str) -> List[int]:
        """O(1) 查詢 L3 分類"""
        category_name = str(category_name or "").strip()
        return self.l3_index.get(category_name, [])
    
    def get_categories(self, level: str) -> List[str]:
        """獲取某層級的所有分類名稱"""
        if level == "L1":
            return list(self.l1_index.keys())
        elif level == "L2":
            return list(self.l2_index.keys())
        elif level == "L3":
            return list(self.l3_index.keys())
        return []


# 全局索引實例
_category_index: Optional[CategoryIndex] = None


def get_category_index() -> CategoryIndex:
    """懶加載獲取分類索引"""
    global _category_index
    if _category_index is None:
        try:
            df = load_data(str(DEFAULT_DATA_PATH))
            _category_index = CategoryIndex(df)
        except Exception as e:
            print(f"[ERROR] Failed to build category index: {e}")
            _category_index = CategoryIndex(pd.DataFrame())
    return _category_index
```

### 步驟 1.2: 新增優化版搜尋函數

在 `backend/goods_search_service.py` 中新增：
```python
def search_products_with_hierarchy(
    df: pd.DataFrame,
    query: str,
    hierarchy: Optional[Dict[str, str]] = None,
    topn: int = 10,
    min_score: float = 1.5,
) -> Tuple[List[Dict[str, Any]], List[str]]:
    """
    🆕 優化版: 利用分類索引加速搜尋
    
    流程：
    1. 如果有分類信息，用索引篩選候選集 (O(1))
    2. 對候選集計分 (O(候選數 * 詞彙數) 而不是 O(950 * 詞彙數))
    3. 執行其餘篩選和排序
    
    預期性能：搜尋時間從 200-500ms 改善到 50-100ms
    """
    
    if hierarchy and any(hierarchy.get(k) for k in ["L1", "L2", "L3"]):
        # 使用分類索引加速
        cat_index = get_category_index()
        candidate_indices = set(range(len(df)))
        
        # 逐層篩選候選集
        if hierarchy.get("L1"):
            l1_indices = cat_index.search_l1(hierarchy["L1"])
            if l1_indices:
                candidate_indices &= set(l1_indices)
                print(f"[DEBUG] L1 filter: {len(l1_indices)} candidates")
        
        if hierarchy.get("L2"):
            l2_indices = cat_index.search_l2(hierarchy["L2"])
            if l2_indices:
                candidate_indices &= set(l2_indices)
                print(f"[DEBUG] L2 filter: {len(l2_indices)} candidates")
        
        if hierarchy.get("L3"):
            l3_indices = cat_index.search_l3(hierarchy["L3"])
            if l3_indices:
                candidate_indices &= set(l3_indices)
                print(f"[DEBUG] L3 filter: {len(l3_indices)} candidates")
        
        # 只對候選集操作
        if candidate_indices:
            df_candidates = df.iloc[list(candidate_indices)].copy()
            print(f"[DEBUG] Candidate pool: {len(df_candidates)} / {len(df)}")
        else:
            # 降級：如果沒有符合分類的商品，使用全表
            print(f"[DEBUG] No candidates for hierarchy, falling back to full search")
            df_candidates = df.copy()
    else:
        # 無分類信息，使用全表
        df_candidates = df.copy()
    
    # 對候選集執行原始搜尋邏輯
    return search_products(
        df_candidates,
        query=query,
        topn=topn,
        min_score=min_score,
    )
```

### 步驟 1.3: 集成到 llm_service.py

修改 `backend/llm_service.py` 中的 `_search_products_for_chat()` 函數：

```python
# llm_service.py 行 1254 附近

def _search_products_for_chat(
    query: str, keywords: List[str], topn: int = 5, filters: Optional[Dict[str, Any]] = None,
    hierarchy: Optional[Dict[str, str]] = None  # 🆕 增加分類層級參數
) -> Dict[str, List[Dict[str, Any]]]:
    result = {"exact": [], "fuzzy": []}
    if not query:
        return result
    try:
        df = _get_chat_df()
        if df is None or df.empty:
            return result

        # 🆕 優先使用分類層級搜尋
        if hierarchy and any(hierarchy.get(k) for k in ["L1", "L2", "L3"]):
            from goods_search_service import search_products_with_hierarchy  # 🆕
            
            hierarchy_results, _ = search_products_with_hierarchy(
                df,
                query=query,
                hierarchy=hierarchy,
                topn=topn * 2
            )
            
            if hierarchy_results:
                result["exact"] = hierarchy_results[:topn]
                print(f"[INFO] Hierarchy search returned {len(result['exact'])} products")
                # 若分類搜尋結果充足，直接返回
                if len(result["exact"]) >= topn:
                    return result
                # 否則繼續進行補充搜尋

        # 🔧 原有的模糊搜尋邏輯保持不變
        fuzzy_records, _ = search_products(
            df,
            query,
            topn=topn,
            sort_price=False,
        )
        # ... 其餘邏輯不變
        
        return result
    except Exception as exc:
        _logger.exception("chat product search failed: %s", exc)
        return result
```

---

## 🧪 步驟 2: 驗證與測試

### 測試 2.1: 單元測試

**文件**：`backend/tests/test_performance.py` (新建)

```python
import time
import pandas as pd
from goods_search_service import (
    load_data, 
    CategoryIndex, 
    get_category_index,
    search_products_with_hierarchy,
    search_products,
    DEFAULT_DATA_PATH,
)

def test_category_index_creation():
    """測試分類索引的構建"""
    df = load_data(str(DEFAULT_DATA_PATH))
    index = CategoryIndex(df)
    
    # 驗證索引非空
    assert len(index.l1_index) > 0, "L1 索引不應為空"
    assert len(index.l2_index) > 0, "L2 索引不應為空"
    print(f"✓ 分類索引構建成功: L1={len(index.l1_index)}, L2={len(index.l2_index)}, L3={len(index.l3_index)}")


def test_index_query_performance():
    """測試分類索引查詢性能 (應 < 1ms)"""
    index = get_category_index()
    
    # 選取一個存在的分類
    l1_categories = index.get_categories("L1")
    if not l1_categories:
        print("✗ 沒有 L1 分類可測試")
        return
    
    test_category = l1_categories[0]
    
    # 測試查詢時間
    start = time.time()
    for _ in range(1000):
        results = index.search_l1(test_category)
    elapsed = (time.time() - start) / 1000
    
    print(f"✓ 索引查詢性能: {elapsed*1000:.3f}ms (應 < 1ms)")
    assert elapsed < 0.001, f"索引查詢過慢: {elapsed*1000:.1f}ms"


def test_search_with_hierarchy():
    """測試優化搜尋的準確度和性能"""
    df = load_data(str(DEFAULT_DATA_PATH))
    
    # 選取一個存在的分類進行測試
    index = get_category_index()
    l1_categories = index.get_categories("L1")
    if not l1_categories:
        print("✗ 沒有 L1 分類可測試")
        return
    
    test_category = l1_categories[0]
    hierarchy = {"L1": test_category, "L2": "", "L3": ""}
    
    # 優化版搜尋
    start = time.time()
    results_optimized, _ = search_products_with_hierarchy(
        df,
        query="商品",
        hierarchy=hierarchy,
        topn=10
    )
    elapsed_optimized = time.time() - start
    
    print(f"✓ 優化搜尋完成: {len(results_optimized)} 結果, 耗時 {elapsed_optimized*1000:.1f}ms")


def test_search_accuracy():
    """測試優化前後的搜尋結果是否一致"""
    df = load_data(str(DEFAULT_DATA_PATH))
    query = "食品"
    
    # 非優化版
    results_original, _ = search_products(df, query=query, topn=5)
    
    # 優化版 (無分類信息)
    results_optimized, _ = search_products_with_hierarchy(
        df,
        query=query,
        hierarchy=None,  # 無分類，應與原版相同
        topn=5
    )
    
    # 比較結果
    original_ids = [r.get("GoodIden") for r in results_original]
    optimized_ids = [r.get("GoodIden") for r in results_optimized]
    
    print(f"✓ 搜尋結果一致性檢查:")
    print(f"  原版: {original_ids}")
    print(f"  優化版: {optimized_ids}")
    
    # 注意: 可能不完全相同，因為優化版使用不同候選集
    # 但應該有高度重疊


if __name__ == "__main__":
    print("=" * 50)
    print("購買型搜尋性能優化 - 測試套件")
    print("=" * 50)
    
    test_category_index_creation()
    print()
    
    test_index_query_performance()
    print()
    
    test_search_with_hierarchy()
    print()
    
    test_search_accuracy()
    print()
    
    print("=" * 50)
    print("✓ 所有測試通過!")
    print("=" * 50)
```

**運行測試**：
```bash
cd backend
python tests/test_performance.py
```

### 測試 2.2: 性能基準測試

```bash
# 創建性能對比腳本
python3 << 'EOF'
import time
from goods_search_service import (
    load_data,
    search_products,
    search_products_with_hierarchy,
    DEFAULT_DATA_PATH,
)

df = load_data(str(DEFAULT_DATA_PATH))

test_cases = [
    {
        "query": "食品類調味油",
        "hierarchy": {"L1": "食品", "L2": "調味油", "L3": ""},
    },
    {
        "query": "女用背包",
        "hierarchy": {"L1": "包", "L2": "", "L3": ""},
    },
    {
        "query": "鞋子",
        "hierarchy": {"L1": "鞋", "L2": "", "L3": ""},
    },
]

print("=" * 70)
print("性能基準測試: 優化前 vs 優化後")
print("=" * 70)

for i, test in enumerate(test_cases, 1):
    query = test["query"]
    hierarchy = test["hierarchy"]
    
    # 原版搜尋
    start = time.time()
    results_original, _ = search_products(df, query=query, topn=10)
    time_original = time.time() - start
    
    # 優化版搜尋
    start = time.time()
    results_optimized, _ = search_products_with_hierarchy(
        df,
        query=query,
        hierarchy=hierarchy,
        topn=10
    )
    time_optimized = time.time() - start
    
    speedup = time_original / time_optimized
    
    print(f"\n測試 {i}: {query}")
    print(f"  原版時間: {time_original*1000:.1f}ms (結果: {len(results_original)})")
    print(f"  優化版: {time_optimized*1000:.1f}ms (結果: {len(results_optimized)})")
    print(f"  性能提升: {speedup:.1f}x ↓")

print("\n" + "=" * 70)
EOF
```

---

## ✅ Phase 1 集成檢查清單

```
□ 代碼實施
  □ CategoryIndex 類已添加到 goods_search_service.py
  □ search_products_with_hierarchy() 已實現
  □ get_category_index() 全局函數已定義
  □ 修改 llm_service.py 使用層級搜尋

□ 測試驗證
  □ 單元測試通過: test_performance.py ✓
  □ 分類索引構建成功 ✓
  □ 索引查詢性能 < 1ms ✓
  □ 搜尋結果準確 ✓
  □ 性能基準測試完成 ✓

□ 集成驗證
  □ 啟動後端，確認無錯誤
  □ 測試 /api/chat 端點
  □ 驗證搜尋結果正確
  □ 監控搜尋時間 (應 < 100ms)

□ 代碼提交
  □ git add -A
  □ git commit -m "優化: 實施分類索引加速購買型搜尋"
  □ git push origin main
```

---

## 🚀 Phase 2: 後續優化 (方案 3-5)

如果 Phase 1 達到預期效果，可依次實施：

### 優化方案 3: 文本快取 (3-5 天)
- 啟動時預計算 _row_text() 結果
- 搜尋時從快取讀取
- 預期性能提升: +30-50%

### 優化方案 4: 單鍵排序 (1-2 天)
- 合併排序鍵為單個複合值
- 簡化排序邏輯
- 預期性能提升: +30-50%

### 優化方案 5: 批量篩選 (1-2 天)
- 使用單個布林掩碼替代逐次過濾
- 減少 DataFrame 複製
- 預期性能提升: +20-30%

---

## 📊 預期結果

### 在實施 Phase 1 後

```
查詢: "我要買食品類的調味油，特別是橄欖油"
CSV: 953 行商品

┌─────────────────┬──────────┬──────────┬────────────┐
│ 指標            │ 優化前   │ 優化後   │ 改進       │
├─────────────────┼──────────┼──────────┼────────────┤
│ 全表計分        │ 150ms    │ 30ms     │ 5 倍 ↓     │
│ 分類過濾        │ 70ms     │ 2ms      │ 35 倍 ↓    │
│ 篩選操作        │ 30ms     │ 5ms      │ 6 倍 ↓     │
│ 排序            │ 40ms     │ 20ms     │ 2 倍 ↓     │
│ 其他開銷        │ 10ms     │ 10ms     │ 無變化     │
├─────────────────┼──────────┼──────────┼────────────┤
│ 📊 總計         │ 300ms    │ 67ms     │ 4.5 倍 ↓   │
└─────────────────┴──────────┴──────────┴────────────┘

🎯 用戶體驗: 搜尋時間從「明顯延遲」改善到「幾乎即時」
```

---

## 💬 常見問題

### Q1: 分類索引會增加多少啟動時間?
**A**: ~100ms 一次性開銷。在 app.py 中懶加載，首次調用時初始化。

### Q2: 如果分類信息不準確怎麼辦?
**A**: 會自動降級到全表搜尋。設有檢查: 如果無候選結果，使用原始方法。

### Q3: 能與現有代碼共存嗎?
**A**: 完全可以。新函數 `search_products_with_hierarchy()` 是獨立的，原 `search_products()` 保持不變。

### Q4: 需要修改 CSV 格式嗎?
**A**: 不需要。只要 CSV 有 L1/L2/L3 欄位即可。

---

**相關文檔**：
- `購買型商品搜尋_性能優化建議.md` - 完整原理
- `購買型搜尋_性能優化_快速版.md` - 5 分鐘速查

