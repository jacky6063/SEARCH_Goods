# 任務卡：熱門分類（含磐鈺建設）聊天區與搜尋流程修正

## 目標
- 點擊熱門分類 chips 時，統一走「分類導覽 → L3 直搜 → 商品卡/空狀態」，避免 LLM 閒聊。
- 確保 L1「磐鈺建設」下的 L2/L3 可直接顯示商品卡或固定空狀態提示。
- 地端/線上皆以正確的 `goods_categories.csv`（含磐鈺建設 4 條）為來源，並透過 `CATEGORIES_PATH` 指向新版。

## 範圍
- 前端：熱門分類 UI 行為、聊天區顯示策略（僅與熱門分類交互相關）。
- 後端：分類導覽/搜尋路由的判斷（from_hot_category、category_hierarchy）、`CATEGORIES_PATH` 設定與分類快取清除。
- 測試：新增/更新熱分類流程的驗證用例。

## 不在範圍
- 不變動商品搜尋/訂單/會員/價格/庫存等商業邏輯。
- 不變更專案架構、路由設計、目錄結構。
- 不處理其他品牌或非熱門分類的體驗優化，除非與本任務直接衝突。

## 禁止事項（務必遵守）
***禁止重構整個專案**
 - 不得主動改動專案架構、路由設計、目錄結構，除非任務卡明確要求。
 - 不得自行「最佳化」或「重寫」與任務無關的模組。

**禁止修改既有商業邏輯**
 - 不得更改任何現有「商品搜尋 / 訂單 / 會員 / 價格計算 / 庫存」邏輯。
 - 任何與 DB schema 有關的變更，一律不在本專案範圍。

**禁止硬編 API Key / 機敏資訊**
 - `OPENAI_API_KEY` 一律由環境變數提供，不得寫死在程式碼。
 - 本文件提到的金鑰皆為占位符，實際值由人類手動設定。

**一次只執行一張任務卡**
 - 任務卡是分階段的 PM 規劃。  
 - AI 在執行任務卡 N 時，**不得偷跑任務卡 N+1 的內容**。
 - 每張卡完成後，由人類檢查、測試，再決定是否進入下一張。

**若有不確定的地方，以「少改、不動舊邏輯」優先**
 - 無法判斷時，寧可只加註解 / 提出建議，不要直接修改大量程式。

## 需求摘要
1) 熱門分類點擊流程  
   - L1 點擊：展開 L2 chips；聊天區不產生 LLM 回覆。  
   - L2 點擊：展開 L3 chips；聊天區不產生 LLM 回覆。  
   - L3 點擊：直接以 `{L1,L2,L3}` 執行商品搜尋，渲染商品卡；若無結果，顯示固定空狀態提示（例如「目前沒有找到該分類的商品」）。禁止 LLM 閒聊回覆。
2) 磐鈺建設特例  
   - 確保 `goods_categories.csv` 含 4 條磐鈺分類；`CATEGORIES_PATH` 指向新版。  
   - 點擊磐鈺建設路徑時也走上述直搜與空狀態提示，無 LLM 閒聊。
3) 分類來源與快取  
   - 地端/線上設定 `CATEGORIES_PATH` 指向新版 CSV；更新後需重啟或 `/api/admin/clear-cache` 清除分類快取。  
   - 驗證 `/api/catalog/scope?level=L1` 回 6 個大分類（含磐鈺建設）。

## 成功驗收標準
- 點擊任一 L3（含磐鈺建設）後，直接看到商品卡或固定空狀態提示，聊天區無閒聊回答。
- `/api/catalog/scope?level=L1` 線上/地端皆返回 6 個 L1（含磐鈺建設）。
- 相關自動化/手動測試覆蓋：L1/L2 導覽、L3 直搜、無結果空狀態。

## 待人類確認後才能動手的修改
- 以上流程與禁止事項確認無誤後，才可開始編碼與提交。  
- 若需新增測試，限於熱門分類相關，且不得改動其他業務模組。
