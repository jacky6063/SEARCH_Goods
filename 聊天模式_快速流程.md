# 聊天模式 LLM 判讀流程 - 快速版 (1 分鐘版)

## 📊 簡化流程圖

```
用戶查詢
  ↓
┌─────────────────────────────────────────────┐
│ chat_endpoint() @app.post(/api/chat)        │
└─────────────────────────────────────────────┘
  ↓
┌─────────────────────────────────────────────┐
│ _legacy_chat_flow() 邏輯分派                │
└─────────────────────────────────────────────┘
  ↓
┌──────────────┬──────────────┬──────────────┐
│  類目導覽    │   概覽問句   │  LLM 判讀    │
│ (L1→L2→L3)  │ (賣什麼)     │  (主流程)    │
└──────────────┴──────────────┴──────────────┘
               ↓ (主流程進行)
         LLM 4 層判讀
         ├─ Step 1: 意圖分析 (LLM)
         ├─ Step 2: 對話分類
         ├─ Step 3: 商品搜尋 (多層)
         └─ Step 4: 自然回覆 (LLM)
               ↓
         ChatResp JSON
               ↓
         前端顯示結果
```

## 🔑 核心 4 步驟

### Step 1️⃣: 意圖分析
- **函數**：`llm_analyze_query()` (llm_service.py:1740)
- **功能**：LLM 分析查詢，提取 L1/L2/L3 分類層級
- **輸出**：意圖 JSON + 分類層級

### Step 2️⃣: 對話意圖分類
- **函數**：`_detect_conversation_intent()` (llm_service.py:1570)
- **決策**：
  - `information` → 資訊回答 (不搜尋商品)
  - `product_search` → 執行商品搜尋
  - `event_food_planning` → 活動策劃
  - `general` → 一般對話

### Step 3️⃣: 多層商品搜尋
- **函數**：`_search_products_for_chat()` (llm_service.py:1254)
- **優先級**：
  1. 分類層級搜尋 (L1→L2→L3)
  2. 模糊搜尋 (名稱/描述/分類)
  3. 結構化篩選 (必須詞/排除詞/價格)
  4. 嚴格搜尋 (備用)
  5. 關鍵詞篩選 (最後)

### Step 4️⃣: 自然回覆生成
- **函數**：`_mock_or_real_llm()` (llm_service.py:1486)
- **過程**：LLM 或 Mock 生成回覆 + 隱藏 JSON
- **JSON**：包含商品 ID/名稱用於前端展示

## 📍 代碼速查

| 步驟 | 文件 | 行號 | 函數名 |
|------|------|------|--------|
| 入口 | app.py | 1373 | `chat_endpoint()` |
| 分派 | chat_router_goods_action.py | 1000 | `_legacy_chat_flow()` |
| 意圖 | llm_service.py | 1740 | `llm_analyze_query()` |
| 分類 | llm_service.py | 1570 | `_detect_conversation_intent()` |
| 搜尋 | llm_service.py | 1254 | `_search_products_for_chat()` |
| 層級 | llm_service.py | 1100 | `_search_by_category_hierarchy()` |
| 回覆 | llm_service.py | 1486 | `_mock_or_real_llm()` |

## 🎯 3 大使用情景

### 1️⃣ 購買意圖 (主流程)
```
用戶：「我要買食品類的調味油」
  ↓ Step 1-2: LLM 意圖分析 → product_search
  ↓ Step 3: 多層搜尋 (L1=食品 → L2=調味油)
  ↓ Step 4: 自然回覆 + 商品列表
前端：顯示 8-12 件符合的調味油商品
```

### 2️⃣ 資訊諮詢
```
用戶：「椰子油對健康有什麼幫助？」
  ↓ Step 1-2: LLM 意圖分析 → information
  ↓ 直接調用 LLM 回答（不搜尋商品）
  ↓ Step 4: 資訊回覆 + 自然引導
前端：只顯示文字回覆，不顯示商品
```

### 3️⃣ 概覽查詢 (快速路徑)
```
用戶：「你們店裡賣什麼？」
  ↓ 快速檢測 (不走 LLM)
  ↓ 讀取 /api/catalog/scope 的 Top-K L1
  ↓ 直接返回分類列表
前端：顯示 8 大分類供選擇
```

## 🛠️ 關鍵環境變數

```bash
OPENAI_API_KEY=sk-...            # OpenAI key
CHAT_USE_LLM_INTENT=True         # ✅ 必啟用
CHAT_USE_LLM_EXPAND=True         # ✅ 必啟用
CHAT_USE_LLM_RERANK=False        # ⚠️ 建議關閉（慢）
CHAT_OPENAI_MODEL=gpt-4o-mini    # 聊天模型
```

## ✅ 完整流程檢查清單

```
□ 用戶輸入查詢
□ chat_endpoint() 接收請求
□ _legacy_chat_flow() 分派
□ llm_analyze_query() 意圖分析
  └─ 提取 L1/L2/L3 分類層級
□ _detect_conversation_intent() 分類
□ 根據意圖選擇路徑：
  ├─ product_search → 商品搜尋
  ├─ information → 資訊回答
  └─ event_food_planning → 活動策劃
□ _search_products_for_chat() (如需要)
  └─ 多層搜尋 + 篩選
□ _mock_or_real_llm() 生成回覆
□ bundle_service.save_bundle() 會話快取
□ 返回 ChatResp JSON
□ 前端解析並顯示
```

---

**詳細版**：見 `聊天模式_LLM判讀流程圖.md`

