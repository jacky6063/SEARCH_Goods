# 重要提示摘要

1. 術語/參數對齊要點  
   - 分類用詞：食品 → 常溫食品；米麵/米麞 → 五穀/豆類/米麵/乾貨  
   - API 參數：parent → parent_l1 / parent_l2  
   - 示例 URL：若需顯式，統一 http://localhost:8000  
   - 熱門分類 L3 payload 建議：from_hot_category=true、disable_rerank=true、page_size=24  
   - 參考：docs/術語對照快表.md  

2. 統一日誌格式  
   - 專案採用 `backend/utils/logging_utils.py` 的 `configure_structured_logging` 與 `get_logger`，確保所有模組透過 `logging` 而非 `print` 輸出。  
   - 每筆 log 會自動帶上 `session_id` 與 `intent`，方便追蹤單一聊天或查詢流程，也便於匯集到集中式日誌平台。

3. 聊天與 LLM 模組已切換為結構化日誌  
   - `backend/chat_router_goods_action.py` 內所有診斷訊息（包含澄清流程、LLM fallback、推薦同步等）都經由 `LOGGER`，因此任一對話出狀況時可從 log 迅速定位 session。  
   - `backend/llm_service.py` 亦改用 `_logger`，涵蓋查詢意圖、商品格式化、行銷文案生成等，大幅降低散落的 `print` 所造成的追蹤困難。

4. 為什麼需要這樣的設計  
   - 先前版本雖能運作，但缺乏統一的 log 格式與上下文，難以在大量請求中追蹤特定 session。  
   - 明確要求「所有日誌需帶 session/context 並送到集中平台」後，工程師才會採用這種統一做法；需求未指明時，通常預設只寫 `print`。

5. 後續溝通建議  
   - 若希望任何功能具備可觀測性或特定輸出格式，務必在需求中註明：「log 需含 A/B 欄位，可匯入 C 平台」。  
   - 同理，若要長期維護 LLM client、cache、fallback 等，也可以先列出期待的行為（例如 client 快取、錯誤上報），工程師就能一次到位。
