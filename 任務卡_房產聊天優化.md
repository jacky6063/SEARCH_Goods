# 任務卡：房地產意圖的聊天話術優化（非熱門分類點擊）

## 目標
- 當使用者在「一般聊天輸入」提到房地產關鍵詞/分類（如磐鈺建設、磐鈺草間漫漫、極致輕寓2房等）時，套用房產專員話術，避免出現與房地產無關的泛用引導。
- 僅調整房產意圖的 LLM 提示詞/路由，不影響其他商品/維修/熱門分類點擊流程。

## 範圍
- 聊天意圖判斷：增加「房產意圖」分支（依關鍵詞/分類白名單判定）。
- 聊天生成：房產意圖時換用房產專員提示詞，語氣簡潔專業，控制長度，避免泛用寒暄。
- 無結果處理：若房產分類查無商品，回覆固定模板（例如「目前沒有找到該分類的房源，需不需要改看其他坪型或社區？」），不讓 LLM 自由發揮。

## 不在範圍
- 不改熱門分類點擊的前端流程（仍可直搜並顯示商品卡）。
- 不改商品搜尋/訂單/會員/價格/庫存等商業邏輯。
- 不改專案架構、路由設計、目錄結構。

## 禁止事項（務必遵守）
***禁止重構整個專案**
 - 不得主動改動專案架構、路由設計、目錄結構，除非任務卡明確要求。
 - 不得自行「最佳化」或「重寫」與任務無關的模組。

**禁止修改既有商業邏輯**
 - 不得更改任何現有「商品搜尋 / 訂單 / 會員 / 價格計算 / 庫存」邏輯。
 - 任何與 DB schema 有關的變更，一律不在本專案範圍。

**禁止硬編 API Key / 機敏資訊**
 - `OPENAI_API_KEY` 一律由環境變數提供，不得寫死在程式碼。
 - 本文件提到的金鑰皆為占位符，實際值由人類手動設定。

**一次只執行一張任務卡**
 - 任務卡是分階段的 PM 規劃。  
 - AI 在執行任務卡 N 時，**不得偷跑任務卡 N+1 的內容**。
 - 每張卡完成後，由人類檢查、測試，再決定是否進入下一張。

**若有不確定的地方，以「少改、不動舊邏輯」優先**
 - 無法判斷時，寧可只加註解 / 提出建議，不要直接修改大量程式。

## 需求摘要
1) 房產意圖偵測：依房產關鍵詞/分類白名單（含磐鈺建設系列）判斷，命中即走房產意圖。  
2) 提示詞改版（房產專員）：  
   - 身份：房產專員/社區顧問；語氣簡潔專業、不寒暄、不自介。  
   - 內容：一句定位 + 2–4 個 bullet（坪數/格局、採光/通風、位置/生活圈等），CTA 收尾（例：「需要看其他坪型或預約賞屋嗎？」）。  
   - 長度控制：整體 60–80 字（不含 bullet），避免冗長行銷文。  
   - 禁忌：不要詢問「日常外出/送禮/工作」等非房產用途。  
3) 無結果模板：房產意圖但無商品時，用固定文案，避免 LLM 自由發揮。  
4) 安全性：未命中房產意圖時，保留原有聊天提示詞和路由；不得影響其他意圖與流程。  

## 成功驗收標準
- 在聊天輸入「磐鈺建設」或「極致輕寓2房」時，回覆為房產專員口吻（定位 + bullet + CTA），無與房產無關的用途詢問。
- 未提及房產關鍵詞時，聊天行為維持原樣，無變更。
- 無結果時收到固定模板提示，無自由發揮的長篇行銷文。
- 熱門分類點擊與商品/維修等其他流程行為不變。

## 待人類確認後才能動手的修改
- 房產關鍵詞/分類白名單來源（含哪些 L1/L2/L3/品牌詞）。  
- 房產專員提示詞的具體文案格式（可再精煉字數/項目）。  
- 無結果模板的用詞與 CTA。  

## 風險與回滾
- 風險：意圖誤判導致非房產對話也套房產話術。  
  - 緩解：白名單匹配 + 逐步測試；若誤判，保留開關可快速回退至原提示詞。  
- 回滾：保留原提示詞與意圖路由分支，必要時切回原版。  
