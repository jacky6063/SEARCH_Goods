# 待改善警告清單（測試與依賴層）

近期 pytest 全跑後（202 passed, 6 skipped）仍有多組警告，已記錄如下，後續可分批優化：

## 框架/相容性
- FastAPI `@app.on_event` 已標示 deprecated，應改用 lifespan handler。
- Pydantic v2 提示：`model.dict()` 改 `model_dump()`（chat_router_goods_action 等）。
- urllib3 警告：當前環境使用 LibreSSL 2.8.3，urllib3 v2 只支援 OpenSSL 1.1.1+，需升級 ssl/urllib3 或鎖定相容版本。

## 測試/插件
- PytestUnhandledCoroutineWarning：async def 測試被跳過，需加入 pytest-asyncio 或重寫測試為同步。
- PytestReturnNotNoneWarning：多個測試函式返回非 None，應改用 assert（mushroom、repair_cloud、company_rich_content 等）。

## 建議的後續處理步驟（分任務執行）
1. FastAPI/Pydantic 相容改造：逐檔改 lifespan + model_dump，跑全測。
2. async 測試：引入 pytest-asyncio 或重構 async 測試，確保被執行。
3. urllib3/ssl：調整 Python/ssl 環境或鎖定 urllib3 相容版，消除 NotOpenSSLWarning。
4. ReturnNotNone：逐個測試改用 assert，避免未來 pytest 錯誤。***
