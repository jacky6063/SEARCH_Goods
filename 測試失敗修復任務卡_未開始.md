# 測試失敗修復任務卡

## 不在範圍
- 不重構專案架構、路由設計、目錄結構。
- 不改 LLM 模型與金鑰設置；不硬編 API Key。

## 禁止事項（務必遵守）
- 禁止重構整個專案：不得主動改動架構、路由、目錄，除非任務卡明確要求。
- 禁止修改既有商業邏輯：不得更動商品搜尋/訂單/會員/價格/庫存與 DB schema。
- 禁止硬編 API Key / 機敏資訊：所有金鑰一律用環境變數，提示詞/程式碼不可寫死。
- 一次只執行一張任務卡：不得偷跑下一張卡的內容。
- 不確定時少改：無法判斷時，優先加註解/提議，不要大改。
- 不執行的命令：不得使用 `git reset --hard`、`git checkout -- .`、`rm -rf /` 等破壞性指令；不得清除非本任務相關檔案。

**日期**: 2025年12月5日  
**狀態**: 待修復  
**優先級**: P0 (高)  
**影響範圍**: 分類導覽功能

---

## 📊 測試執行摘要

```bash
pytest tests/ -q
```

**結果統計**:
- ✅ **通過**: 177 測試 (94.7%)
- ❌ **失敗**: 6 測試 (3.2%)
- ⏭️ **跳過**: 6 測試 (3.2%)
- ⚠️ **警告**: 16 個

---

## 🔴 失敗測試清單

### 類別 A: `display_mode` 欄位缺失 (4 個)

| # | 測試檔案 | 測試函數 | 行號 | 錯誤類型 |
|---|---------|---------|------|---------|
| 1 | `test_category_navigation_parsing.py` | `test_parse_l1_navigation` | 23 | `display_mode` 為 `None` |
| 2 | `test_category_navigation_parsing.py` | `test_parse_l2_navigation` | 37 | `display_mode` 為 `None` |
| 3 | `test_chat_overview_scope.py` | `test_chat_category_navigation_l1_to_l2` | 38 | `display_mode` 為 `None` |
| 4 | `test_chat_overview_scope.py` | `test_chat_overview_lists_l1_scope` | 27 | `display_mode` 為 `None` |

**期望值**: `"text_only"`  
**實際值**: `None`

---

### 類別 B: 分類層級判斷錯誤 (2 個)

| # | 測試檔案 | 測試函數 | 行號 | 問題描述 |
|---|---------|---------|------|---------|
| 5 | `test_category_navigation_parsing.py` | `test_parse_l2_navigation` | 40 | `level` 返回 `"L1"` 而非 `"L2"/"L3"` |
| 6 | `test_chat_overview_scope.py` | `test_chat_category_navigation_specific_l2_l3` | 55 | `level` 返回 `"L1"` 而非 `"L3"` |

**查詢範例**:
- "在常溫食品下我對零食點心有興趣，還有哪些小分類？" → 期望 L2/L3，實際 L1
- "想看女用背包分類有哪些" → 期望 L3，實際 L1

---

### 類別 C: 分類名稱抽取失敗 (2 個)

| # | 測試檔案 | 測試函數 | 行號 | 問題描述 |
|---|---------|---------|------|---------|
| 7 | `test_category_navigation_parsing.py` | `test_extract_l3_from_slash_terms` | 48 | 無法抽取 L1 "常溫食品" |
| 8 | `test_category_navigation_parsing.py` | `test_extract_l2_from_fashion_bag_keywords` | 57 | 無法抽取 L1 "時尚女性" |

**查詢範例**:
- "在常溫食品下我要找豆包/豆腐/米類/佐醬湯料" → `selected["L1"]` 為 `None`
- "在時尚女性下我要找包包還有哪些分類重點？" → `selected["L1"]` 為 `None`

---

## 🔍 根本原因分析

### 問題 1: Mock LLM 模式下返回結構不完整

**位置**: `backend/chat_router_goods_action.py:1980`

**日誌證據**:
```
WARNING  OpenAI client not available, using mock LLM mode
```

**原因**:
- Mock 模式下的 `llm_result` 缺少 `display_mode` 欄位
- 即使有預設值邏輯 `llm_result.get("display_mode", "text_only")`,但 Mock 返回的字典本身可能為空或不完整

**影響路徑**:
```python
# Line 2067
"display_mode": llm_result.get("display_mode", "text_only"),  # Mock 時 llm_result 可能不完整
```

---

### 問題 2: `_extract_selected_levels_from_text()` 函數匹配失效

**位置**: `backend/chat_router_goods_action.py:1455-1570`

**原因分析**:

#### 2.1 L1 抽取失敗
正則表達式 `r"在\s*([\u4e00-\u9fffA-Za-z0-9 /&-]{1,20})\s*下"` 應該能匹配:
- ✅ "在常溫食品下" → 捕獲 "常溫食品"
- ✅ "在時尚女性下" → 捕獲 "時尚女性"

**但為何失敗?**
1. `l1_names` 列表可能為空 (分類快取未載入)
2. `_best_match(l1_names, "常溫食品")` 返回 `None` (匹配邏輯太嚴格)
3. `_normalize_text_for_match()` 可能改變了原始文字

#### 2.2 L2 關鍵字提示不足
`L2_HINTS_BY_L1` 字典缺少關鍵對應:
```python
# 缺少這些映射
"常溫食品": {
    "零食點心": ["零食", "點心"],  # ❌ 不存在
}
"時尚女性": {
    "女用皮包": ["包包", "背包"],  # ❌ 不存在
}
```

#### 2.3 層級推斷邏輯錯誤
當識別到 L2 (如 "零食點心") 時,`available_scope.level` 仍返回 `"L1"`:

**錯誤邏輯**:
```python
# 即使 selected["L2"] = "零食點心"
# 但返回時仍設置 level = "L1"
meta = {
    "available_scope": {
        "level": "L1",  # ❌ 應該是 "L2" 或 "L3"
    }
}
```

---

### 問題 3: 分類導覽查詢識別失效

**位置**: `backend/llm_service.py:793` (`_is_category_navigation_query`)

**排除邏輯過於激進**:
```python
product_indicators = [
    "包", "包包",  # ❌ 這會誤判 "女用背包分類有哪些" 為商品查詢
]
```

**結果**: 導覽查詢被誤判為商品查詢,觸發層級自動補全,干擾正常流程

---

## 🛠️ 修復方案

### 任務 1: 確保 `display_mode` 欄位始終存在 ⭐⭐⭐

**優先級**: P0  
**難度**: 低  
**預計時間**: 15 分鐘  
**影響測試**: 4 個

#### 修改位置 1.1: Mock LLM 返回結構

**檔案**: `backend/chat_router_goods_action.py`  
**行號**: ~1980 (Mock 模式處理)

```python
# 修改前
if not openai_client:
    logger.warning("OpenAI client not available, using mock LLM mode")
    mock_result = {...}  # 可能缺少 display_mode

# 修改後
if not openai_client:
    logger.warning("OpenAI client not available, using mock LLM mode")
    mock_result = {
        "intent": "information",
        "reply": mock_reply,
        "display_mode": "text_only",  # ✅ 明確設置
        "meta": {...},
        "overview": True,
    }
```

#### 修改位置 1.2: 資訊/概覽模式返回

**檔案**: `backend/chat_router_goods_action.py`  
**行號**: ~2060-2070

```python
# 確保所有返回路徑都有 display_mode
info_payload = {
    "ok": True,
    "reply": llm_result.get("reply", ""),
    "display_mode": llm_result.get("display_mode") or "text_only",  # ✅ 雙重保險
    # ...
}
```

#### 修改位置 1.3: `_finalize_text_only_fallback` 函數

**檔案**: `backend/chat_router_goods_action.py`  
**行號**: 660-696

```python
# 已有正確設置,但需確認所有調用方
sanitized: Dict[str, Any] = {
    # ...
    "display_mode": "text_only",  # ✅ 已存在
}
```

**驗證方法**:
```bash
cd backend
pytest tests/test_category_navigation_parsing.py::test_parse_l1_navigation -v
pytest tests/test_chat_overview_scope.py::test_chat_category_navigation_l1_to_l2 -v
```

---

### 任務 2: 修復 `_extract_selected_levels_from_text()` 匹配邏輯 ⭐⭐

**優先級**: P1  
**難度**: 中  
**預計時間**: 45 分鐘  
**影響測試**: 2 個

#### 修改位置 2.1: 增強 L1 名稱匹配

**檔案**: `backend/chat_router_goods_action.py`  
**行號**: 1465-1490

```python
def _extract_selected_levels_from_text(text: str) -> Dict[str, Optional[str]]:
    selected = {"L1": None, "L2": None, "L3": None}
    raw = _normalize_text_for_match(text)
    
    # ✅ 強制重載分類快取
    try:
        categories_service.get_scope(level="L1", top_k=None, force=True)  # 添加 force=True
    except Exception as e:
        logger.warning(f"Failed to load L1 categories: {e}")
    
    taxonomy_index = categories_service.get_taxonomy_index(force=True)
    
    # ✅ 獲取 L1 名稱列表 (增強容錯)
    l1_names = []
    try:
        scope_result = _get_scope_names("L1", top_k=None)
        l1_names = scope_result.get("names") or []
    except Exception:
        pass
    
    if not l1_names:
        l1_names = [entry.get("name") for entry in taxonomy_index.get("l1", []) if entry.get("name")]
    
    # ✅ 添加調試日誌
    logger.debug(f"[Extract] L1 names count: {len(l1_names)}, query: {raw}")
    
    # ... 後續匹配邏輯
```

#### 修改位置 2.2: 降低 `_best_match()` 閾值

**檔案**: `backend/chat_router_goods_action.py`  
**行號**: 搜尋 `def _best_match`

```python
# 如果使用相似度匹配,降低閾值
def _best_match(candidates: List[str], query: str, threshold: float = 0.6) -> Optional[str]:
    # 修改前: threshold = 0.8
    # 修改後: threshold = 0.6  # ✅ 更寬鬆的匹配
    # ...
```

#### 修改位置 2.3: 擴充 `L2_HINTS_BY_L1` 提示表

**檔案**: `backend/chat_router_goods_action.py`  
**行號**: 搜尋 `L2_HINTS_BY_L1`

```python
L2_HINTS_BY_L1 = {
    "常溫食品": {
        "零食點心": ["零食", "點心", "餅乾", "糖果", "小食"],  # ✅ 新增
        "五穀/豆類/米麵/乾貨": ["豆包", "豆腐", "米類", "佐醬", "湯料", "乾貨"],  # ✅ 新增
        # ...
    },
    "時尚女性": {
        "女用皮包": ["包包", "皮包", "背包", "手提包", "肩背包"],  # ✅ 新增
        # ...
    },
    # 其他分類...
}
```

**驗證方法**:
```bash
pytest tests/test_category_navigation_parsing.py::test_extract_l3_from_slash_terms -v
pytest tests/test_category_navigation_parsing.py::test_extract_l2_from_fashion_bag_keywords -v
```

---

### 任務 3: 修復分類層級判斷邏輯 ⭐⭐

**優先級**: P1  
**難度**: 中  
**預計時間**: 30 分鐘  
**影響測試**: 2 個

#### 修改位置 3.1: 導覽回應層級判斷

**檔案**: `backend/chat_router_goods_action.py`  
**行號**: 1590-1640 (搜尋 `_try_overview_scope_reply` 相關邏輯)

```python
def _build_category_navigation_response(user_text: str, selected: Dict[str, Optional[str]]) -> Dict[str, Any]:
    """根據已識別的分類層級構建導覽回應"""
    
    # ✅ 智能判斷應返回的層級
    if selected.get("L3"):
        # 已到最深層,返回 L3 列表
        level = "L3"
        scope_data = _get_scope_names("L3", parent_l1=selected["L1"], parent_l2=selected["L2"])
        next_level = None
    elif selected.get("L2"):
        # 識別到 L2,返回其下的 L3 列表
        level = "L3"  # ✅ 注意:返回的是下一層
        scope_data = _get_scope_names("L3", parent_l1=selected["L1"], parent_l2=selected["L2"])
        next_level = "L3"
    elif selected.get("L1"):
        # 識別到 L1,返回其下的 L2 列表
        level = "L2"  # ✅ 注意:返回的是下一層
        scope_data = _get_scope_names("L2", parent_l1=selected["L1"])
        next_level = "L2"
    else:
        # 未識別任何層級,返回 L1 總覽
        level = "L1"
        scope_data = _get_scope_names("L1", top_k=8)
        next_level = "L1"
    
    reply_text = f"以下是可選的{level}分類：{'、'.join(scope_data.get('names', [])[:10])}"
    
    return {
        "ok": True,
        "reply": reply_text,
        "display_mode": "text_only",
        "meta": {
            "available_scope": {
                "level": level,  # ✅ 正確的層級
                level.lower(): scope_data.get("names", []),
                "more_count": scope_data.get("more_count", 0),
            },
            "category_context": {
                "selected": selected,
                "next_level": next_level,
            }
        },
        "action": {"type": "none"},
        "suggestion_ids": [],
    }
```

**驗證方法**:
```bash
pytest tests/test_category_navigation_parsing.py::test_parse_l2_navigation -v
pytest tests/test_chat_overview_scope.py::test_chat_category_navigation_specific_l2_l3 -v
```

---

### 任務 4: 優化 `_is_category_navigation_query()` 判斷邏輯 ⭐

**優先級**: P2  
**難度**: 低  
**預計時間**: 15 分鐘  
**影響**: 間接影響測試

#### 修改位置 4.1: 調整排除關鍵字

**檔案**: `backend/llm_service.py`  
**行號**: 793-840

```python
def _is_category_navigation_query(query: str) -> bool:
    """檢測查詢是否為純分類導覽"""
    if not query:
        return False
    
    normalized = query.lower()
    
    # ✅ 更精確的商品意圖檢測 (避免誤判)
    product_indicators = [
        "禮盒", "伴手", "醬油", "鞋", 
        "要買", "我要", "有賣", "想買", "購買",
        "醬", "xo", "豆腐乳", "米", "衣服", "褲子"
        # ❌ 移除 "包"、"包包" (這些可能出現在導覽查詢中)
    ]
    
    # ✅ 增加導覽意圖檢測 (優先權更高)
    navigation_strong_signals = [
        "分類有哪些", "還有哪些分類", "小分類", "品類",
        "類別有什麼", "種類有什麼"
    ]
    if any(signal in normalized for signal in navigation_strong_signals):
        return True  # ✅ 強導覽信號,立即返回 True
    
    # 檢查商品意圖
    if any(indicator in normalized for indicator in product_indicators):
        return False
    
    # 正則模式檢測
    # ... (原有邏輯)
```

---

## 📋 修復檢查清單

### Phase 1: 快速修復 (P0)
- [ ] 任務 1.1: Mock LLM 添加 `display_mode`
- [ ] 任務 1.2: 資訊模式添加預設值
- [ ] 執行測試: `pytest tests/test_category_navigation_parsing.py::test_parse_l1_navigation -v`
- [ ] 確認 4 個 `display_mode` 測試通過

### Phase 2: 核心邏輯修復 (P1)
- [ ] 任務 2.1: 強化 L1 名稱載入邏輯
- [ ] 任務 2.2: 調整 `_best_match()` 閾值
- [ ] 任務 2.3: 擴充 `L2_HINTS_BY_L1` 字典
- [ ] 執行測試: `pytest tests/test_category_navigation_parsing.py -v`
- [ ] 確認層級抽取測試通過

### Phase 3: 層級判斷優化 (P1)
- [ ] 任務 3.1: 實現 `_build_category_navigation_response()`
- [ ] 整合到現有導覽流程
- [ ] 執行測試: `pytest tests/test_chat_overview_scope.py -v`
- [ ] 確認層級判斷測試通過

### Phase 4: 細節優化 (P2)
- [ ] 任務 4.1: 優化 `_is_category_navigation_query()`
- [ ] 添加調試日誌
- [ ] 執行完整測試: `pytest tests/ -q`
- [ ] 確認所有 183 個測試通過

---

## 🧪 測試驗證計劃

### 單元測試
```bash
# 分類導覽解析測試
pytest tests/test_category_navigation_parsing.py -v

# 聊天概覽作用域測試
pytest tests/test_chat_overview_scope.py -v
```

### 回歸測試
```bash
# 完整測試套件
cd backend
pytest tests/ -v --tb=short

# 預期結果
# - 183 passed (原 177 + 修復的 6)
# - 6 skipped
# - 0 failed
```

### 手動驗證
```bash
# 啟動服務
cd backend
source .venv/bin/activate
uvicorn app:app --reload --port 8000

# 測試查詢
curl -X POST http://localhost:8000/api/chat \
  -H "Content-Type: application/json" \
  -d '{
    "message": "你們有什麼常溫食品的品類？",
    "history": []
  }' | jq '.display_mode, .meta.available_scope.level'

# 期望輸出
# "text_only"
# "L2"
```

---

## 📊 修復影響評估

### 風險評估
- **低風險**: 任務 1 (僅添加欄位)
- **中風險**: 任務 2、3 (修改核心匹配邏輯)
- **低風險**: 任務 4 (優化判斷邏輯)

### 回歸風險
- ✅ 所有修改都是**增強型**,不移除現有功能
- ✅ 修改範圍限定在分類導覽相關函數
- ⚠️ 需要驗證商品搜尋流程不受影響

### 性能影響
- ✅ 無性能負面影響
- ✅ `force=True` 參數僅在必要時使用

---

## 📝 修改記錄模板

修復完成後更新此區塊:

### 修改 1: [任務標題]
- **檔案**: `backend/xxx.py`
- **行號**: xxx-xxx
- **修改類型**: 新增/修改/刪除
- **影響測試**: [測試名稱]
- **驗證狀態**: ✅ 通過 / ❌ 失敗

---

## 🔗 相關文件

- 測試失敗原始報告: GitHub Actions CI Log
- 分類導覽設計文檔: `CATEGORY_EXAMPLES_OPTIMIZATION_REPORT.md`
- 欄位標準化指南: `欄位使用記錄_中英文對照_標準化指南.md`

---

## 📌 補充意見

1) **Mock 路徑覆蓋**  
   - 除 `backend/chat_router_goods_action.py` 的 mock 返回要帶 `display_mode`，也檢查其他 mock/fixture（如 `llm_service` mock）是否存在回傳 `None` 的隱藏路徑，一併補齊。

2) **導覽流程串接點**  
   - 任務 3 的 `_build_category_navigation_response` 需明確插入到現有導覽流程（如 `_try_overview_scope_reply` 或後續 action/router）且避免被前置 return 短路。可先畫流程圖標示「新函式接手」位置再實作。

3) **提示表維護成本**  
   - `L2_HINTS_BY_L1` 擴充後，考慮提供 fallback：由 taxonomy 子節點自動生成同義詞或加監控 log，避免新增分類時漏加提示。

4) **相似度閾值降到 0.6 的風險控管**  
   - 在 `_best_match` 調整後，建議 log top-3 候選與分數，並在測試覆蓋近似詞/多音字，觀察誤命中率。

5) **導覽 vs 商品意圖測試**  
   - `_is_category_navigation_query` 移除「包/包包」排除後，補充反例測試：  
     - 純商品但含導覽詞  
     - 純導覽但含商品詞  
   - 確保分流穩定，避免回歸。

6) **診斷用日誌**  
   - 增加「最終判斷層級 & 依據」的 log（如 `selected`, `level_decision`, `matched_tokens`），便於後續排查。

7) **邊界測試建議**  
   - L1/L2 名稱含空格或斜線（如「冷凍/冷藏 食品」）的匹配。  
   - Mock 與真實 LLM 路徑都驗證 `display_mode` 預設為 `text_only`。  
   - 混合導覽與商品意圖的句子，確認分流穩定。

---

## 👤 負責人

- **開發者**: [待分配]
- **審核者**: [待分配]
- **測試者**: [待分配]

---

**最後更新**: 2025年12月5日 17:30  
**文件版本**: v1.0
