# 🔍 新分類商品系統調整分析

## 📋 問題背景

**查詢**：「極致輕寓2房」  
**現象**：系統要求澄清意圖，顯示「想確認一下，您是在找特定類型的商品嗎？」  
**預期**：應該直接顯示 4 款磐鈺建設相關商品

---

## ✅ 資料確認

### 1. 商品資料 (VIEW_GOODS_enhanced.csv)
```csv
商品編號,商品名稱,大分類名稱,中分類名稱,小分類名稱
puB0012,入主科博館...磐鈺草間漫漫30坪極致2房輕寓,磐鈺建設,磐鈺草間漫漫,極致輕寓2房
puB0013,私藏科博館...磐鈺草間漫漫35坪寬域2房雙衛,磐鈺建設,磐鈺草間漫漫,極致輕寓2房
puB0014,剛剛好的幸福尺度...磐鈺草間漫漫45坪,磐鈺建設,磐鈺草間漫漫,極致輕寓2房
puB0015,科博綠海之上...磐鈺草間漫漫55坪,磐鈺建設,磐鈺草間漫漫,極致輕寓2房
```
✅ **資料完整**：4 筆商品都有正確的分類資訊

### 2. 分類定義 (goods_categories.csv)
```csv
L1,L2,L3,Enabled,DisplayOrder
磐鈺建設,磐鈺草間漫漫,極致輕寓2房,1,49
```
✅ **分類已註冊**：在分類系統中有登錄

---

## 🔴 根本原因分析

### **問題核心：LLM 的分類知識庫未更新**

系統使用 LLM 進行意圖分析時，**LLM 的 Prompt 中沒有「極致輕寓2房」的相關資訊**，導致：

1. **意圖分析階段** (`llm_analyze_query`)
   - LLM 收到查詢「極致輕寓2房」
   - 查找內建的分類範例 → **找不到**
   - 返回低置信度 (`hierarchy_confidence < 0.55`)

2. **澄清判斷階段** (`llm_clarify_or_confirm`)
   ```python
   if max_conf < 0.55:
       return {"type": "clarify", "message": DEFAULT_CHAT_CLARIFY_MESSAGE}
   ```
   - 因為置信度低，系統認為需要澄清
   - 顯示：「想確認一下，您是在找特定類型的商品嗎？」

### **對比：「醬油」為什麼可以成功？**

LLM Prompt 中有明確的醬油相關範例：
```
📦 常溫食品類:
  🧂 調味/醬料/醬菜:
    ⭐ 醬油/味噌/糖: 昆布醬油、黑豆蔭油、素蠔油...
```
✅ LLM 識別置信度高 (> 0.55) → 直接顯示商品

---

## 🛠️ 需要調整的程式碼

### **檔案：`backend/llm_service.py`**

**函數：`_build_category_hierarchy_prompt()` (line 580)**

#### 當前狀態：
```python
def _build_category_hierarchy_prompt() -> str:
    """
    根據 CSV 中的實際分類層級，動態構建 LLM 提示詞
    """
    return """
📦 常溫食品類:
  🥘 五穀/豆類/米麵/乾貨: ...
  🧂 調味/醬料/醬菜: ...

👜 包包配件類: ...
👟 戶外與運動用品類: ...
"""
```

#### ❌ **問題**：
- 分類範例是**硬編碼**在程式碼中
- 新增分類後，**沒有機制自動更新 LLM 的知識**

---

## 💡 解決方案

### **方案 1：手動更新 LLM Prompt（快速修復）**

**修改位置**：`backend/llm_service.py` line 580-680

**新增內容**：
```python
def _build_category_hierarchy_prompt() -> str:
    return """
📦 常溫食品類: ...
👜 包包配件類: ...
👟 戶外與運動用品類: ...

🏢 磐鈺建設類:  # ← 新增
  🏘️ 磐鈺草間漫漫:
    ⭐ 極致輕寓2房: 30坪D戶型、35坪寬域2房、45坪經典3房、55坪旗艦3房
    範例: "極致輕寓2房"、"草間漫漫30坪"、"科博館住宅"
  
  🏙️ 磐鈺雲華:
    • 光合雅寓3房: 科技豪宅、綠建築
  
  🏙️ 磐鈺雲詠:
    • 市景菁英3房: 市中心精華地段
    • 景觀大戶4房: 大坪數豪宅

【識別範例】
查詢: "極致輕寓2房"
✅ 正確識別:
{{
  "category_hierarchy": {{"L1": "磐鈺建設", "L2": "磐鈺草間漫漫", "L3": "極致輕寓2房"}},
  "confidence": {{"L1": 0.95, "L2": 0.90, "L3": 0.85}},
  "matching_keywords": ["極致輕寓", "2房"]
}}
"""
```

---

### **方案 2：動態載入分類（長期方案）**

**目標**：讓 LLM Prompt 自動從 `goods_categories.csv` 和 `VIEW_GOODS_enhanced.csv` 生成

**修改位置**：`backend/llm_service.py`

```python
def _build_category_hierarchy_prompt() -> str:
    """
    動態從資料庫/CSV 載入分類層級，自動生成 LLM 提示詞
    """
    from services.categories_service import get_categories_tree
    from services.catalog_service import snapshot
    
    # 載入分類樹狀結構
    categories = get_categories_tree()
    
    # 載入商品範例（每個分類取 2-3 個代表性商品名稱）
    products = snapshot(limit=1000)
    
    # 動態生成 Prompt
    prompt_sections = []
    
    for l1, l2_dict in categories.items():
        prompt_sections.append(f"\n📦 {l1}:")
        for l2, l3_list in l2_dict.items():
            prompt_sections.append(f"  🔹 {l2}:")
            for l3 in l3_list:
                # 找出該分類下的商品範例
                examples = [
                    p['商品名稱'][:30] 
                    for p in products 
                    if p.get('小分類名稱') == l3
                ][:3]  # 取前 3 個
                
                examples_str = "、".join(examples) if examples else "（無範例）"
                prompt_sections.append(f"    • {l3}: {examples_str}")
    
    return "\n".join(prompt_sections)
```

**優點**：
- ✅ 新增商品/分類後無需改程式碼
- ✅ LLM 總是使用最新的分類資訊
- ✅ 商品範例自動更新

**缺點**：
- ⚠️ 每次請求都需要讀取 CSV（可加快取優化）
- ⚠️ Prompt 變長可能超過 Token 限制

---

## 📝 完整調整清單

### **當新增商品/分類時，需要調整：**

| 步驟 | 檔案 | 動作 | 重啟服務 |
|------|------|------|---------|
| 1 | `data/VIEW_GOODS_enhanced.csv` | 新增商品記錄（含分類欄位） | ❌ |
| 2 | `backend/data/goods_categories.csv` | 新增分類定義 | ❌ |
| 3 | **`backend/llm_service.py`** | **更新 `_build_category_hierarchy_prompt()` 函數** | ✅ **必須** |
| 4 | - | 清除快取 (`POST /api/admin/clear-cache`) | ❌ |

---

## 🎯 建議實施步驟

### **立即修復（方案 1）**
1. 修改 `backend/llm_service.py` line 580-680
2. 在 Prompt 中新增磐鈺建設相關範例
3. 重啟服務
4. 測試「極致輕寓2房」查詢

**預估時間**：15 分鐘  
**測試指令**：
```bash
curl -X POST http://127.0.0.1:8000/api/chat \
  -H "Content-Type: application/json" \
  -d '{"message": "極致輕寓2房", "history": []}'
```

### **長期優化（方案 2）**
1. 實作 `get_categories_tree()` 函數
2. 改寫 `_build_category_hierarchy_prompt()` 為動態載入
3. 加入快取機制（TTL 5 分鐘）
4. 整合測試

**預估時間**：2-3 小時

---

## 🔍 驗證方法

### **測試案例 1：極致輕寓2房**
```bash
# 測試查詢
curl -X POST http://127.0.0.1:8000/api/chat \
  -H "Content-Type: application/json" \
  -d '{"message": "極致輕寓2房", "history": [], "session_id": "test123"}' \
  | python3 -m json.tool
```

**預期結果**：
```json
{
  "reply": "根據您的需求「極致輕寓2房」，我為您找到了 4 款相關商品...",
  "structured_products": [
    {"商品編號": "puB0012", "商品名稱": "...30坪極致2房輕寓..."},
    {"商品編號": "puB0013", ...},
    ...
  ]
}
```

### **測試案例 2：其他新分類**
- 測試「磐鈺雲華」
- 測試「光合雅寓3房」
- 測試「景觀大戶4房」

---

## 📊 總結

### **根本原因**
✅ 資料層面（CSV）完整  
❌ **LLM 知識層面缺失** ← 問題核心

### **關鍵發現**
「極致輕寓2房」查詢失敗 **不是因為搜尋功能問題**，而是因為：
1. LLM 意圖分析時找不到相關分類範例
2. 返回低置信度
3. 觸發澄清流程

### **修復重點**
更新 `backend/llm_service.py` 中的 `_build_category_hierarchy_prompt()` 函數，讓 LLM 認識新的商品分類。

---

## 🚀 行動建議

**建議採用：方案 1（立即修復）+ 方案 2（排入開發計畫）**

1. **現在**：手動更新 Prompt，讓「極致輕寓2房」可以搜尋
2. **未來**：實作動態載入機制，徹底解決新分類更新問題

這樣可以**快速修復當前問題**，同時**避免未來重複發生**。
