# 生產環境診斷指南

## 🎯 目的
在生產環境（Render）執行相同的診斷工具，與本地環境比對，找出造成查詢結果差異的根本原因。

---

## 📊 本地環境基準值（2025-11-25）

```
商品資料 Hash: 298fe674
商品總數: 953 筆
L3 分類數: 48 個
籃球鞋商品: 124 筆

程式碼 Hash:
- app.py: b75635d0
- goods_search_service.py: bfae5fcc
- categories_service.py: 43639d68
- index.html: 2a616179

配置檔案 Hash:
- column_definitions.json: cea01c97
- branding_config.json: 8ca824a2
```

---

## 🚀 方法一：透過 Render Shell 執行（推薦）

### 步驟 1：登入 Render Dashboard
1. 前往 https://dashboard.render.com
2. 選擇你的 Backend Service
3. 點擊 **Shell** 標籤

### 步驟 2：執行診斷工具
在 Render Shell 中貼上以下指令：

```bash
# 確認當前目錄
pwd

# 執行診斷工具
python diagnostic_environment_check.py

# 如果需要，先確認檔案存在
ls -la diagnostic_environment_check.py
```

### 步驟 3：複製輸出結果
- 將完整輸出複製到本地文字檔
- 特別注意 Hash 值和環境變數部分

---

## 🔧 方法二：透過 API 端點執行（備選）

如果 Render Shell 無法使用，可以建立臨時 API 端點：

### 新增診斷端點到 app.py

```python
@app.get("/api/admin/diagnostic")
async def admin_diagnostic(admin_token: str = None):
    """生產環境診斷端點（記得使用後刪除）"""
    if admin_token != os.getenv("ADMIN_TOKEN"):
        raise HTTPException(status_code=401, detail="Unauthorized")
    
    import subprocess
    result = subprocess.run(
        ["python", "diagnostic_environment_check.py"],
        capture_output=True,
        text=True,
        cwd="/opt/render/project/src/backend"
    )
    
    return {
        "stdout": result.stdout,
        "stderr": result.stderr,
        "returncode": result.returncode
    }
```

### 呼叫診斷端點

```bash
curl "https://你的服務網址.onrender.com/api/admin/diagnostic?admin_token=你的ADMIN_TOKEN"
```

---

## 🔍 方法三：檢查環境變數（最快速）

直接在 Render Dashboard 查看環境變數設定：

### 步驟：
1. Render Dashboard → 選擇服務
2. 點擊 **Environment** 標籤
3. 檢查以下關鍵變數：

```
重點檢查項目：
□ DEFAULT_PAGE_SIZE（預設分頁大小）
□ HOT_CATEGORY_PAGE_SIZE（熱門分類分頁大小）
□ DATA_PATH（資料檔案路徑）
□ USE_LLM_* 系列變數（LLM 功能開關）
```

---

## 📝 比對檢查清單

將生產環境結果與本地比對，逐項核對：

### 1. 資料檔案一致性
- [ ] Hash 值是否相同？（`298fe674`）
- [ ] 商品總數是否相同？（`953 筆`）
- [ ] L3 分類數是否相同？（`48 個`）
- [ ] 籃球鞋商品數是否相同？（`124 筆`）

### 2. 程式碼版本一致性
- [ ] app.py Hash 是否相同？（`b75635d0`）
- [ ] goods_search_service.py Hash 是否相同？（`bfae5fcc`）
- [ ] categories_service.py Hash 是否相同？（`43639d68`）
- [ ] index.html Hash 是否相同？（`2a616179`）

### 3. 環境變數差異
- [ ] DEFAULT_PAGE_SIZE 設定值？
- [ ] HOT_CATEGORY_PAGE_SIZE 設定值？
- [ ] 是否有本地沒有的環境變數？

### 4. 配置檔案差異
- [ ] column_definitions.json Hash 是否相同？（`cea01c97`）
- [ ] branding_config.json Hash 是否相同？（`8ca824a2`）

---

## 🎯 預期發現

根據症狀（生產端 6 筆 vs 本地 124 筆），最可能的原因：

### 可能性 1：環境變數設定 page_size
```bash
# 生產環境可能有設定
HOT_CATEGORY_PAGE_SIZE=6
# 或
DEFAULT_PAGE_SIZE=6
```

### 可能性 2：程式碼版本不同
- app.py 的 page_size 處理邏輯不同
- frontend/index.html 的 page_size 參數不同

### 可能性 3：資料檔案不同
- 生產環境的籃球鞋商品只有 6 筆
- 資料檔案未更新到最新版

---

## 💡 診斷後續動作

### 如果是環境變數差異：
1. 在本地 `.env.dev` 加入相同設定
2. 重新測試確認結果一致

### 如果是程式碼版本差異：
1. 確認 Git 是否有未推送的 commit
2. 執行 `git push` 觸發 Render 自動部署
3. 等待部署完成後重新診斷

### 如果是資料檔案差異：
1. 使用 `/api/admin/upload-csv` 上傳最新資料
2. 呼叫 `/api/admin/clear-cache` 清除快取
3. 重新測試查詢

---

## 🚨 注意事項

1. **診斷端點安全性**：如果使用方法二建立 API 端點，診斷完畢後立即刪除該端點
2. **ADMIN_TOKEN 保密**：不要在公開場合暴露 ADMIN_TOKEN
3. **生產環境操作**：在 Render Shell 執行指令時小心，避免修改檔案
4. **診斷時機**：建議在低流量時段執行，避免影響使用者

---

## 📞 需要協助？

如果診斷結果不清楚，請提供以下資訊：

1. 完整的診斷工具輸出
2. Render 環境變數截圖
3. 最近的部署記錄
4. Git commit hash（`git log -1 --oneline`）

---

**建立時間**：2025-11-25  
**工具版本**：diagnostic_environment_check.py  
**適用環境**：Render Cloud Platform
