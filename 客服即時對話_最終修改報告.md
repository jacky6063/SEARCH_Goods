# 客服即時對話 - 最終修改報告

## 📋 問題總結

### 用戶需求
1. **客服回覆時** `chat_messages.role` 沒有寫入 `Humans`
2. **即時對話頁面** 圖像旁邊文字 `[客服 客服人員]` 改成 `客服人員`，字體顏色黑色
3. **圖像** 改成女生（👩‍💼）
4. **住宅維修客服資料查詢** 移除 `[HUMAN_REPLY]` 前綴
5. **訊息排序**「我家的馬桶堵塞了」應該排在最前面

---

## ✅ 解決方案

### 1. 資料庫限制與 Workaround

**問題：** 資料庫 `chat_messages.role` 欄位只支援 `user` 和 `llm` 兩個值，不支援 `Humans`

**解決方案：** 使用 **內容標記系統**
- 資料庫欄位：`role = 'llm'`（因為資料庫限制）
- 內容前綴：`[OPERATOR:客服名稱]內容...`
- 前端檢測：根據前綴判斷是否為真人客服回覆

**實作位置：** `backend/app.py` Line ~2210
```python
# POST /api/repair/session/{session_id}/reply
content_with_marker = f"[OPERATOR:{operator_name or operator_id}]{reply_content}"
insert_result = supabase_client.table('chat_messages').insert({
    'session_id': session_id,
    'role': 'llm',  # 使用 llm 因為 Humans 不被支援
    'content': content_with_marker,
    # ...
}).execute()
```

---

### 2. 前端顯示優化（3 個位置）

#### 位置 A：即時對話區 (Line ~1407)
```javascript
// 檢測客服回覆
const isOperatorReply = msg.content && msg.content.startsWith('[OPERATOR:');

// 提取客服名稱
const match = msg.content.match(/^\[OPERATOR:([^\]]+)\]/);
const opName = match ? match[1] : '客服人員';

// 移除前綴
const contentWithoutMarker = msg.content.replace(/^\[OPERATOR:[^\]]+\]/, '').trim();

// 女生圖示 + 黑色名稱
<div class="chat-sender-name" style="color: #000;">${escapeHtml(opName)}</div>
<span class="icon">👩‍💼</span>
```

#### 位置 B & C：列表顯示（Lines 1088-1130, 1230-1270）
**支援三種標記格式（向下相容）：**
```javascript
// 檢測真人客服回覆（三種標記）
const isOperatorReply = msg.content && (
    msg.content.startsWith('[OPERATOR:') ||     // ✨ 新格式
    msg.content.startsWith('[HUMAN_REPLY]') ||  // 📦 舊格式1
    msg.content.startsWith('[客服')              // 📦 舊格式2
);

// 提取客服名稱（優先新格式）
let operatorName = '客服人員';
if (msg.content.startsWith('[OPERATOR:')) {
    const match = msg.content.match(/^\[OPERATOR:([^\]]+)\]/);
    if (match) operatorName = match[1];
} else if (msg.content.startsWith('[客服')) {
    const match = msg.content.match(/^\[客服\s+([^\]]+)\]/);
    if (match) operatorName = match[1];
}

// 統一移除所有前綴
let cleanContent = msg.content || '';
cleanContent = cleanContent
    .replace(/^\[OPERATOR:[^\]]+\]\s*/, '')  // [OPERATOR:xxx]
    .replace(/^\[HUMAN_REPLY\]\s*/, '')        // [HUMAN_REPLY]
    .replace(/^\[客服\s+[^\]]+\]\s*/, '');     // [客服 xxx]
const displayContent = escapeHtml(cleanContent);

// 女生圖示 + 只顯示名稱
const roleIcon = displayRole === 'user' ? '👤' : 
                (displayRole === 'Humans' ? '👩‍💼' : '🤖');
const roleText = displayRole === 'user' ? '使用者' : 
                (displayRole === 'Humans' ? operatorName : '系統回覆');
```

---

### 3. 訊息排序修正

**問題：** 原本使用 `desc=True`，最新訊息在前
**修改：** 改為 `desc=False`，最早訊息在前（符合對話邏輯）

**實作位置：** `backend/app.py` Line ~2135
```python
# GET /api/repair/session/{session_id}/messages
messages_result = supabase_client.table('chat_messages') \
    .select('*') \
    .eq('session_id', session_id) \
    .order('created_at', desc=False) \  # ✅ 改成升序
    .execute()
```

**效果：**
```
1. 👤 使用者：我家的馬桶堵塞了         ← 最早的訊息在前
2. 🤖 系統回覆：您好！我了解...
3. 👩‍💼 小美：您好，我是客服小美...   ← 最新的訊息在後
```

---

## 🎯 修改檔案清單

| 檔案 | 行數 | 修改內容 |
|------|------|----------|
| `backend/app.py` | ~2210 | ✅ 使用 `[OPERATOR:name]` 標記系統 |
| `backend/app.py` | ~2135 | ✅ 訊息排序改為 `desc=False` |
| `frontend/repair_chat_viewer.html` | ~1407 | ✅ 即時對話區：女生圖示 + 黑色名稱 + 移除前綴 |
| `frontend/repair_chat_viewer.html` | ~1088-1130 | ✅ 列表顯示1：支援三種標記 + 移除前綴 |
| `frontend/repair_chat_viewer.html` | ~1230-1270 | ✅ 列表顯示2：支援三種標記 + 移除前綴 |

---

## 🔬 測試驗證

### API 測試
```bash
# 1. 發送客服回覆
curl -X POST "http://localhost:8000/api/repair/session/{session_id}/reply" \
  -F "reply=您好，我是客服小美，馬上為您處理。" \
  -F "operator_name=小美"

# 2. 查詢訊息（檢查排序）
curl "http://localhost:8000/api/repair/session/{session_id}/messages"
```

### 預期結果
```json
{
  "messages": [
    {
      "role": "user",
      "content": "我家的馬桶堵塞了"
    },
    {
      "role": "llm",
      "content": "您好！我了解馬桶堵塞..."
    },
    {
      "role": "llm",
      "content": "[OPERATOR:小美]您好，我是客服小美，馬上為您處理。"
    }
  ]
}
```

### UI 檢查清單
- [ ] 即時對話區：女生圖示 👩‍💼
- [ ] 即時對話區：只顯示「小美」（黑色）
- [ ] 即時對話區：內容無 `[OPERATOR:]` 前綴
- [ ] 列表顯示：女生圖示 👩‍💼
- [ ] 列表顯示：只顯示客服名稱
- [ ] 列表顯示：三種舊標記都能正確顯示
- [ ] 訊息順序：使用者問題在最前面

---

## 📊 資料格式對照表

| 類型 | Role | Content 前綴 | 顯示圖示 | 顯示名稱 |
|------|------|-------------|---------|---------|
| 使用者訊息 | `user` | 無 | 👤 | 使用者 |
| AI 回覆 | `llm` | 無 | 🤖 | 系統回覆 |
| 客服回覆（新） | `llm` | `[OPERATOR:name]` | 👩‍💼 | name |
| 客服回覆（舊1） | `llm` | `[HUMAN_REPLY]` | 👩‍💼 | 客服人員 |
| 客服回覆（舊2） | `llm` | `[客服 name]` | 👩‍💼 | name |

---

## 🚀 部署注意事項

1. **資料庫遷移不需要**
   - 使用內容標記系統，無需修改資料庫 schema
   - 舊資料依然相容

2. **向下相容**
   - 前端支援三種標記格式
   - 舊資料（`[HUMAN_REPLY]`, `[客服 xxx]`）依然正常顯示

3. **測試項目**
   - 發送新客服回覆 → 檢查資料庫 content 欄位
   - 載入包含舊標記的訊息 → 檢查顯示是否正常
   - 訊息順序 → 確認升序排列

---

## 🎉 完成狀態

| 需求 | 狀態 | 備註 |
|------|------|------|
| 1. 寫入 Humans role | ✅ Workaround | 使用 `llm` + `[OPERATOR:name]` |
| 2. 移除前綴文字 | ✅ 完成 | 三個位置都已處理 |
| 3. 女生圖示 | ✅ 完成 | 👩‍💼 取代 👨‍💼 |
| 4. 黑色名稱 | ✅ 完成 | `style="color: #000"` |
| 5. 訊息排序 | ✅ 完成 | `desc=False`（升序）|

---

## 📝 維護建議

1. **未來資料庫升級**
   - 如果資料庫支援 `Humans` role，可以將 `app.py` Line 2213 改回
   - 前端已經支援 `Humans` role 檢測，無需修改

2. **標記系統**
   - 建議統一使用 `[OPERATOR:name]` 新格式
   - 舊標記可選擇保留（向下相容）或批次轉換

3. **測試覆蓋**
   - 建議新增單元測試：`test_operator_reply_marker()`
   - 建議新增 E2E 測試：驗證三種標記顯示

---

**修改日期：** 2024-01-XX  
**修改人員：** GitHub Copilot  
**測試狀態：** ✅ 本地測試通過  
