# 🏛️ 後續開發原則 & 檢查清單

**文檔版本**：1.0  
**生效日期**：2025年11月8日  
**適用範圍**：所有新增功能、優化和維護  
**維護者**：GitHub Copilot & 開發團隊  

---

## 📌 核心承諾

> **後續的所有設計、優化和新增流程都將遵循統一的欄位標準原則**

本文檔為所有開發人員的行為指南，確保：
- ✅ 代碼一致性
- ✅ 可維護性
- ✅ 可擴展性
- ✅ 數據完整性

---

## 🎯 開發原則

### 原則 1：優先使用 FieldAccessor

**適用場景**：所有欄位訪問

```python
# ✅ 新增功能必須遵循
from field_utils import FieldAccessor

# 讀取欄位
name = FieldAccessor.get_name(item)
price = FieldAccessor.get_price(item)
category = FieldAccessor.get_category_l3(item)

# ❌ 禁止
name = item.get("商品名稱")
price = item["Price"]
```

**為什麼**：
- 自動支援多種別名
- 自動容錯和類型轉換
- CSV 欄位改名時無需改代碼

**責任**：代碼審查者必須檢查

---

### 原則 2：新欄位必須先加入配置

**流程**：

```
新增欄位需求
   ↓
1️⃣ 更新 column_definitions.json
   ├─ 添加標準欄位名
   ├─ 添加所有別名（中英文）
   └─ 新增範例
   ↓
2️⃣ 更新 field_utils.py (FieldAccessor)
   ├─ 更新 FIELD_MAPPINGS
   ├─ 新增 getter 方法
   └─ 添加文檔
   ↓
3️⃣ 更新相關搜尋層（如需要）
   ├─ goods_search_service.py
   └─ 任何特殊邏輯
   ↓
4️⃣ 更新對照表文檔
   ├─ 欄位使用記錄_中英文對照_標準化指南.md
   ├─ 欄位_快速參考表.md
   └─ 本文檔
   ↓
5️⃣ 提交 Pull Request
   ├─ 附加配置變更清單
   ├─ 測試所有別名
   └─ 代碼審查通過
```

**檢查清單**：

```
□ column_definitions.json 已更新
□ field_utils.py 已更新
□ 新 getter 方法已添加
□ 文檔已更新
□ 單元測試涵蓋新欄位
□ 別名測試通過（至少 3 個別名）
□ 向後兼容性已驗證
```

---

### 原則 3：所有查詢邏輯必須使用映射

**適用場景**：篩選、排序、分組

```python
# ❌ 舊方式（禁止）
df[df["商品名稱"].str.contains("米")]
df[df["Price"] > 100]

# ✅ 新方式（必須）
from goods_search_service import get_all_column_variants

# 分類篩選
l3_variants = get_all_column_variants("L3")
condition = df[l3_variants].apply(lambda row: row.isin(["米類"]).any(), axis=1)

# 數值比較
from goods_search_service import NUMERIC_FIELD_ALIASES
price_columns = NUMERIC_FIELD_ALIASES["Price"]
```

**為什麼**：
- 自動支援別名
- 防止數據漏掉
- CSV 結構變更時自動適配

---

### 原則 4：API 回應統一格式

**標準回應格式**（所有端點）：

```python
# ✅ 必須使用統一格式
{
    "商品編號": "...",        # 使用中文欄位名
    "商品名稱": "...",
    "商品描述": "...",
    "商品價格": "...",
    "商品特價": "...",
    "商品購物網址": "...",
    "商品圖片網址": "...",
    "品牌": "...",
    "規格": "...",
    "L1_分類": "...",
    "L2_分類": "...",
    "L3_分類": "..."
}

# ❌ 禁止
{
    "GoodIden": "...",        # 不使用英文欄位名
    "Name": "...",
    "Price": "..."
}
```

**實現方式**：

```python
from goods_search_service import format_for_chat

# 所有搜尋結果都用這個方法格式化
formatted_items = format_for_chat(records, slim_mode=False)
```

---

### 原則 5：新功能必須考慮多語言

**規則**：

```
新增功能/欄位
   ├─ 中文名稱（必須）
   ├─ 英文名稱（必須）
   ├─ 其他語言別名（推薦）
   └─ 文檔同步更新
```

**示例**：

```python
# ✅ 新增「商品評分」欄位時

# 1. column_definitions.json
{
  "Rating": ["商品評分", "rating", "product_rating", "分數"]
}

# 2. field_utils.py
def get_rating(self, item: Dict[str, Any]) -> Optional[float]:
    """取得商品評分 (0-5)"""
    return self.get_field(item, "rating")

# 3. goods_search_service.py
# 如果用於排序需要加入
NUMERIC_FIELD_ALIASES["Rating"] = ["商品評分", "rating", "score"]

# 4. 文檔
# 更新所有對照表文檔
```

---

### 原則 6：版本控制和文檔同步

**每次提交必須**：

```
git commit -m "功能/優化：簡要說明

修改內容：
- code 文件: 具體改動
- column_definitions.json: 新增/更新欄位映射
- 文檔: 欄位使用記錄_中英文對照_標準化指南.md

檢查清單：
- [x] FieldAccessor 使用規範
- [x] 欄位配置已更新
- [x] 別名測試通過
- [x] 文檔已同步
- [x] 向後兼容性驗證
"
```

---

### 原則 7：富內容與網址必須可操作

**範圍**：所有聊天輸出、公司資訊、活動行銷、FAQ 等含有網址／電話／地圖／影片的場景。

**實作要求**：

1. **後端**
   - 任何可操作資訊都必須填入 `rich_content.items`（`type`, `label`, `value`, `icon`, `action`, `action_label`）。
   - 純文字內容仍需保留原始 URL（供前端 fallback 轉換）。
2. **前端**
   - `appendChatBubble` 僅能使用 `<a>` 或具 `href` 的元素輸出連結，需設定 `target="_blank"` 及 `rel="noopener noreferrer"`。
   - `formatAssistantMessage` 必須將一般 `https://` 片段轉成 `<a>`，並保有既有的「購物連結」自訂格式。
   - 禁止只顯示純文字網址。
3. **測試**
   - PR 需附截圖或影片證明頁面上的網址可點擊。
   - 至少新增一個自動化驗證（Playwright / DOM parser / pytest）檢查 API 回傳含 `rich_content`，且渲染結果存在 `<a>`。

**檢查清單**：

```
□ API payload 是否包含 rich_content?
□ 純文字 URL 會被 formatAssistantMessage 轉成 <a> 嗎？
□ 電話/地址/影片按鈕是否有 action、action_label？
□ target / rel 屬性設定是否安全？
□ 測試或截圖證據是否附上？
```

---

## 🔍 新增功能檢查清單

### A. 功能設計階段

```
□ 欄位定義是否明確？
  └─ 英文名、中文名、別名、資料類型、長度限制
  
□ 是否涉及新欄位？
  └─ 需要先在 column_definitions.json 中定義
  
□ 是否涉及新查詢類型？
  └─ 需要在 goods_search_service.py 中配置映射
  
□ 是否需要新的 API 端點？
  └─ 回應格式必須遵循統一標準
  
□ 是否需要修改 CSV 結構？
  └─ 需要向下兼容（支援舊欄位名）
```

### B. 開發階段

```
□ 是否使用了 FieldAccessor？
  └─ 所有欄位訪問都用 FieldAccessor
  
□ 是否添加了欄位配置？
  └─ column_definitions.json 已更新
  └─ field_utils.py FIELD_MAPPINGS 已更新
  └─ 新增 getter 方法（如需要）
  
□ 是否考慮了別名？
  └─ 至少支援 3 個別名
  └─ 包括中英文混用
  
□ 錯誤處理是否完善？
  └─ 使用 FieldAccessor 的容錯機制
  └─ 沒有假設欄位存在
  
□ 代碼是否可讀？
  └─ 使用有意義的變數名
  └─ 添加了註解
```

### C. 測試階段

```
□ 單元測試涵蓋新欄位？
  └─ 測試主要別名
  └─ 測試空值情況
  └─ 測試類型轉換
  
□ 集成測試通過？
  └─ 與現有代碼相容
  └─ 數據完整性驗證
  
□ 性能測試通過？
  └─ 不影響查詢速度
  └─ 記憶體占用合理
  
□ 向後兼容測試通過？
  └─ 舊欄位名仍然工作
  └─ 舊 API 調用仍然工作
```

### D. 代碼審查階段

```
□ 是否遵循 FieldAccessor 原則？
□ 是否更新了配置文件？
□ 是否更新了文檔？
□ 是否有硬編碼欄位名？
□ 是否考慮了多語言？
□ 是否有足夠的註解？
□ 提交訊息是否清晰？
```

### E. 文檔更新階段

```
□ 欄位對照表已更新
  └─ 欄位使用記錄_中英文對照_標準化指南.md
  
□ 快速參考表已更新
  └─ 欄位_快速參考表.md
  
□ API 文檔已更新
  └─ 新 API 端點說明
  
□ 變更日誌已更新
  └─ CHANGELOG.md
  
□ 示例代碼已更新
  └─ README.md 示例
```

---

## 🚨 禁止事項清單

### 絕對禁止

```
❌ 1. 硬編碼欄位名
   row.get("商品名稱")
   row["Price"]
   
❌ 2. 假設欄位存在
   int(row["Price"])  # 可能拋異常
   
❌ 3. 混用中英文欄位名
   if "Name" in row or "商品名稱" in row
   
❌ 4. 忽視別名
   df[df["Price"] == 100]  # 漏掉 "售價"
   
❌ 5. 新欄位未加入配置
   item.get("新欄位名")
   
❌ 6. 直接修改 CSV 結構
   新增欄位前必須通知團隊
   
❌ 7. 不做向後兼容測試
   直接替換舊欄位名
   
❌ 8. 跳過文檔更新
   配置變更必須更新文檔
```

---

## ✅ 必須做的事項清單

### 每次開發前

```
✅ 1. 複習《欄位使用記錄_中英文對照_標準化指南.md》
✅ 2. 複習《欄位_快速參考表.md》
✅ 3. 查看《欄位_快速參考表.md》的使用示例
✅ 4. 準備功能檢查清單
```

### 每次開發中

```
✅ 1. 使用 FieldAccessor 訪問所有欄位
✅ 2. 在配置檔案中宣告新欄位
✅ 3. 編寫容錯代碼
✅ 4. 測試多個別名
✅ 5. 添加充分的註解
```

### 每次提交前

```
✅ 1. 檢查是否有硬編碼欄位名
✅ 2. 運行本地測試
✅ 3. 驗證向後兼容性
✅ 4. 更新相關文檔
✅ 5. 準備清晰的提交訊息
```

---

## 📊 優化流程原則

### 優化項目評估矩陣

| 優化項目 | 涉及欄位變更 | 需要配置更新 | 需要文檔更新 | 典型工作量 |
|--------|-----------|-----------|-----------|---------|
| **性能優化** | 否 | 否 | 可選 | 低 |
| **代碼重構** | 否 | 否 | 否 | 低 |
| **新增欄位** | 是 | 是 | 是 | 中 |
| **修改欄位** | 是 | 是 | 是 | 高 |
| **新增功能** | 可能 | 可能 | 是 | 中-高 |
| **新增 API** | 可能 | 否 | 是 | 中 |

### 優化檢查清單（簡化版）

```
【性能優化】
✅ 不修改欄位結構
✅ 更新性能指標文檔
✅ 驗證查詢結果一致

【代碼重構】
✅ 使用 FieldAccessor 替換硬編碼
✅ 保持 API 相容
✅ 更新代碼示例文檔

【新增功能】
✅ 評估欄位需求
✅ 更新配置檔案
✅ 完整的檢查清單（見 A-E 部分）
```

---

## 🎓 開發者培訓檢查清單

### 新開發者入職必讀

```
□ 閱讀《欄位使用記錄_中英文對照_標準化指南.md》
   時間：30 分鐘
   
□ 閱讀《欄位_快速參考表.md》
   時間：5 分鐘
   
□ 查看 FieldAccessor 範例代碼
   文件：backend/field_utils.py
   時間：15 分鐘
   
□ 查看 column_definitions.json
   文件：backend/column_definitions.json
   時間：10 分鐘
   
□ 實踐：修改一個簡單功能
   任務：新增一個 getter 方法
   時間：30 分鐘
   
□ 代碼審查：審查他人代碼
   時間：30 分鐘
```

**培訓完成標誌**：
- ✅ 能解釋欄位標準化的必要性
- ✅ 能正確使用 FieldAccessor
- ✅ 能識別代碼中的非標準欄位使用
- ✅ 能正確添加新欄位配置

---

## 📈 遵循情況監測

### 自動檢查工具

```bash
# 掃描非標準欄位使用
python backend/validate_field_standardization.py

# 檢查配置完整性
python backend/validate_column_definitions.py

# 測試所有別名
pytest backend/tests/test_field_accessor.py
```

### 定期審查

```
【每週】
- 檢查新提交是否遵循原則
- 更新遵循率統計

【每月】
- 審查本月所有新功能
- 記錄不規範項目
- 討論改進措施

【每季度】
- 更新原則文檔（如需要）
- 總結最佳實踐
- 規劃下一步改進
```

---

## 🔄 版本升級路線圖

### Phase 1（現在 - 2 週）
```
目標：統一認識和標準建立
□ 所有開發者複習文檔
□ 新代碼必須遵循原則
□ 舊代碼標記為「待遷移」
```

### Phase 2（2-4 週）
```
目標：遷移現有代碼
□ 逐個模組遷移到 FieldAccessor
□ 運行全面測試
□ 更新文檔
```

### Phase 3（4-8 週）
```
目標：CI/CD 自動檢查
□ 實現自動掃描
□ 提交前檢查
□ Pull Request 檢查
```

### Phase 4（8+ 週）
```
目標：完全規範化
□ 100% FieldAccessor 使用率
□ ORM/Schema 驗證（可選）
□ 外部 API 規範化
```

---

## 📞 常見問題

### Q: 現有代碼不符合原則怎麼辦？

**A**：分兩類處理：
- **關鍵路徑**：立即遷移（如 search_products）
- **舊功能**：逐步遷移（如 admin 端點）

標記為 `# TODO: Migrate to FieldAccessor` 供追蹤

### Q: 遺漏了新增欄位到配置文檔怎麼辦？

**A**：代碼審查會攔截。如果漏掉：
1. 立即補充配置
2. 更新文檔
3. 補充測試
4. 重新審查

### Q: 如何判斷是否需要新增欄位？

**A**：
- ❓ CSV 新增了列嗎？→ 需要
- ❓ 前端需要顯示新數據嗎？→ 需要
- ❓ API 需要返回新欄位嗎？→ 需要
- ❓ 需要過濾或排序嗎？→ 需要

### Q: FieldAccessor 會影響性能嗎？

**A**：基本沒有。FieldAccessor 的操作都是 O(1)：
- 字典查找：O(1)
- 字符串操作：通常 < 1ms
- 類型轉換：通常 < 1ms

### Q: 可以在 FieldAccessor 之外存取欄位嗎？

**A**：只有特定場景允許：
- **允許**：內部中間轉換（添加註解）
- **允許**：性能關鍵路徑（需要 profiling 證明）
- **禁止**：所有其他情況

必須加註解說明原因。

---

## 📋 簽約協議

**我理解並同意遵循上述開發原則**

```
□ 新增功能前更新配置文件
□ 所有欄位訪問使用 FieldAccessor
□ 測試所有欄位別名
□ 提交前更新文檔
□ 接受代碼審查
□ 遵循檢查清單
□ 參與團隊培訓
```

---

**文檔版本控制**

| 版本 | 日期 | 改動 |
|------|------|------|
| 1.0 | 2025-11-08 | 初始版本，建立完整的開發原則和檢查清單 |

**下一次審查**：2025年11月15日
