# 📤 快速上傳指南

## 問題已解決 ✅

你無法上傳 CSV 的原因是**管理端點需要認證**。我已為你配置好開發模式。

---

## 🚀 現在可以直接上傳！

### 3 個簡單步驟

1. **點擊管理版面**
   - 前端右下角 "⚙️ 管理版面" 按鈕

2. **選擇 CSV 檔案**
   - 點擊 "選擇檔案"
   - 選擇 `VIEW_GOODS_enhanced.csv`

3. **上傳**
   - 點擊 "上傳 CSV" 按鈕
   - 等待 "上傳成功" 提示

✅ **完成！** 新商品數據已更新

---

## ⚙️ 我為你配置了什麼？

### 更改的檔案

| 檔案 | 改動 |
|------|------|
| `backend/.env` | 添加 `ALLOW_DEV_ADMIN=1` |
| `ADMIN_GUIDE.md` | 新增完整使用指南 |
| `CSV_UPLOAD_SOLUTION.md` | 新增解決方案文檔 |

### 啟用的功能

- ✅ 不需要 token 認證
- ✅ 直接上傳 CSV 檔案
- ✅ 自動更新到 `data/` 目錄
- ✅ 自動清除快取

---

## 📚 詳細文檔

- **新手**: 查看 `ADMIN_GUIDE.md` (完整使用指南)
- **技術**: 查看 `CSV_UPLOAD_SOLUTION.md` (技術細節)
- **後端代碼**: `backend/app.py` (731 行，upload-csv 端點)

---

## 🔐 生產部署提醒

開發模式 (`ALLOW_DEV_ADMIN=1`) **只在本地開發使用**。

部署到生產前，請修改 `.env`：

```env
# 關閉開發模式
ALLOW_DEV_ADMIN=0

# 設置強隨機 token
ADMIN_TOKEN=your_very_secure_random_token_here
```

---

## 💡 故障排查

| 問題 | 解決方案 |
|------|---------|
| 上傳失敗 | 重啟後端服務 |
| 數據未更新 | 刷新頁面或點擊 "清除快取" |
| 檔案未找到 | 檢查 CSV 編碼是 UTF-8 |

---

**已完成時間**: 2025年11月4日  
**GitHub**: 最新代碼已推送
