# 🚨 住宅維修模組雲端未啟用診斷報告

**問題描述**: 測試查詢「馬桶嚴重堵塞」在地端正常運作，但雲端環境未觸發住宅維修模組

---

## 🔍 問題診斷

### 症狀
- ✅ **地端測試**: 正常識別為維修需求，返回維修建議
- ❌ **雲端測試**: 未識別住宅維修意圖，當作一般商品搜尋處理

### 根本原因
```python
# backend/app.py Line 98
ENABLE_REPAIR_SERVICE = os.getenv("ENABLE_REPAIR_SERVICE", "False").lower() in ("1", "true", "yes")
```

**雲端 Render 環境缺少環境變數 `ENABLE_REPAIR_SERVICE=True`**，導致：
1. 維修服務模組未載入
2. `/api/repair/chat` 端點未註冊
3. 前端路由判斷無法觸發住宅維修流程

---

## ✅ 解決方案

### Step 1: 在 Render Dashboard 設定環境變數

登入 Render Dashboard → 選擇後端服務 → Environment 頁籤 → 新增：

```bash
ENABLE_REPAIR_SERVICE=True
```

### Step 2: 確認相關環境變數齊全

同時檢查以下環境變數是否已設定（維修服務依賴項）：

```bash
# 必需
ENABLE_REPAIR_SERVICE=True
OPENAI_API_KEY=sk-xxxxx (真實 API key)

# 選填（有預設值）
REPAIR_DATA_PATH=data/集合式住宅報修資料.csv  # 預設路徑
REPAIR_USE_LLM=True                           # 預設啟用
REPAIR_OPENAI_MODEL=gpt-4o-mini              # 預設模型
```

### Step 3: 觸發重新部署

1. 手動觸發：在 Render Dashboard 點擊 **Manual Deploy** → **Clear build cache & deploy**
2. 或推送任意 commit 觸發自動部署

### Step 4: 驗證部署成功

部署完成後，執行測試：

```bash
# 測試 1: 檢查環境變數
curl https://your-backend.onrender.com/api/health

# 測試 2: 維修查詢
curl -X POST https://your-backend.onrender.com/api/repair/chat \
  -H "Content-Type: application/json" \
  -d '{"message": "馬桶嚴重堵塞", "history": []}'

# 預期回應包含：
# - reply: "...疏通...管道..."
# - repairs: [{...維修項目...}]
```

---

## 🔎 診斷檢查清單

### 地端檢查（已通過）
- ✅ `.env` 檔案包含 `ENABLE_REPAIR_SERVICE=True`
- ✅ `repair_search_service.py` 正常載入
- ✅ `/api/repair/chat` 端點可訪問
- ✅ 測試查詢返回維修建議

### 雲端檢查（待修復）
- ❌ **Render 環境變數未設定** ← 核心問題
- ⏳ 重新部署後確認
- ⏳ `/api/repair/chat` 端點可用性測試
- ⏳ 功能驗證測試

---

## 📊 環境對比

| 項目 | 地端 | 雲端 Render |
|------|------|-------------|
| `ENABLE_REPAIR_SERVICE` | ✅ `True` (`.env`) | ❌ **未設定** |
| 維修服務載入 | ✅ 正常 | ❌ 跳過 |
| `/api/repair/chat` | ✅ 可用 | ❌ 404 Not Found |
| 前端判斷邏輯 | ✅ 觸發 | ❌ 降級到商品搜尋 |

---

## 🎯 預期修復效果

修復後，當用戶輸入維修相關查詢時：

```
輸入: "馬桶嚴重堵塞"

前端判斷:
  ✅ 檢測到維修關鍵字 → 路由到 /api/repair/chat

後端處理:
  ✅ ENABLE_REPAIR_SERVICE=True
  ✅ 載入 repair_search_service
  ✅ 搜尋「集合式住宅報修資料.csv」
  ✅ 返回維修建議 (給排水 > 馬桶疏通)

前端顯示:
  ✅ 維修建議卡片
  ✅ 緊急程度標記
  ✅ 處理建議說明
```

---

## 📝 技術細節

### 環境變數載入順序
```python
# backend/app.py
load_dotenv()  # 載入 .env 文件

# 讀取環境變數（優先級：系統環境變數 > .env 文件）
ENABLE_REPAIR_SERVICE = os.getenv("ENABLE_REPAIR_SERVICE", "False")
```

### 條件性模組導入
```python
if ENABLE_REPAIR_SERVICE:
    try:
        from repair_search_service import search_repairs, ...
        from repair_llm_service import repair_chat_reply, ...
    except Exception as e:
        logger.error(f"Failed to import repair services: {e}")
        ENABLE_REPAIR_SERVICE = False  # 降級
```

### API 路由註冊
```python
# 只有當 ENABLE_REPAIR_SERVICE=True 時才註冊
if ENABLE_REPAIR_SERVICE:
    @app.post("/api/repair/chat")
    def repair_chat_endpoint(req: RepairChatReq):
        ...
```

---

## 🚀 執行修復

### 立即修復步驟

1. **登入 Render Dashboard**
   - URL: https://dashboard.render.com/
   - 選擇 SEARCH_Goods 後端服務

2. **設定環境變數**
   - 導航到 **Environment** 頁籤
   - 點擊 **Add Environment Variable**
   - 輸入:
     ```
     Key: ENABLE_REPAIR_SERVICE
     Value: True
     ```
   - 點擊 **Save Changes**

3. **觸發部署**
   - 方式 1: 點擊 **Manual Deploy** (立即生效)
   - 方式 2: 推送 commit 自動觸發

4. **驗證修復**
   ```bash
   # 測試維修查詢
   curl -X POST https://your-backend.onrender.com/api/repair/chat \
     -H "Content-Type: application/json" \
     -d '{
       "message": "馬桶嚴重堵塞怎麼辦",
       "history": [],
       "topn": 5
     }'
   ```

   預期回應：
   ```json
   {
     "reply": "您好！關於馬桶堵塞問題，這屬於【給排水】維修類別...",
     "repairs": [
       {
         "維修項目": "馬桶疏通",
         "維修類別": "給排水",
         "緊急程度": "中",
         ...
       }
     ],
     "session_id": "abc123",
     "meta": {...}
   }
   ```

---

## 📚 相關文件

- [維修服務完整文件](backend/REPAIR_SERVICE_README.md)
- [環境變數設定指南](.env.example)
- [部署檢查清單](DEPLOYMENT_CHECKLIST.md)

---

## ✅ 修復確認

完成以上步驟後，請確認：

- [ ] Render 環境變數已設定 `ENABLE_REPAIR_SERVICE=True`
- [ ] 服務重新部署成功
- [ ] `/api/repair/chat` 端點返回 200 (非 404)
- [ ] 測試查詢「馬桶堵塞」返回維修建議
- [ ] 前端顯示維修建議卡片（而非商品搜尋結果）

---

**問題識別時間**: 2025年11月11日  
**預計修復時間**: < 5 分鐘 (Render 環境變數設定 + 重新部署)  
**影響範圍**: 住宅維修功能（商品搜尋不受影響）
