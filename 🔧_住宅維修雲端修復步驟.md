# 🔧 住宅維修雲端快速修復

**問題**: 「馬桶嚴重堵塞」在雲端無法觸發維修模組  
**原因**: Render 環境變數 `ENABLE_REPAIR_SERVICE` 未設定  
**預計時間**: 5 分鐘

---

## ⚡ 立即修復（3 步驟）

### Step 1: 登入 Render Dashboard
```
🔗 https://dashboard.render.com/
→ 選擇 SEARCH_Goods 後端服務
```

### Step 2: 設定環境變數
```
點擊左側 "Environment" 頁籤
→ 點擊 "Add Environment Variable"
→ 輸入:
   Key: ENABLE_REPAIR_SERVICE
   Value: True
→ 點擊 "Save Changes"
```

### Step 3: 觸發部署
```
點擊 "Manual Deploy"
→ "Clear build cache & deploy"
→ 等待部署完成（約 2-3 分鐘）
```

---

## ✅ 驗證修復

### 方法 1: 瀏覽器測試（推薦）
```
1. 打開前端: https://your-frontend.netlify.app
2. 點擊「聊天模式」
3. 輸入: "馬桶嚴重堵塞"
4. 預期結果: 顯示維修建議卡片（帶 🔧 圖示）
```

### 方法 2: API 測試
```bash
curl -X POST https://your-backend.onrender.com/api/repair/chat \
  -H "Content-Type: application/json" \
  -d '{
    "message": "馬桶嚴重堵塞",
    "history": [],
    "topn": 5
  }'
```

**預期回應**:
```json
{
  "reply": "您好！關於馬桶堵塞問題...",
  "repairs": [
    {
      "維修項目": "馬桶疏通",
      "維修類別": "給排水",
      "緊急程度": "中",
      "處理建議": "請聯絡專業水電人員..."
    }
  ],
  "session_id": "xxx",
  "meta": {
    "intent": "repair",
    "keywords": ["馬桶", "堵塞"]
  }
}
```

---

## 🔍 檢查清單

修復前:
- ❌ 雲端環境變數 `ENABLE_REPAIR_SERVICE` 未設定
- ❌ `/api/repair/chat` 返回 404
- ❌ 前端無法路由到維修服務

修復後:
- ✅ 雲端環境變數 `ENABLE_REPAIR_SERVICE=True`
- ✅ `/api/repair/chat` 正常響應
- ✅ 前端正確路由到維修服務
- ✅ 顯示維修建議卡片

---

## 🎯 測試案例

執行以下測試確認完整功能:

1. **維修查詢** (應觸發維修服務)
   - "馬桶嚴重堵塞"
   - "水龍頭滴水怎麼辦"
   - "跳電維修"

2. **商品查詢** (應觸發商品搜尋)
   - "我要買椰子油"
   - "推薦堅果"
   - "有什麼特價商品"

3. **混合查詢** (意圖識別測試)
   - "維修工具" (維修意圖)
   - "修理膠帶" (商品意圖)

---

## 📊 前端意圖識別邏輯

```javascript
// frontend/index.html Line 1590+
function detectIntent(message) {
  // 維修關鍵字: 水龍頭、馬桶、堵塞、跳電...
  let repairScore = 0;
  REPAIR_KEYWORDS.forEach(keyword => {
    if (message.includes(keyword)) repairScore++;
  });
  
  // 商品關鍵字: 買、價格、推薦...
  let shoppingScore = 0;
  SHOPPING_KEYWORDS.forEach(keyword => {
    if (message.includes(keyword)) shoppingScore++;
  });
  
  // 維修分數更高 → 'repair'
  if (repairScore > shoppingScore && repairScore > 0) {
    return 'repair';
  }
  
  // 預設 → 'shopping'
  return 'shopping';
}
```

**測試案例結果**:
- "馬桶堵塞" → repairScore=2 (馬桶, 堵塞) → `repair` ✅
- "我要買馬桶" → shoppingScore=1 (買) > repairScore=1 → `shopping` ✅

---

## 🚨 常見問題

### Q1: 設定環境變數後還是 404?
**A**: 需要重新部署。點擊 "Manual Deploy" 或推送 commit。

### Q2: 顯示商品而非維修建議?
**A**: 檢查:
1. 前端是否使用最新版本 (包含意圖識別邏輯)
2. 瀏覽器 Console 確認 `detectIntent()` 的輸出
3. 確認 API 回應中 `repairs` 欄位有資料

### Q3: 地端正常雲端異常?
**A**: 典型的環境變數不一致問題。確認:
```bash
# 地端
cat backend/.env | grep ENABLE_REPAIR_SERVICE
# 應顯示: ENABLE_REPAIR_SERVICE=True

# 雲端
Render Dashboard → Environment → 確認有此變數
```

---

## 📝 技術原理

### 後端環境變數控制
```python
# backend/app.py Line 98
ENABLE_REPAIR_SERVICE = os.getenv("ENABLE_REPAIR_SERVICE", "False")

if ENABLE_REPAIR_SERVICE.lower() in ("1", "true", "yes"):
    # ✅ 載入維修模組
    from repair_search_service import search_repairs
    from repair_llm_service import repair_chat_reply
    
    # ✅ 註冊 /api/repair/chat 端點
    @app.post("/api/repair/chat")
    def repair_chat_endpoint(req):
        ...
else:
    # ❌ 跳過維修模組
    pass
```

### 前端路由決策
```javascript
// 1. 檢測意圖
const intent = detectIntent(message);
// → 'repair' 或 'shopping'

// 2. 路由到對應 API
if (intent === 'repair') {
  // POST /api/repair/chat
  const resp = await fetch('/api/repair/chat', {...});
  appendRepairResponse(resp.reply, resp.repairs);
} else {
  // POST /api/chat
  const resp = await fetch('/api/chat', {...});
  appendChatResponse(resp.reply, resp.products);
}
```

---

## 🎉 修復完成確認

完成以上步驟後，系統應該:

✅ **功能完整**
- 維修查詢觸發維修服務
- 商品查詢觸發商品搜尋
- 意圖自動識別準確

✅ **UI 正確**
- 維修回應顯示 🔧 圖示
- 維修建議卡片包含類別、緊急度、處理建議
- 商品回應顯示商品卡片

✅ **向後相容**
- 現有商品搜尋功能不受影響
- 未識別維修意圖時降級到商品搜尋

---

**建議執行時間**: 立即（5 分鐘內）  
**影響範圍**: 住宅維修功能啟用  
**風險評估**: 無風險（獨立模組，不影響現有功能）
