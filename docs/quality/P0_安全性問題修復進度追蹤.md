# P0 安全性問題修復進度追蹤

**建立日期**: 2025-11-13  
**問題編號**: P0-SUPABASE-001  
**問題描述**: `.env.example` 洩漏真實生產憑證到 GitHub

---

## ✅ 已完成步驟

### 1. 修復 `.env.example` 檔案 ✅
- **時間**: 2025-11-13 15:05
- **Commit**: `79cc120` - "security: Remove real credentials from .env.example (P0 fix)"
- **狀態**: ✅ 已推送到 GitHub (`origin/main`)

**變更內容**:
```diff
- SUPABASE_URL=https://jyflluhapfmbrqjlnjwy.supabase.co
- SUPABASE_KEY=eyJhbGci... (真實 token)
- SUPABASE_SERVICE_KEY=sb_secret_pl7tj5DkqlDQkXLpclgr6g_cmK74DDb
- DATABASE_URL=postgresql://postgres:<CWpY2nSj1IaZRmcC>@...

+ SUPABASE_URL=https://your-project-id.supabase.co
+ SUPABASE_KEY=your_anon_public_key_here
+ SUPABASE_SERVICE_KEY=your_service_role_secret_key_here
+ DATABASE_URL=postgresql://postgres:<YOUR_DATABASE_PASSWORD>@...
```

---

## 🔴 緊急待辦事項 (必須立即執行)

### 2. 更換 Supabase 憑證 ⚠️ **最高優先級**
- **風險**: 舊憑證已公開在 GitHub,可能被濫用
- **時限**: ⏰ **24 小時內完成**

#### 步驟 2.1: 更換 Service Role Key
```bash
# 登入 Supabase Dashboard
# https://supabase.com/dashboard/project/jyflluhapfmbrqjlnjwy

1. Settings → API
2. 找到 "Service Role Key" 區塊
3. 點擊 "Reset Service Role Key"
4. 複製新的 key 並儲存
```

**更新位置**:
- [ ] 本地 `.env` 檔案
- [ ] Render 環境變數 (`SUPABASE_SERVICE_KEY`)
- [ ] GitHub Actions Secrets (`SUPABASE_SERVICE_KEY`)

#### 步驟 2.2: 更換 Database Password
```bash
# Supabase Dashboard
1. Settings → Database
2. Database Settings → Reset Database Password
3. 輸入新密碼 (建議使用 32 位隨機密碼)
4. 更新 DATABASE_URL
```

**更新位置**:
- [ ] 本地 `.env` 檔案
- [ ] Render 環境變數 (`DATABASE_URL`)
- [ ] 任何使用直連 PostgreSQL 的腳本

#### 步驟 2.3: 更換 Anon Key (選用但建議)
```bash
# Supabase Dashboard
1. Settings → API
2. 找到 "Anon Key" → Reset Anon Key
3. 更新前端環境變數
```

**更新位置**:
- [ ] 本地 `.env` 檔案
- [ ] Netlify 環境變數 (`SUPABASE_KEY`)
- [ ] GitHub Actions Secrets (`SUPABASE_KEY`)

---

### 3. 驗證服務運作 ⚠️ **憑證更換後必須執行**

```bash
# 測試 Supabase 連線
cd /Users/huangchangchi/Documents/SEARCH_Goods
source backend/.venv/bin/activate
python scripts/supabase_db_test.py

# 預期輸出: [] 或包含資料的陣列
# 若報錯,表示憑證尚未正確更新
```

**檢查清單**:
- [ ] 本地開發環境正常
- [ ] Render 後端服務正常
- [ ] Netlify 前端服務正常
- [ ] GitHub Actions CI 通過

---

## 🟡 重要後續步驟 (1 週內完成)

### 4. 清理 Git 歷史記錄 🔒

**問題**: 即使修改了 `.env.example`,舊憑證仍存在於 Git 歷史中

#### 選項 A: 使用 BFG Repo-Cleaner (推薦)

```bash
# 1. 安裝 BFG (macOS)
brew install bfg

# 2. 建立替換規則檔案
cat > /tmp/supabase-secrets.txt << 'EOF'
eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Imp5ZmxsdWhhcGZtYnJxamxuand5Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjI5NTM5NjEsImV4cCI6MjA3ODUyOTk2MX0.nnAALXteChGTpjf8UWuh9mPIU-GPW9Slx_gAZGKTGx8===>REDACTED_ANON_KEY
sb_secret_pl7tj5DkqlDQkXLpclgr6g_cmK74DDb===>REDACTED_SERVICE_KEY
<CWpY2nSj1IaZRmcC>===>REDACTED_PASSWORD
jyflluhapfmbrqjlnjwy===>REDACTED_PROJECT_ID
EOF

# 3. 克隆 mirror 倉庫
cd ~/Documents
git clone --mirror https://github.com/jacky6063/SEARCH_Goods.git SEARCH_Goods-cleanup.git

# 4. 執行清理
cd SEARCH_Goods-cleanup.git
bfg --replace-text /tmp/supabase-secrets.txt

# 5. 清理並強制推送
git reflog expire --expire=now --all
git gc --prune=now --aggressive
git push --force

# 6. 通知團隊成員重新克隆倉庫
echo "⚠️ 所有團隊成員需要刪除本地倉庫並重新 git clone"
```

**⚠️ 注意**: 此操作會改寫 Git 歷史,所有團隊成員需要重新克隆倉庫!

#### 選項 B: 接受風險,依賴憑證更換

如果不方便執行 Git 歷史清理,確保:
- ✅ 已更換所有洩漏的憑證
- ✅ 監控 Supabase 存取日誌,偵測異常行為
- ✅ 設定 IP 白名單 (如果 Supabase 方案支援)

---

### 5. 實施預防措施 🛡️

#### 5.1 設定 Pre-commit Hooks

```bash
# 安裝 pre-commit
pip install pre-commit

# 創建配置檔
cat > .pre-commit-config.yaml << 'EOF'
repos:
  - repo: https://github.com/Yelp/detect-secrets
    rev: v1.4.0
    hooks:
      - id: detect-secrets
        args: ['--baseline', '.secrets.baseline']
        exclude: package-lock.json

  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v4.5.0
    hooks:
      - id: check-added-large-files
      - id: check-merge-conflict
      - id: trailing-whitespace
EOF

# 初始化
pre-commit install
pre-commit run --all-files

# 生成 baseline (排除誤報)
detect-secrets scan > .secrets.baseline
```

#### 5.2 啟用 GitHub Secret Scanning

1. 前往 GitHub 倉庫: https://github.com/jacky6063/SEARCH_Goods
2. **Settings** → **Code security and analysis**
3. 啟用:
   - ✅ **Secret scanning**
   - ✅ **Push protection** (阻止推送包含密鑰的提交)

#### 5.3 建立憑證管理文件

```bash
# 創建安全性文檔
mkdir -p docs/security
cat > docs/security/CREDENTIAL_MANAGEMENT.md << 'EOF'
# 憑證管理規範

## 原則
1. ❌ 絕不將真實憑證提交到版本控制
2. ✅ 使用環境變數管理敏感資訊
3. ✅ `.env` 必須加入 `.gitignore`
4. ✅ `.env.example` 只包含佔位符

## 檢查清單
- [ ] 提交前檢查是否包含敏感資訊
- [ ] 新增環境變數前確認是否為敏感資料
- [ ] 定期輪換 Service Role Key (每季一次)
- [ ] 監控 Supabase 存取日誌

## 緊急聯絡
- Supabase 支援: https://supabase.com/support
- 內部安全團隊: [填寫聯絡資訊]
EOF

git add docs/security/CREDENTIAL_MANAGEMENT.md
git commit -m "docs: Add credential management guidelines"
git push
```

---

## 📊 修復進度儀表板

| 步驟 | 狀態 | 完成時間 | 負責人 |
|------|------|---------|--------|
| 1. 修復 `.env.example` | ✅ 完成 | 2025-11-13 15:05 | 系統自動 |
| 2. 更換 Service Role Key | ⚪ 待執行 | - | [DevOps] |
| 3. 更換 Database Password | ⚪ 待執行 | - | [DevOps] |
| 4. 更新部署環境變數 | ⚪ 待執行 | - | [DevOps] |
| 5. 驗證服務運作 | ⚪ 待執行 | - | [QA] |
| 6. 清理 Git 歷史 | ⚪ 待執行 | - | [DevOps] |
| 7. 實施預防措施 | ⚪ 待執行 | - | [Dev Team] |

---

## 📈 風險等級變化

| 時間點 | 風險等級 | 說明 |
|--------|---------|------|
| 修復前 | 🔴 極高 | 真實憑證公開在 GitHub |
| **當前** | 🟠 **高** | `.env.example` 已修復,但舊憑證尚未更換 |
| 更換憑證後 | 🟡 中 | 舊憑證失效,但 Git 歷史仍有記錄 |
| 清理 Git 歷史後 | 🟢 低 | 完全移除敏感資訊 |

---

## 🎯 下一步行動

### 立即執行 (今天):
1. ⚠️ **更換 Supabase Service Role Key**
2. ⚠️ **更換 Database Password**
3. ⚠️ **更新所有部署環境的環境變數**
4. ⚠️ **驗證服務正常運作**

### 本週執行:
5. 🔒 **清理 Git 歷史記錄** (使用 BFG)
6. 🛡️ **設定 Pre-commit Hooks**
7. 📝 **建立憑證管理文件**

### 監控指標:
- 🔍 Supabase Dashboard → Logs → 檢查異常存取
- 🔍 GitHub → Insights → Traffic → 監控異常流量
- 🔍 Render → Logs → 檢查後端服務健康狀態

---

## 📞 緊急聯絡

如發現異常活動:
1. 立即在 Supabase Dashboard 停用專案
2. 聯絡 Supabase 支援: https://supabase.com/support
3. 檢查資料庫是否有未授權的變更

---

**最後更新**: 2025-11-13 15:05  
**下次檢查**: 2025-11-14 (24 小時後)
