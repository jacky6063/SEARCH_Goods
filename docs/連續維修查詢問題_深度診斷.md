# 連續維修查詢問題 - 深度診斷報告

> **問題日期：** 2025年11月13日  
> **測試結果：** 方案A實施後，問題**未解決**  
> **狀態：** 🔴 需要進一步修正

---

## 🔴 問題重現

### 實際測試結果

| 步驟 | 輸入 | 預期結果 | 實際結果 | 狀態 |
|-----|------|---------|---------|------|
| 1 | 瓦斯洩漏或鏽蝕 | 維修 | ✅ 維修 | 正確 |
| 2 | 公司地址 | 公司資訊 | ✅ 公司資訊 | 正確 |
| 3 | 瓦斯洩漏或鏽蝕 | 維修 | ✅ 維修 | 正確 |
| 4 | **插座發熱** | **維修** | **❌ 商品** | **錯誤** |

---

## 🔍 根本原因分析（深度版）

### 問題根源：意圖類型不一致

系統中實際存在**三種意圖**，但 `detectIntent()` 函式只處理**兩種**！

#### 實際意圖類型

1. **`repair`** - 住宅維修
2. **`shopping`** - 商品搜尋  
3. **`company`** - 公司資訊 ← **這個被遺漏了！**

#### `detectIntent()` 的邏輯缺陷

```javascript
// ❌ 現有代碼（lines 1710-1714）
let detectedIntent = 'shopping';
if (repairScore > shoppingScore && repairScore > 0) {
  detectedIntent = 'repair';
} else if (shoppingScore > repairScore && shoppingScore > 0) {
  detectedIntent = 'shopping';
}
// ⚠️ 完全沒有處理 company 意圖！
```

**問題：** 沒有計算 `company` 關鍵字分數，也沒有判斷 `company` 意圖。

---

## 🧪 問題場景模擬

### 場景：瓦斯洩漏 → 公司地址 → 插座發熱

#### 步驟 1：「瓦斯洩漏」

```javascript
// 輸入: "瓦斯洩漏"
repairScore = 2   // "瓦斯", "洩漏"
shoppingScore = 0

// 判斷
detectedIntent = 'repair'  ✅

// 狀態更新
currentIntentMode = 'repair'
intentModeLock = 'system'
```

#### 步驟 2：「公司地址」

```javascript
// 輸入: "公司地址"
repairScore = 0
shoppingScore = 0
// ⚠️ 沒有計算 companyScore！

// 判斷
detectedIntent = 'shopping'  // 預設值 ❌

// 系統鎖定檢查
hasExplicitIntent = (detectedIntent === 'repair' && repairScore >= 1) ||
                    (detectedIntent === 'shopping' && shoppingScore >= 1)
                  = ('shopping' === 'repair' && 0 >= 1) ||
                    ('shopping' === 'shopping' && 0 >= 1)
                  = false  ❌

// 結果：保持鎖定
return currentIntentMode  // 返回 'repair' ❌❌❌

// 但實際上使用者查詢的是公司資訊！
// 這裡就開始偏離了！
```

**關鍵問題：** 「公司地址」被誤判為 `shopping`，且因為沒有關鍵字（`shoppingScore=0`），系統保持在 `repair` 鎖定狀態！

#### 步驟 3：「瓦斯洩漏」（第二次）

```javascript
// 輸入: "瓦斯洩漏"
repairScore = 2
shoppingScore = 0

// 判斷
detectedIntent = 'repair'  ✅

// 系統鎖定檢查
currentIntentMode = 'repair'  // 步驟2的錯誤狀態
hasExplicitIntent = ('repair' === 'repair' && 2 >= 1) = true ✅

// 意圖持續
return 'repair'  ✅
```

#### 步驟 4：「插座發熱」❌ 失敗！

```javascript
// 輸入: "插座發熱"
repairScore = 2   // "插座", "發熱"
shoppingScore = 0

// 判斷
detectedIntent = 'repair'  ✅

// 系統鎖定檢查
currentIntentMode = 'repair'  // 🚨 但這是錯誤的狀態！
intentModeLock = 'system'

hasExplicitIntent = ('repair' === 'repair' && 2 >= 1) = true ✅

// 意圖持續
detectedIntent === currentIntentMode  // true
console.log('✅ [Intent] 意圖持續: repair')  ✅
setIntentMode('repair', 'explicit-intent', 'system')
return 'repair'  ✅✅✅

// ❓ 但為什麼實際路由到商品？
```

**等等！detectIntent 明明返回了 'repair'，為什麼路由到商品？**

---

## 🔎 更深層的問題：後端路由邏輯

讓我們檢查 `routeMessageByIntent()` 函式：

```javascript
async function routeMessageByIntent(message, intent, options = {}) {
  // 檢測意圖切換
  let lastIntent = null;
  if (chatHistory.length > 0) {
    for (let i = chatHistory.length - 1; i >= 0; i--) {
      if (chatHistory[i].role === 'user') {
        lastIntent = detectIntentPure(chatHistory[i].content);  // ✅ 使用純粹檢測
        break;
      }
    }
  }
  
  const intentChanged = lastIntent && lastIntent !== intent;
  
  // 如果意圖切換，清空歷史
  const payloadHistory = intentChanged ? [] : chatHistory.slice(-10);
}
```

**問題發現：**

#### 步驟 4 執行時的狀態

```javascript
// chatHistory 中最後一條使用者訊息是：「瓦斯洩漏」（步驟3）

lastIntent = detectIntentPure('瓦斯洩漏') = 'repair'
intent = detectIntent('插座發熱') = 'repair'

intentChanged = 'repair' !== 'repair' = false  ✅

// 不會清空歷史，路由應該正確
```

**看起來邏輯沒問題啊！**

---

## 🤔 還有一個可能：後端 API 判斷

問題可能不在前端意圖檢測，而在**後端API的意圖二次判斷**！

### 後端流程

用戶輸入 → 前端 `detectIntent()` → 路由到 `/api/chat` or `/api/repair/chat` → **後端LLM分析意圖** → 最終路由

#### 關鍵文件：`backend/llm_service.py`

```python
def llm_analyze_query(user_query: str) -> dict:
    """
    使用 LLM 分析查詢意圖
    返回: {
        "intent": "company_info" | "product_search" | "information",
        "required_terms": [...],
        "excluded_terms": [...]
    }
    """
```

**懷疑：**
1. 前端正確檢測為 `repair`
2. 路由到 `/api/repair/chat`
3. **但後端住宅維修服務可能有問題**
4. 或者前端實際路由時用錯了 intent 參數

---

## 🎯 真正的問題：調用鏈追蹤

讓我們追蹤實際的調用：

### 前端調用流程

```javascript
// 1. 用戶輸入
const userMessage = "插座發熱";

// 2. 檢測意圖
const intent = detectIntent(userMessage);  // 返回 'repair'

// 3. 調用 routeMessageByIntent
await routeMessageByIntent(userMessage, intent);

// 4. 在 routeMessageByIntent 內部
if (intent === 'repair') {
  // 路由到維修服務
  console.log('🔧 [Route] 路由到維修服務');
  return await fetch('/api/repair/chat', {
    method: 'POST',
    body: JSON.stringify({
      message: userMessage,
      history: payloadHistory
    })
  });
} else {
  // 路由到商品/公司服務
  console.log('🛒 [Route] 路由到商品/公司服務');
  return await fetch('/api/chat', {
    method: 'POST',
    body: JSON.stringify({
      message: userMessage,
      history: payloadHistory
    })
  });
}
```

**如果 Console 顯示 `🛒 [Route] 路由到商品/公司服務`，那問題就在前端！**

---

## 💡 診斷步驟

### 立即行動：添加調試日誌

1. 打開瀏覽器開發者工具（F12）
2. 進入 Console 標籤
3. 輸入測試序列：
   - 瓦斯洩漏
   - 公司地址
   - 瓦斯洩漏
   - 插座發熱

4. **觀察關鍵日誌：**
   ```
   🎯 [Intent] 意圖分析: { ... }
   ✅ [Intent] 意圖持續: repair
   🔧 [Route] 路由到維修服務  ← 應該看到這個
   或
   🛒 [Route] 路由到商品/公司服務  ← 如果看到這個就是問題
   ```

---

## 🔧 修正方案

### 方案 B：完整的三意圖支援

#### 修改 1：增加 company 關鍵字計算

```javascript
function detectIntent(message) {
  const manualLocked = intentModeLock === 'manual' && currentIntentMode !== 'auto';
  if (manualLocked) {
    return currentIntentMode;
  }

  // ✅ 計算三種意圖分數
  let repairScore = 0;
  let shoppingScore = 0;
  let companyScore = 0;  // ← 新增
  
  REPAIR_KEYWORDS.forEach(keyword => {
    if (message.includes(keyword)) {
      repairScore++;
    }
  });
  
  SHOPPING_KEYWORDS.forEach(keyword => {
    if (message.includes(keyword)) {
      shoppingScore++;
    }
  });
  
  // ✅ 新增 company 關鍵字檢測
  const COMPANY_KEYWORDS = ['公司', '地址', '電話', '聯絡', '營業時間', '服務時間'];
  COMPANY_KEYWORDS.forEach(keyword => {
    if (message.includes(keyword)) {
      companyScore++;
    }
  });
  
  // ✅ 三意圖判斷邏輯
  let detectedIntent = 'shopping';
  if (companyScore > 0 && companyScore >= repairScore && companyScore >= shoppingScore) {
    detectedIntent = 'company';  // 公司優先
  } else if (repairScore > shoppingScore && repairScore > 0) {
    detectedIntent = 'repair';
  } else if (shoppingScore > 0) {
    detectedIntent = 'shopping';
  }
  
  console.log('🎯 [Intent] 意圖分析:', {
    message: message.substring(0, 50),
    repairScore,
    shoppingScore,
    companyScore,  // ← 新增
    detected: detectedIntent,
    current: currentIntentMode,
    lock: intentModeLock
  });
  
  // ✅ 系統鎖定時的意圖處理邏輯（支援三種意圖）
  if (intentModeLock === 'system' && currentIntentMode !== 'auto') {
    const hasExplicitIntent = 
      (detectedIntent === 'repair' && repairScore >= 1) ||
      (detectedIntent === 'shopping' && shoppingScore >= 1) ||
      (detectedIntent === 'company' && companyScore >= 1);  // ← 新增
    
    if (hasExplicitIntent) {
      if (detectedIntent !== currentIntentMode) {
        console.log(`🔄 [Intent] 意圖切換: ${currentIntentMode} → ${detectedIntent}`);
      } else {
        console.log(`✅ [Intent] 意圖持續: ${detectedIntent}`);
      }
      setIntentMode(detectedIntent, 'explicit-intent', 'system');
      return detectedIntent;
    }
    
    console.log(`🔒 [Intent] 保持鎖定: ${currentIntentMode} (無明確關鍵字)`);
    return currentIntentMode;
  }
  
  // auto 模式首次檢測
  if (currentIntentMode === 'auto' && (repairScore > 0 || shoppingScore > 0 || companyScore > 0)) {
    setIntentMode(detectedIntent, 'auto-detect', 'system');
  }
  
  return detectedIntent;
}
```

---

## 📋 下一步行動

1. **先診斷：** 使用瀏覽器 Console 查看實際日誌
2. **確認問題：** 是前端還是後端？
3. **實施修正：** 根據診斷結果選擇方案
4. **測試驗證：** 重新執行完整測試

**請先執行診斷步驟，將 Console 日誌貼出來，我才能精確定位問題！**
