# 聊天意圖模式優化紀錄

## 變更背景
- 依據最新需求，需要在前端統一管理維修 / 商品意圖切換，避免偵測結果無法持續鎖定維修路由，導致後續追問又落回商品模型。
- 既有流程中 `setIntentMode`、`detectIntent`、`routeMessageByIntent`、回應渲染與重置按鈕分別處理，缺乏單一入口，造成狀態不同步。

## 優化摘要
1. **新增 setIntentMode 統一入口**：所有模式切換都透過此函式更新 `currentIntentMode`、同步 UI 與留存 log。
2. **自動鎖定維修模式**：`detectIntent()` 在 auto 模式下判定出維修後立即鎖定；`routeMessageByIntent()`、回應渲染與後端 `_intent` 旗標都會確保鎖定行為。
3. **模式按鈕常駐並支援手動鎖定**：介面上顯示「自動 / 商品 / 維修」三顆按鈕，使用者可手動鎖定，提示文字會說明目前狀態。
4. **鎖定機制拆分**：`setIntentMode` 增加來源參數（auto/manual/system）與 `intentModeLock`，防止手動鎖定被自動流程覆寫，並透過提示提醒如何解除。
5. **UI 初始同步**：頁面載入即呼叫 `updateModeButtons` 與 `updateModeLockHint()`，避免顯示與實際狀態不同。

## 詳細調整

### 1. setIntentMode 強化
- 新增 `intentModeLock`（auto/manual/system）與 `modeHint` 顯示，並記錄來源：手動維持鎖定、系統可根據偵測結果鎖定，切回「自動」即可解除。
- 增加 `prevMode` 紀錄並在 Console 顯示模式與鎖定來源。
- 即使模式未變更也會刷新按鈕與提示，以免 UI 同步錯誤。

### 2. 手動按鈕與提示
- 將「自動 / 商品 / 維修」按鈕固定顯示在查詢面板，並新增 `modeHint` 說明目前鎖定原因與解鎖指引。
- 按鈕事件以 `source='manual'` 呼叫 `setIntentMode`，確保鎖定狀態被標記為手動。

### 3. detectIntent 與維修鎖定
- 當 `intentModeLock` 是手動或系統（非 auto）時，`detectIntent` 直接回傳鎖定模式；否則依關鍵字評分並在自動模式下鎖定維修。

### 4. routeMessageByIntent / render 對後端 `_intent` 的處理
- 成功呼叫 `/api/repair/chat` 或後端回傳 `_intent: 'repair'` 時，若當前為 auto 模式則立即鎖定。
- 商品路由維持既有流程，但若後端意外回傳 `_intent: 'repair'` 也會鎖定，避免落差。
- 渲染回應時再次檢查 `_intent`，確保畫面與狀態一致。

### 5. 重置流程
- `doReset()` 於清除會話後呼叫 `setIntentMode('auto', 'reset')`，確保重新開始後回到 auto。
- 三顆模式按鈕皆透過 `setIntentMode` 控制，UI 與狀態一致。

## 測試建議
1. 在 auto 模式輸入維修需求並持續追問，確認後續請求都命中 `/api/repair/chat`，直到按「重新開始」。
2. 測試「重新開始」與三個模式按鈕，觀察 Console log 與畫面提示應顯示 `setIntentMode` 前後模式、鎖定來源與解鎖方式。
3. 先手動鎖定商品，再切回自動並輸入維修問題，確認系統會重新鎖定維修；若後端在商品路由回傳 `_intent: 'repair'`，前端也應鎖定並在提示中說明原因。***
