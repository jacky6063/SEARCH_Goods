# 聊天模式 LLM 流程 - 開發維護指南

## 🎯 核心概念速記

> **一句話總結**：用戶聊天 → LLM 分析意圖 → 多層商品搜尋 → LLM 生成自然回覆 → 前端顯示

## 🚀 快速定位問題

### ❓ 「聊天為什麼沒有找到商品？」

**診斷流程：**
```
1️⃣  檢查 LLM 是否被調用
    → 查看 DEBUG 日誌中是否有 "llm_analyze_query() 被呼叫"
    → 檢查 OPENAI_API_KEY 是否設置

2️⃣  檢查意圖是否正確識別
    → 查看日誌中的 "intention: product_search"
    → 如果是 "information"，LLM 只回答資訊，不搜尋商品

3️⃣  檢查分類層級識別
    → 查看日誌中的 "Category hierarchy detected: L1=... L2=... L3=..."
    → 如果沒有識別到分類，使用傳統模糊搜尋

4️⃣  檢查搜尋結果
    → 直接運行 goods_search_service.search_products()
    → 測試查詢詞是否能在 CSV 中找到
```

### ❓ 「為什麼搜尋又改成了 Mock 模式？」

**可能原因：**
```
❌ OpenAI API key 失效或未設置
   → 修復：檢查 .env 中的 OPENAI_API_KEY

❌ 模型不存在或無權限
   → 修復：檢查 CHAT_OPENAI_MODEL 是否有效
   → 常見值：gpt-4o-mini, gpt-4o, gpt-3.5-turbo

❌ LLM 開關被關閉
   → 修復：CHAT_USE_LLM_INTENT=True

❌ LLM 返回空值
   → 修復：檢查 _call_chat() 的回傳
   → 查看 OpenAI 是否返回錯誤信息
```

### ❓ 「分類層級搜尋為什麼不工作？」

**逐步診斷：**
```
1️⃣  驗證 CSV 中的分類欄位存在
    $ grep "CateName_L1\|大分類名稱" data/VIEW_GOODS_enhanced.csv | head

2️⃣  驗證分類同義詞快取
    → llm_service.py 行 139-140：_extract_category_synonyms()
    → 檢查是否有 DEBUG 日誌「Extracted X category synonyms」

3️⃣  驗證 LLM 分類識別
    → 測試代碼：
      from llm_service import llm_analyze_query
      result = llm_analyze_query("我要買食品類的調味油")
      print(result.get("category_hierarchy"))
      # 應輸出 {"L1": "食品", "L2": "調味油", "L3": ""}

4️⃣  驗證層級搜尋邏輯
    → 測試代碼：
      from llm_service import _search_by_category_hierarchy
      df = _get_chat_df()
      hierarchy = {"L1": "食品", "L2": "調味油", "L3": ""}
      results = _search_by_category_hierarchy(df, hierarchy)
      print(f"Found: {len(results)} products")
```

## 📝 代碼修改清單

### 如果要修改商品搜尋邏輯

**涉及文件：**
```
llm_service.py
  ├─ _search_products_for_chat() [1254]     ← 主搜尋引擎
  ├─ _search_by_category_hierarchy() [1100] ← 分類層級搜尋
  ├─ _apply_structured_filters() [1229]     ← 結構化篩選
  ├─ _extract_keywords() [1360]              ← 關鍵詞提取
  └─ _filter_products_by_keywords() [1308]   ← 關鍵詞篩選

chat_router_goods_action.py
  ├─ _build_user_friendly_reply() [80]       ← 回覆格式
  └─ _build_structured_items() [120]         ← 商品結構化
```

**修改步驟：**
```
1️⃣  修改搜尋邏輯
2️⃣  添加 DEBUG 日誌：print(f"[DEBUG] ...")
3️⃣  運行測試：pytest backend/tests/test_llm_intent_parsing.py
4️⃣  檢查回歸：確保舊測試仍通過
5️⃣  提交：遵循 開發原則 檢查清單
```

### 如果要調整 LLM 行為

**涉及環境變數：**
```bash
# 查詢擴展
CHAT_USE_LLM_EXPAND=True          # 啟用同義詞擴展
SEARCH_USE_LLM_EXPAND=False       # 搜尋模式不擴展（節省 token）

# 意圖分析
CHAT_USE_LLM_INTENT=True          # 必須啟用
SEARCH_USE_LLM_INTENT=False       # 搜尋模式不分析（節省 token）

# 結果重排（謹慎使用，會影響性能）
CHAT_USE_LLM_RERANK=False         # ⚠️ 建議關閉
SEARCH_USE_LLM_RERANK=False       # ⚠️ 建議關閉

# 模型選擇
CHAT_OPENAI_MODEL=gpt-4o-mini     # 推薦：成本低、速度快
SEARCH_OPENAI_MODEL=gpt-4o-mini   # 推薦：與聊天模型一致
```

## 🧪 測試指令

### 測試 LLM 意圖分析

```python
# backend/test_llm_flow.py
from llm_service import llm_analyze_query, _detect_conversation_intent

# 測試意圖分析
query = "我要買食品類的調味油，特別是橄欖油"
analysis = llm_analyze_query(query, use_search_config=False)
print(f"意圖: {analysis.get('required_terms')}")
print(f"分類: {analysis.get('category_hierarchy')}")

# 測試對話意圖
intent = _detect_conversation_intent(query)
print(f"對話意圖: {intent}")  # 應輸出: "product_search"
```

### 測試商品搜尋

```python
from llm_service import _search_products_for_chat, _get_chat_df

query = "我要買食品類的調味油"
keywords = ["食品", "調味油", "橄欖油"]

results = _search_products_for_chat(
    query=query,
    keywords=keywords,
    topn=5,
    hierarchy={"L1": "食品", "L2": "調味油", "L3": ""}
)

print(f"精確匹配: {len(results['exact'])} 件")
print(f"模糊匹配: {len(results['fuzzy'])} 件")

for item in results['exact'][:3]:
    print(f"  - {item.get('Name')}")
```

### 運行完整流程測試

```bash
# 後端測試
cd backend
pytest tests/test_llm_intent_parsing.py -v
pytest tests/test_api.py::test_chat_endpoint -v

# 手動測試
python3 -c "
from chat_router_goods_action import chat_handler, ChatReq

req = ChatReq(
    user_message='我要買食品類的調味油',
    message='',
    history=[],
    session_id='test-123'
)

result = chat_handler(req)
print(f'回覆: {result.reply[:100]}...')
print(f'推薦商品: {result.suggestion_ids}')
"
```

## 📊 性能優化建議

### 1. 減少 LLM 調用

```python
# ❌ 不推薦：每次搜尋都調用 LLM 重排
CHAT_USE_LLM_RERANK=True

# ✅ 推薦：只在必要時使用分類層級搜尋
# LLM 意圖分析 → 分類層級搜尋 → 直接返回
# 跳過 LLM 重排，節省 token 和時間
```

### 2. 快取優化

```python
# 在 llm_service.py 中已有：
_CATEGORY_SYNONYMS_CACHE  # 分類同義詞快取
_CHAT_DF_CACHE            # 商品 DataFrame 快取

# 使用 SESSION_CACHE 在後續搜尋中重用
SESSION_ALIGN_CACHE[session_id]
```

### 3. 數據庫查詢優化

```python
# 若切換到數據庫，考慮索引：
# 商品表：
#   - 名稱 (Name) 索引
#   - L1/L2/L3 複合索引
#   - 特價狀態 (SpecialOffer) 索引

# 分類表：
#   - (L1, L2, L3) 複合索引用於層級查詢
```

## 🔍 日誌解讀指南

### 正常流程日誌

```
[INFO] 📋 llm_analyze_query() 被呼叫
[INFO]   - 意圖分析啟用: True, 模型: gpt-4o-mini
[INFO]   - 呼叫 OpenAI API 進行意圖分析
[INFO]   ✅ 意圖分析完成: {...}
[INFO] Hierarchy search: matched 8 products with levels ['L1', 'L2']
[DEBUG] Fallback to mock reply
```

### 常見錯誤日誌

```
# ❌ API key 失效
[DEBUG] OPENAI_API_KEY not found in environment
[DEBUG] Failed to create OpenAI client

# ❌ LLM 意圖分析失敗
[WARNING] ❌ 意圖分析失敗: json.JSONDecodeError

# ❌ 搜尋結果為空
[WARNING] Chat product search failed: list index out of range
```

## 📞 常見問題排查

| 症狀 | 可能原因 | 解決方案 |
|------|--------|--------|
| 聊天沒有搜尋到商品 | LLM 未調用或返回空 | 檢查 OPENAI_API_KEY + CHAT_USE_LLM_INTENT |
| 總是返回 Mock 回覆 | LLM 不可用 | 檢查 OpenAI 配額和網絡連接 |
| 搜尋結果不相關 | 分類層級識別失敗 | 驗證 CSV 欄位名和分類值 |
| 查詢時間過長 | LLM 重排啟用 | 關閉 CHAT_USE_LLM_RERANK |
| 特價商品沒出現 | 篩選器排除了特價 | 檢查 _apply_structured_filters() |

## 🎓 新手入門路線

```
Day 1: 理解整體架構
  □ 讀 聊天模式_LLM判讀流程圖.md
  □ 追蹤一個用戶查詢的完整流程

Day 2: 理解代碼結構
  □ 熟悉 llm_service.py 的 4 個核心函數
  □ 了解 chat_router_goods_action.py 的分派邏輯

Day 3: 修改和測試
  □ 修改一個環境變數，觀察效果
  □ 運行測試用例 (test_llm_intent_parsing.py)
  □ 手動測試一個查詢

Day 4: 優化和文檔
  □ 識別一個性能瓶頸
  □ 提出一個優化方案
  □ 更新相關文檔
```

## 📚 相關文檔

- **詳細流程**：`聊天模式_LLM判讀流程圖.md` (586 行)
- **快速流程**：`聊天模式_快速流程.md` (136 行)
- **開發原則**：`後續開發原則_檢查清單.md`
- **欄位標準**：`欄位使用記錄_中英文對照_標準化指南.md`

---

**最後更新**：2025年11月8日
**提交者**：GitHub Copilot
**版本**：v1.0

