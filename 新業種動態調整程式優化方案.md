# 新業種動態調整程式優化方案

## ⚠️ 實施注意事項

### 不在範圍
- 不重構專案架構、路由設計、目錄結構。
- 不改 LLM 模型與金鑰設置；不硬編 API Key。

### 禁止事項（務必遵守）
- **禁止重構整個專案**：不得主動改動架構、路由、目錄，除非任務卡明確要求。
- **禁止修改既有商業邏輯**：不得更動商品搜尋/訂單/會員/價格/庫存與 DB schema。
- **禁止硬編 API Key / 機敏資訊**：所有金鑰一律用環境變數，提示詞/程式碼不可寫死。
- **一次只執行一張任務卡**：不得偷跑下一張卡的內容。
- **不確定時少改**：無法判斷時，優先加註解/提議，不要大改。
- **不執行的命令**：不得使用 `git reset --hard`、`git checkout -- .`、`rm -rf /` 等破壞性指令；不得清除非本任務相關檔案。

---

## 🎯 核心問題釐清

### Q1: 未來 VIEW_GOODS_enhanced.csv 有新的大分類時，是否要有程式去執行補強某些程式段，才能順利被引用？

**答案：目前系統設計是「是的」❌，但這是不合理的！**

### Q2: 程式碼是否要隨時更新？

**答案：不應該！這是設計缺陷！✋**

---

## 🔍 問題本質分析

### 現況：兩層資料結構脫節

```
📊 資料層（動態）
├─ VIEW_GOODS_enhanced.csv      ← 商品資料
└─ goods_categories.csv          ← 分類定義

⬇️ 應該自動同步 ❌ 但目前沒有

🤖 AI 知識層（靜態）
└─ llm_service.py 
   └─ _build_category_hierarchy_prompt()  ← 硬編碼的分類範例
```

### 問題：資料層和 AI 知識層脫節

1. **資料層**：新增「磐鈺建設」分類 → ✅ 商品可搜尋到
2. **AI 知識層**：LLM Prompt 沒更新 → ❌ LLM 不認識 → 要求澄清

**實際案例：**
- 查詢「醬油」→ ✅ 成功（Prompt 中有範例）
- 查詢「極致輕寓2房」→ ❌ 要求澄清（Prompt 中無範例）

---

## 💡 正確的系統設計應該是

### 原則：資料驅動，程式碼不需修改

```
📊 資料層（CSV）
  ↓ 自動讀取
🤖 AI 知識層（動態生成 Prompt）
  ↓ 自動使用
🔍 搜尋/聊天功能
```

**新增分類時，只需：**
1. 更新 `VIEW_GOODS_enhanced.csv` ← 新增商品
2. 更新 `goods_categories.csv` ← 新增分類定義
3. 清除快取 ← API 呼叫
4. **結束！不需要改程式碼！** ✅

---

## 🛠️ 目前系統的問題根源

### 檔案：`backend/llm_service.py` (line 580)

```python
def _build_category_hierarchy_prompt() -> str:
    """
    根據 CSV 中的實際分類層級，動態構建 LLM 提示詞
    優化版本: 整合真實商品範例,重點標註容易誤判的分類
    """
    return """
    📦 常溫食品類: ...  ← 硬編碼 ❌
    👜 包包配件類: ...  ← 硬編碼 ❌
    👟 戶外與運動用品類: ... ← 硬編碼 ❌
    """
```

**問題：**
- 函數名稱說「動態構建」
- 但實際上 **return 一個固定的字串**
- 新增分類後，LLM 不認識新的業種

---

## ✅ 正確的解決方案

### 方案：真正實現動態載入

```python
def _build_category_hierarchy_prompt() -> str:
    """
    真正動態從 CSV 構建 LLM 提示詞（不需要改程式碼）
    """
    from services.categories_service import get_all_categories
    from services.catalog_service import snapshot
    
    # 1. 從 goods_categories.csv 讀取所有分類
    categories = get_all_categories()
    
    # 2. 從 VIEW_GOODS_enhanced.csv 取得商品範例
    products = snapshot(limit=2000)
    
    # 3. 動態生成 Prompt
    prompt_parts = ["你是商品分類專家，以下是目前系統的所有分類：\n"]
    
    # 4. 建立分類樹狀結構
    category_tree = {}
    for cat in categories:
        l1, l2, l3 = cat['L1'], cat['L2'], cat['L3']
        if l1 not in category_tree:
            category_tree[l1] = {}
        if l2 not in category_tree[l1]:
            category_tree[l1][l2] = []
        if l3 and l3 not in category_tree[l1][l2]:
            category_tree[l1][l2].append(l3)
    
    # 5. 為每個分類找出商品範例
    for l1, l2_dict in category_tree.items():
        prompt_parts.append(f"\n📦 {l1}:")
        for l2, l3_list in l2_dict.items():
            prompt_parts.append(f"  • {l2}:")
            for l3 in l3_list:
                # 找該分類的商品範例（前3個）
                examples = [
                    p.get('商品名稱', '')[:30]
                    for p in products
                    if p.get('小分類名稱') == l3
                ][:3]
                
                if examples:
                    examples_str = "、".join(examples)
                    prompt_parts.append(f"    - {l3}: {examples_str}")
                else:
                    prompt_parts.append(f"    - {l3}")
    
    # 6. 加入識別原則
    prompt_parts.extend([
        "\n【識別原則】",
        "1. 商品核心名詞優先：提取查詢中的核心商品名",
        "2. 忽略修飾詞：「台灣」、「日曬」、「有機」等為修飾詞",
        "3. 不確定時給出最相關層級：至少識別 L1 大分類",
        "\n【輸出格式】",
        "必須包含 JSON 格式：",
        "{",
        '  "category_hierarchy": {"L1": "...", "L2": "...", "L3": "..."},',
        '  "confidence": {"L1": 0.0-1.0, "L2": 0.0-1.0, "L3": 0.0-1.0},',
        '  "matching_keywords": [...]',
        "}"
    ])
    
    return "\n".join(prompt_parts)
```

---

## 🎯 方案優點

### ✅ 資料驅動的優勢

| 項目 | 目前系統（硬編碼）❌ | 優化後（動態載入）✅ |
|------|---------------------|---------------------|
| **新增分類** | 需修改程式碼 | 只需更新 CSV |
| **商品範例** | 手動維護 | 自動抓取最新商品 |
| **LLM 知識** | 可能過時 | 永遠使用最新資訊 |
| **維護成本** | 高（需懂程式碼） | 低（只需更新資料） |
| **出錯風險** | 高（忘記更新） | 低（自動同步） |
| **擴展性** | 差 | 優秀 |

### ✅ 具體改善

1. **新增「磐鈺建設」分類**
   - 目前：需改 `llm_service.py` + 重啟服務
   - 優化後：更新 CSV + 清除快取

2. **商品範例更新**
   - 目前：手動在程式碼中寫死範例
   - 優化後：自動從最新商品中抓取

3. **系統維護**
   - 目前：需要工程師介入
   - 優化後：業務人員即可操作

---

## 📊 實施方案比較

### 選項 A：快速修復（臨時方案）

**做法**：
```python
def _build_category_hierarchy_prompt() -> str:
    return """
    📦 常溫食品類: ...
    👜 包包配件類: ...
    👟 戶外與運動用品類: ...
    
    🏢 磐鈺建設類:  # ← 手動新增
      🏘️ 磐鈺草間漫漫:
        ⭐ 極致輕寓2房: 30坪D戶型、35坪寬域2房、45坪經典3房、55坪旗艦3房
        範例: "極致輕寓2房"、"草間漫漫30坪"、"科博館住宅"
    """
```

**優點**：
- ✅ 立即可用（15 分鐘內完成）
- ✅ 讓「極致輕寓2房」查詢正常

**缺點**：
- ❌ 下次新增分類還是要改程式碼
- ❌ 沒有解決根本問題
- ❌ 維護成本持續存在

---

### 選項 B：徹底修復（建議方案）✅

**做法**：改寫 `_build_category_hierarchy_prompt()` 為真正的動態載入

**優點**：
- ✅ 一勞永逸
- ✅ 未來新增分類不需改程式碼
- ✅ 商品範例自動更新
- ✅ 系統更容易維護
- ✅ 降低出錯風險

**缺點**：
- ⚠️ 需要較多開發時間（2-3 小時）
- ⚠️ 需要完整測試

**預估工作量**：
1. 修改 `_build_category_hierarchy_prompt()` - 1 小時
2. 測試各種分類查詢 - 30 分鐘
3. 加入快取機制（TTL 5 分鐘）- 30 分鐘
4. 整合測試與調校 - 1 小時

---

## 🚀 實施步驟（建議方案）

### Phase 1: 核心功能實作

```python
# backend/llm_service.py

def _build_category_hierarchy_prompt() -> str:
    """動態從 CSV 構建分類提示詞"""
    try:
        from services.categories_service import get_all_categories
        from services.catalog_service import snapshot
        
        categories = get_all_categories()
        products = snapshot(limit=2000)
        
        # 構建分類樹
        category_tree = _build_category_tree(categories)
        
        # 生成 Prompt
        prompt = _generate_prompt_from_tree(category_tree, products)
        
        return prompt
    except Exception as e:
        _logger.warning(f"動態生成 Prompt 失敗，使用備用版本: {e}")
        return _get_fallback_prompt()  # 降級處理

def _build_category_tree(categories):
    """構建三層分類樹"""
    tree = {}
    for cat in categories:
        l1, l2, l3 = cat['L1'], cat['L2'], cat['L3']
        if l1 not in tree:
            tree[l1] = {}
        if l2 not in tree[l1]:
            tree[l1][l2] = []
        if l3 and l3 not in tree[l1][l2]:
            tree[l1][l2].append(l3)
    return tree

def _generate_prompt_from_tree(tree, products):
    """從分類樹和商品資料生成 Prompt"""
    parts = ["你是商品分類專家，以下是目前系統的所有分類：\n"]
    
    for l1, l2_dict in tree.items():
        parts.append(f"\n📦 {l1}:")
        for l2, l3_list in l2_dict.items():
            parts.append(f"  • {l2}:")
            for l3 in l3_list:
                examples = _get_product_examples(products, l3, limit=3)
                if examples:
                    parts.append(f"    - {l3}: {', '.join(examples)}")
                else:
                    parts.append(f"    - {l3}")
    
    parts.extend(_get_identification_rules())
    return "\n".join(parts)

def _get_product_examples(products, category, limit=3):
    """取得指定分類的商品範例"""
    examples = [
        p.get('商品名稱', '')[:30]
        for p in products
        if p.get('小分類名稱') == category
    ]
    return examples[:limit]

def _get_identification_rules():
    """取得識別原則說明"""
    return [
        "\n【識別原則】",
        "1. 提取查詢中的核心商品名",
        "2. 忽略修飾詞（台灣、有機等）",
        "3. 至少識別 L1 大分類",
        "\n【輸出格式】JSON:",
        '{"category_hierarchy": {"L1": "", "L2": "", "L3": ""},',
        ' "confidence": {"L1": 0.0, "L2": 0.0, "L3": 0.0}}'
    ]

def _get_fallback_prompt():
    """備用靜態 Prompt（當動態載入失敗時）"""
    return """基本商品分類識別..."""
```

### Phase 2: 快取優化（含降級機制）

```python
# 加入快取機制避免每次都重新生成

from functools import lru_cache
import time

_category_prompt_cache = None
_category_prompt_cache_time = 0
_category_prompt_failed_count = 0  # 🆕 失敗計數
PROMPT_CACHE_TTL = 300  # 5 分鐘
MAX_PROMPT_LENGTH = 8000  # 🆕 最大長度限制
FALLBACK_STATIC_PROMPT = None  # 🆕 靜態備用 Prompt

def _build_category_hierarchy_prompt() -> str:
    """
    動態構建提示詞，含快取、降級、告警機制
    """
    global _category_prompt_cache, _category_prompt_cache_time, _category_prompt_failed_count
    
    now = time.time()
    
    # 使用快取
    if _category_prompt_cache and (now - _category_prompt_cache_time) < PROMPT_CACHE_TTL:
        _logger.debug("使用快取的分類提示詞")
        return _category_prompt_cache
    
    # 重新生成
    try:
        _logger.info("開始重建分類提示詞...")
        prompt = _generate_dynamic_prompt_with_controls()
        
        # 🆕 驗證生成結果
        if not prompt or len(prompt) < 100:
            raise ValueError("生成的提示詞過短或為空")
        
        if len(prompt) > MAX_PROMPT_LENGTH:
            _logger.warning(f"提示詞過長 ({len(prompt)} 字元)，進行裁剪")
            prompt = _trim_prompt_intelligently(prompt, MAX_PROMPT_LENGTH)
        
        # 🆕 覆蓋檢查：確保新分類包含在提示詞中
        missing_categories = _check_category_coverage(prompt)
        if missing_categories:
            _logger.warning(f"提示詞缺少以下分類: {missing_categories}")
        
        # 更新快取
        _category_prompt_cache = prompt
        _category_prompt_cache_time = now
        _category_prompt_failed_count = 0  # 重置失敗計數
        
        _logger.info(f"✅ 分類提示詞重建成功 (長度: {len(prompt)})")
        return prompt
        
    except Exception as e:
        _category_prompt_failed_count += 1
        _logger.error(f"❌ 動態生成提示詞失敗 (第 {_category_prompt_failed_count} 次): {e}", exc_info=True)
        
        # 🆕 降級處理：使用靜態備用版本
        if FALLBACK_STATIC_PROMPT:
            _logger.warning("⚠️ 使用靜態備用提示詞")
            return FALLBACK_STATIC_PROMPT
        
        # 最終降級：返回最基本的提示詞
        _logger.error("🚨 無備用提示詞可用，使用最小化版本")
        return _get_minimal_fallback_prompt()

def _check_category_coverage(prompt: str) -> List[str]:
    """
    🆕 檢查提示詞是否包含所有啟用的分類
    返回缺失的分類列表
    """
    try:
        from services.categories_service import get_all_categories
        categories = get_all_categories()
        
        missing = []
        for cat in categories:
            if cat.get('Enabled') == 1:  # 只檢查啟用的分類
                l1 = cat.get('L1', '')
                if l1 and l1 not in prompt:
                    missing.append(l1)
        
        return list(set(missing))  # 去重
    except Exception as e:
        _logger.warning(f"分類覆蓋檢查失敗: {e}")
        return []

def _trim_prompt_intelligently(prompt: str, max_length: int) -> str:
    """
    🆕 智能裁剪提示詞，保留每個 L3 至少 1 個範例
    優先級：規則說明 > L1/L2/L3 結構 > 商品範例
    """
    lines = prompt.split('\n')
    
    # 分類內容行
    rule_lines = []  # 識別原則
    category_lines = []  # 分類結構
    example_lines = []  # 商品範例
    
    current_section = 'rule'
    for line in lines:
        if '【識別原則】' in line or '【輸出格式】' in line:
            current_section = 'rule'
            rule_lines.append(line)
        elif line.startswith('📦') or line.startswith('  •'):
            current_section = 'category'
            category_lines.append(line)
        elif line.strip().startswith('-') and current_section == 'category':
            example_lines.append(line)
        elif current_section == 'rule':
            rule_lines.append(line)
        elif current_section == 'category':
            category_lines.append(line)
    
    # 按優先級組合
    result = '\n'.join(rule_lines) + '\n'
    result += '\n'.join(category_lines) + '\n'
    
    # 如果還有空間，加入商品範例
    remaining = max_length - len(result)
    if remaining > 0 and example_lines:
        # 確保每個 L3 至少保留 1 個範例
        examples_to_keep = _ensure_one_example_per_l3(example_lines, remaining)
        result += '\n'.join(examples_to_keep)
    
    return result[:max_length]

def _ensure_one_example_per_l3(example_lines: List[str], max_length: int) -> List[str]:
    """
    🆕 確保每個 L3 分類至少保留 1 個商品範例
    """
    l3_examples = {}
    for line in example_lines:
        # 解析 L3 名稱（假設格式：    - L3名稱: 商品1、商品2）
        if ':' in line:
            l3_name = line.split(':')[0].strip()
            if l3_name not in l3_examples:
                l3_examples[l3_name] = line
    
    # 取得每個 L3 的第一個範例
    result = list(l3_examples.values())
    
    # 如果總長度超過限制，按 DisplayOrder 排序後裁剪
    current_length = sum(len(line) for line in result)
    if current_length > max_length:
        result = result[:max_length // 100]  # 粗略估計每行約 100 字元
    
    return result

def _get_minimal_fallback_prompt() -> str:
    """
    🆕 最小化備用提示詞（當所有動態生成都失敗時）
    """
    return """
你是商品分類專家，請根據用戶查詢識別商品分類。

基本分類包括：
- 常溫食品
- 生活用品
- 時尚女性
- 潮流男性
- 戶外與運動用品

請輸出 JSON 格式：
{
  "category_hierarchy": {"L1": "", "L2": "", "L3": ""},
  "confidence": {"L1": 0.0, "L2": 0.0, "L3": 0.0}
}
""".strip()
```

### Phase 3: 管理介面與上傳流程

```python
# backend/app.py

@app.post("/api/admin/upload-categories-csv")
async def upload_categories_csv(file: UploadFile = File(...), admin_token: str = Form(...)):
    """
    🆕 上傳 goods_categories.csv
    上傳成功後自動：
    1. 清除分類快取
    2. 清除提示詞快取
    3. 可選：立即重建提示詞
    """
    # 驗證管理員權限
    if not _verify_admin_token(admin_token):
        raise HTTPException(status_code=403, detail="無效的管理員令牌")
    
    try:
        # 讀取上傳的檔案
        content = await file.read()
        
        # 驗證 CSV 格式
        try:
            import pandas as pd
            import io
            df = pd.read_csv(io.BytesIO(content))
            required_columns = ['L1', 'L2', 'L3', 'Enabled', 'DisplayOrder']
            if not all(col in df.columns for col in required_columns):
                raise ValueError(f"CSV 缺少必要欄位: {required_columns}")
        except Exception as e:
            raise HTTPException(status_code=400, detail=f"CSV 格式錯誤: {str(e)}")
        
        # 備份現有檔案
        categories_path = Path("backend/data/goods_categories.csv")
        backup_path = categories_path.with_suffix('.csv.backup')
        if categories_path.exists():
            import shutil
            shutil.copy2(categories_path, backup_path)
        
        # 寫入新檔案（原子操作）
        temp_path = categories_path.with_suffix('.csv.tmp')
        temp_path.write_bytes(content)
        temp_path.replace(categories_path)
        
        # 🆕 清除相關快取
        _clear_category_and_prompt_caches()
        
        # 🆕 可選：立即重建提示詞（而非等下次請求）
        try:
            from llm_service import _build_category_hierarchy_prompt
            new_prompt = _build_category_hierarchy_prompt()
            logger.info(f"✅ 提示詞已立即重建 (長度: {len(new_prompt)})")
        except Exception as rebuild_error:
            logger.warning(f"⚠️ 提示詞重建失敗，將於下次請求時重試: {rebuild_error}")
        
        return {
            "status": "ok",
            "message": "分類檔案上傳成功，快取已清除",
            "file_size": len(content),
            "backup_created": backup_path.exists()
        }
    
    except HTTPException:
        raise
    except Exception as e:
        logger.error(f"上傳分類檔案失敗: {e}", exc_info=True)
        raise HTTPException(status_code=500, detail=f"上傳失敗: {str(e)}")

@app.post("/api/admin/upload-goods-csv")
async def upload_goods_csv(file: UploadFile = File(...), admin_token: str = Form(...)):
    """
    🆕 上傳 VIEW_GOODS_enhanced.csv
    上傳成功後自動：
    1. 清除商品快取
    2. 清除提示詞快取（因為商品範例可能變更）
    """
    # ... 類似的上傳邏輯 ...
    
    # 🆕 清除相關快取
    _clear_catalog_and_prompt_caches()
    
    return {"status": "ok", "message": "商品檔案上傳成功"}

def _clear_category_and_prompt_caches():
    """
    🆕 清除分類與提示詞快取
    """
    global _category_prompt_cache, _category_prompt_cache_time
    
    # 清除提示詞快取
    _category_prompt_cache = None
    _category_prompt_cache_time = 0
    
    # 清除分類服務快取
    try:
        from services.categories_service import clear_cache
        clear_cache()
        logger.info("✅ 分類快取已清除")
    except Exception as e:
        logger.warning(f"清除分類快取失敗: {e}")

def _clear_catalog_and_prompt_caches():
    """
    🆕 清除商品與提示詞快取
    """
    global _category_prompt_cache, _category_prompt_cache_time
    
    # 清除提示詞快取
    _category_prompt_cache = None
    _category_prompt_cache_time = 0
    
    # 清除商品快取
    try:
        from services.catalog_service import clear_cache
        clear_cache()
        logger.info("✅ 商品快取已清除")
    except Exception as e:
        logger.warning(f"清除商品快取失敗: {e}")

@app.post("/api/admin/rebuild-llm-prompt")
def rebuild_llm_prompt(admin_token: str = Form(...)):
    """
    🆕 手動重建 LLM 分類 Prompt
    提供給管理員強制刷新使用
    """
    if not _verify_admin_token(admin_token):
        raise HTTPException(status_code=403, detail="無效的管理員令牌")
    
    try:
        _clear_category_and_prompt_caches()
        
        # 立即重建
        from llm_service import _build_category_hierarchy_prompt
        new_prompt = _build_category_hierarchy_prompt()
        
        # 🆕 驗證覆蓋度
        missing = _check_category_coverage(new_prompt)
        
        return {
            "status": "ok",
            "message": "LLM Prompt 已重建",
            "prompt_length": len(new_prompt),
            "missing_categories": missing,
            "cache_cleared": True
        }
    except Exception as e:
        logger.error(f"重建提示詞失敗: {e}", exc_info=True)
        return {
            "status": "error",
            "message": f"重建失敗: {str(e)}",
            "fallback_active": True
        }

@app.get("/api/admin/prompt-status")
def get_prompt_status(admin_token: str = Query(...)):
    """
    🆕 查詢提示詞狀態（用於監控）
    """
    if not _verify_admin_token(admin_token):
        raise HTTPException(status_code=403, detail="無效的管理員令牌")
    
    global _category_prompt_cache, _category_prompt_cache_time, _category_prompt_failed_count
    
    cache_age = time.time() - _category_prompt_cache_time if _category_prompt_cache_time else None
    
    return {
        "cache_exists": _category_prompt_cache is not None,
        "cache_age_seconds": cache_age,
        "cache_ttl_seconds": PROMPT_CACHE_TTL,
        "failed_count": _category_prompt_failed_count,
        "prompt_length": len(_category_prompt_cache) if _category_prompt_cache else 0,
        "using_fallback": _category_prompt_cache is None and _category_prompt_failed_count > 0
    }
```

### Phase 4: 前端管理介面

```html
<!-- frontend/admin.html (新增管理頁面) -->
<!DOCTYPE html>
<html>
<head>
    <title>商品查詢系統 - 管理介面</title>
    <meta charset="utf-8">
    <style>
        body {
            font-family: Arial, sans-serif;
            max-width: 800px;
            margin: 50px auto;
            padding: 20px;
        }
        .upload-section {
            border: 1px solid #ddd;
            padding: 20px;
            margin-bottom: 20px;
            border-radius: 5px;
        }
        .upload-section h2 {
            margin-top: 0;
        }
        .status {
            padding: 10px;
            margin: 10px 0;
            border-radius: 3px;
        }
        .status.success {
            background: #d4edda;
            color: #155724;
        }
        .status.error {
            background: #f8d7da;
            color: #721c24;
        }
        .status.warning {
            background: #fff3cd;
            color: #856404;
        }
        button {
            background: #007bff;
            color: white;
            border: none;
            padding: 10px 20px;
            cursor: pointer;
            border-radius: 3px;
            margin-right: 10px;
        }
        button:hover {
            background: #0056b3;
        }
        .prompt-info {
            background: #f8f9fa;
            padding: 15px;
            border-radius: 5px;
            margin: 10px 0;
        }
    </style>
</head>
<body>
    <h1>🔧 商品查詢系統 - 管理介面</h1>
    
    <!-- 分類檔案上傳 -->
    <div class="upload-section">
        <h2>📂 上傳 goods_categories.csv</h2>
        <p>上傳後會自動清除分類快取與 LLM 提示詞快取</p>
        <input type="file" id="categoriesFile" accept=".csv">
        <button onclick="uploadCategoriesCSV()">上傳分類檔案</button>
        <div id="categoriesStatus"></div>
    </div>
    
    <!-- 商品檔案上傳 -->
    <div class="upload-section">
        <h2>📦 上傳 VIEW_GOODS_enhanced.csv</h2>
        <p>上傳後會自動清除商品快取與 LLM 提示詞快取</p>
        <input type="file" id="goodsFile" accept=".csv">
        <button onclick="uploadGoodsCSV()">上傳商品檔案</button>
        <div id="goodsStatus"></div>
    </div>
    
    <!-- LLM Prompt 管理 -->
    <div class="upload-section">
        <h2>🤖 LLM 提示詞管理</h2>
        <button onclick="checkPromptStatus()">查詢狀態</button>
        <button onclick="rebuildPrompt()">手動重建</button>
        <div id="promptStatus"></div>
    </div>
    
    <!-- 管理員令牌輸入 -->
    <div class="upload-section">
        <h2>🔑 管理員令牌</h2>
        <input type="password" id="adminToken" placeholder="請輸入管理員令牌" style="width: 100%; padding: 8px;">
    </div>

    <script>
        const API_BASE = window.location.origin;
        
        function getAdminToken() {
            const token = document.getElementById('adminToken').value;
            if (!token) {
                alert('請先輸入管理員令牌');
                throw new Error('缺少管理員令牌');
            }
            return token;
        }
        
        async function uploadCategoriesCSV() {
            const fileInput = document.getElementById('categoriesFile');
            const statusDiv = document.getElementById('categoriesStatus');
            
            if (!fileInput.files.length) {
                statusDiv.innerHTML = '<div class="status error">請選擇檔案</div>';
                return;
            }
            
            try {
                const token = getAdminToken();
                const formData = new FormData();
                formData.append('file', fileInput.files[0]);
                formData.append('admin_token', token);
                
                statusDiv.innerHTML = '<div class="status">上傳中...</div>';
                
                const response = await fetch(`${API_BASE}/api/admin/upload-categories-csv`, {
                    method: 'POST',
                    body: formData
                });
                
                const result = await response.json();
                
                if (response.ok) {
                    statusDiv.innerHTML = `
                        <div class="status success">
                            ✅ ${result.message}<br>
                            檔案大小: ${result.file_size} bytes<br>
                            備份已建立: ${result.backup_created ? '是' : '否'}
                        </div>
                    `;
                    fileInput.value = '';
                } else {
                    statusDiv.innerHTML = `<div class="status error">❌ ${result.detail}</div>`;
                }
            } catch (error) {
                statusDiv.innerHTML = `<div class="status error">❌ 上傳失敗: ${error.message}</div>`;
            }
        }
        
        async function uploadGoodsCSV() {
            const fileInput = document.getElementById('goodsFile');
            const statusDiv = document.getElementById('goodsStatus');
            
            if (!fileInput.files.length) {
                statusDiv.innerHTML = '<div class="status error">請選擇檔案</div>';
                return;
            }
            
            try {
                const token = getAdminToken();
                const formData = new FormData();
                formData.append('file', fileInput.files[0]);
                formData.append('admin_token', token);
                
                statusDiv.innerHTML = '<div class="status">上傳中...</div>';
                
                const response = await fetch(`${API_BASE}/api/admin/upload-goods-csv`, {
                    method: 'POST',
                    body: formData
                });
                
                const result = await response.json();
                
                if (response.ok) {
                    statusDiv.innerHTML = `<div class="status success">✅ ${result.message}</div>`;
                    fileInput.value = '';
                } else {
                    statusDiv.innerHTML = `<div class="status error">❌ ${result.detail}</div>`;
                }
            } catch (error) {
                statusDiv.innerHTML = `<div class="status error">❌ 上傳失敗: ${error.message}</div>`;
            }
        }
        
        async function checkPromptStatus() {
            const statusDiv = document.getElementById('promptStatus');
            
            try {
                const token = getAdminToken();
                const response = await fetch(`${API_BASE}/api/admin/prompt-status?admin_token=${encodeURIComponent(token)}`);
                const result = await response.json();
                
                if (response.ok) {
                    const cacheAge = result.cache_age_seconds 
                        ? `${Math.floor(result.cache_age_seconds)} 秒` 
                        : 'N/A';
                    
                    const statusClass = result.using_fallback ? 'warning' : 'success';
                    
                    statusDiv.innerHTML = `
                        <div class="status ${statusClass}">
                            <div class="prompt-info">
                                <strong>快取狀態:</strong> ${result.cache_exists ? '✅ 已快取' : '❌ 未快取'}<br>
                                <strong>快取年齡:</strong> ${cacheAge}<br>
                                <strong>快取 TTL:</strong> ${result.cache_ttl_seconds} 秒<br>
                                <strong>失敗次數:</strong> ${result.failed_count}<br>
                                <strong>提示詞長度:</strong> ${result.prompt_length} 字元<br>
                                <strong>使用降級模式:</strong> ${result.using_fallback ? '⚠️ 是' : '✅ 否'}
                            </div>
                        </div>
                    `;
                } else {
                    statusDiv.innerHTML = `<div class="status error">❌ ${result.detail}</div>`;
                }
            } catch (error) {
                statusDiv.innerHTML = `<div class="status error">❌ 查詢失敗: ${error.message}</div>`;
            }
        }
        
        async function rebuildPrompt() {
            const statusDiv = document.getElementById('promptStatus');
            
            try {
                const token = getAdminToken();
                const formData = new FormData();
                formData.append('admin_token', token);
                
                statusDiv.innerHTML = '<div class="status">重建中...</div>';
                
                const response = await fetch(`${API_BASE}/api/admin/rebuild-llm-prompt`, {
                    method: 'POST',
                    body: formData
                });
                
                const result = await response.json();
                
                if (result.status === 'ok') {
                    const missingInfo = result.missing_categories.length > 0
                        ? `<br><strong>⚠️ 缺少的分類:</strong> ${result.missing_categories.join(', ')}`
                        : '<br>✅ 所有啟用的分類都已包含';
                    
                    statusDiv.innerHTML = `
                        <div class="status success">
                            ✅ ${result.message}<br>
                            提示詞長度: ${result.prompt_length} 字元
                            ${missingInfo}
                        </div>
                    `;
                } else {
                    statusDiv.innerHTML = `
                        <div class="status error">
                            ❌ ${result.message}<br>
                            已啟用降級模式
                        </div>
                    `;
                }
            } catch (error) {
                statusDiv.innerHTML = `<div class="status error">❌ 重建失敗: ${error.message}</div>`;
            }
        }
    </script>
</body>
</html>
```

### Phase 5: 測試與驗證

#### 5.1 單元測試

```python
# backend/tests/test_dynamic_prompt.py

import pytest
from llm_service import _build_category_hierarchy_prompt, _check_category_coverage

def test_prompt_contains_new_category():
    """測試新分類是否出現在提示詞中"""
    prompt = _build_category_hierarchy_prompt()
    
    # 驗證新分類存在
    assert "磐鈺建設" in prompt
    assert "極致輕寓2房" in prompt
    
    # 驗證舊分類仍存在
    assert "常溫食品" in prompt
    assert "醬油" in prompt

def test_prompt_length_control():
    """測試提示詞長度控制"""
    prompt = _build_category_hierarchy_prompt()
    
    # 不應超過最大長度
    assert len(prompt) <= 8000

def test_category_coverage():
    """測試分類覆蓋度檢查"""
    prompt = _build_category_hierarchy_prompt()
    missing = _check_category_coverage(prompt)
    
    # 不應有缺少的啟用分類
    assert len(missing) == 0, f"缺少分類: {missing}"

def test_each_l3_has_example():
    """測試每個 L3 至少有一個商品範例"""
    prompt = _build_category_hierarchy_prompt()
    
    # 這裡應該驗證每個 L3 分類都有對應的商品範例
    # 實際檢查邏輯取決於提示詞的格式
    pass
```

#### 5.2 整合測試

```python
# backend/tests/test_search_integration.py

def test_new_category_search_direct():
    """測試新分類搜尋是否直接返回結果（不需澄清）"""
    from app import app
    from fastapi.testclient import TestClient
    
    client = TestClient(app)
    
    # 測試新分類商品搜尋
    response = client.post("/api/search", json={
        "query": "極致輕寓2房",
        "mode": "chat"
    })
    
    assert response.status_code == 200
    data = response.json()
    
    # 應該直接返回商品，而非澄清
    assert data.get("need_clarification") == False
    assert len(data.get("products", [])) > 0
    
    # 驗證置信度
    assert data.get("confidence_score", 0) >= 0.7

def test_cache_invalidation():
    """測試快取清除功能"""
    from app import app, _clear_category_and_prompt_caches
    
    # 第一次調用會建立快取
    from llm_service import _build_category_hierarchy_prompt
    prompt1 = _build_category_hierarchy_prompt()
    
    # 清除快取
    _clear_category_and_prompt_caches()
    
    # 第二次調用應該重建
    prompt2 = _build_category_hierarchy_prompt()
    
    # 兩次結果應該相同（假設資料沒變）
    assert prompt1 == prompt2
```

#### 5.3 手動驗證清單

- [ ] 上傳新的 goods_categories.csv 後，LLM 能識別新分類
- [ ] 搜尋新分類商品時，置信度 ≥ 0.7
- [ ] 快取在 TTL 過期後自動更新
- [ ] 快取清除後立即生效
- [ ] 提示詞長度在合理範圍內（< 8000 字元）
- [ ] 降級機制正常運作（模擬 CSV 讀取失敗）
- [ ] 所有啟用的 L3 分類都有至少 1 個商品範例
- [ ] 管理介面可以正常上傳檔案
- [ ] 管理員令牌驗證正常
- [ ] 失敗計數器與告警機制運作

#### 5.4 API 測試案例

```bash
# 1. 測試現有分類（醬油）
curl -X POST http://127.0.0.1:8000/api/chat \
  -H "Content-Type: application/json" \
  -d '{"message": "醬油", "history": []}'

# 2. 測試新分類（極致輕寓2房）
curl -X POST http://127.0.0.1:8000/api/chat \
  -H "Content-Type: application/json" \
  -d '{"message": "極致輕寓2房", "history": []}'

# 3. 測試其他新分類
curl -X POST http://127.0.0.1:8000/api/chat \
  -H "Content-Type: application/json" \
  -d '{"message": "磐鈺雲華", "history": []}'

# 4. 測試模糊查詢
curl -X POST http://127.0.0.1:8000/api/chat \
  -H "Content-Type: application/json" \
  -d '{"message": "科博館附近的房子", "history": []}'
```

### 驗收標準

✅ 所有分類查詢都能正確識別  
✅ 商品範例自動更新  
✅ 新增分類不需改程式碼  
✅ 快取機制正常運作  
✅ 降級處理機制有效  

---

## 📊 實施優先順序

### 🚀 立即實施（1-2 天）
1. **Phase 1**: 核心動態載入功能
   - 修改 `_build_category_hierarchy_prompt()` 為真正動態
   - 實現基本的 CSV 讀取與範例提取邏輯
   - 實現降級機制確保服務可用性

### ⚡ 短期實施（3-5 天）
2. **Phase 2**: 快取與降級機制
   - 實現 TTL 快取（5 分鐘）
   - 建立多層降級機制（動態 → 靜態 → 最小化）
   - 實現長度控制與智能截斷
   - 確保每個 L3 至少有 1 個範例
   - 實現覆蓋度檢查與告警

3. **Phase 3**: 管理介面後端 API
   - 實現 CSV 上傳端點（分類與商品）
   - 實現快取清除邏輯
   - 實現覆蓋度檢查端點
   - 實現提示詞狀態監控端點

### 🎯 中期完善（1-2 週）
4. **Phase 4**: 前端管理頁面
   - 建立管理介面 UI (admin.html)
   - 實現檔案上傳功能
   - 實現狀態監控面板
   - 實現手動重建提示詞功能
   - 管理員令牌驗證機制

5. **Phase 5**: 測試與驗證
   - 撰寫單元測試（提示詞生成、長度控制、覆蓋度）
   - 執行整合測試（搜尋功能、快取機制）
   - 手動驗證所有功能（10 項檢查清單）
   - API 測試案例驗證

---

## 🔒 重要注意事項（來自任務卡）

### ❌ 禁止事項
- **禁止大規模架構重構**：只修改相關函數，不改變整體架構
- **禁止硬寫 API Key**：所有敏感資訊使用環境變數
- **禁止破壞性指令**：不使用 `rm -rf` 等危險命令
- **禁止直接修改資料**：不在程式中直接修改 CSV 內容

### ✅ 必須遵守
- **向下相容**：現有功能不受影響
- **環境變數控制**：新增 `USE_DYNAMIC_PROMPT=True` 控制開關
- **錯誤處理**：所有異常都要妥善處理並記錄
- **測試覆蓋**：所有新功能都要有對應測試
- **文件更新**：修改後更新 README 與技術文件

---

## 📈 預期效果

### Before (現狀)
```
新增商品分類 → 需要修改 llm_service.py → 部署新版本 → 才能搜尋
時間成本: 30-60 分鐘
風險: 程式碼錯誤可能影響所有搜尋功能
```

### After (優化後)
```
新增商品分類 → 更新 CSV 檔案 → 等待快取過期（5 分鐘）或手動重建 → 立即可搜尋
時間成本: 5-10 分鐘
風險: 降級機制確保不影響現有功能
```

### 量化指標
- **部署時間**: 從 30-60 分鐘降至 5-10 分鐘 (↓ 80%)
- **程式碼修改**: 從每次新增分類都要改程式碼，變成零程式碼維護
- **錯誤風險**: 從高風險（可能影響所有搜尋）降至低風險（降級機制保護）
- **維護成本**: 從需要工程師介入，變成業務人員可自行管理

---

## 🎓 技術學習要點

1. **快取策略**: 理解 TTL 快取與失效機制
2. **降級設計**: 多層降級確保服務可用性
3. **CSV 原子操作**: 使用 `os.replace()` 確保檔案更新的原子性
4. **覆蓋度驗證**: 確保動態生成的內容完整性
5. **可觀測性**: 透過日誌與監控端點了解系統狀態

---

## 💼 業務影響評估

### 改善前（目前）

| 業務需求 | 需要動作 | 負責人 | 時間 |
|---------|---------|--------|------|
| 新增商品業種 | 更新 CSV + 改程式碼 + 重啟 | 工程師 | 1-2 小時 |
| 更新商品範例 | 改程式碼 + 重啟 | 工程師 | 30 分鐘 |
| 測試新分類 | 手動測試 | 工程師 | 30 分鐘 |

### 改善後（動態載入）

| 業務需求 | 需要動作 | 負責人 | 時間 |
|---------|---------|--------|------|
| 新增商品業種 | 更新 CSV + 清除快取 | 業務人員 | 5 分鐘 |
| 更新商品範例 | 自動更新 | 系統自動 | 0 分鐘 |
| 測試新分類 | 自動生效 | 系統自動 | 0 分鐘 |

**效率提升：90% 以上**

---

## 🎯 結論與建議

### 核心答案

**Q: 未來有新分類時，是否要程式補強？**
- **目前系統**：是的 ❌（需要改 `llm_service.py`）
- **應該要**：不需要 ✅（改成動態載入即可）

**Q: 程式碼是否要隨時更新？**
- **目前**：要 ❌
- **應該**：不要 ✅

### 建議實施

**強烈建議：選擇方案 B（徹底修復）**

理由：
1. ✅ 一次性投資，長期受益
2. ✅ 大幅降低維護成本
3. ✅ 減少人為出錯機會
4. ✅ 提升系統擴展性
5. ✅ 業務人員可自主操作

**時程建議**：
- Week 1: 實作核心功能 + 測試
- Week 2: 加入快取優化 + 管理介面
- Week 3: 完整測試 + 上線

**風險控管**：
- 保留備用靜態 Prompt（降級處理）
- 分階段上線（先測試環境）
- 完整的測試案例覆蓋

### 提示詞重建機制

**提示詞重建支援立即重建與延遲重建兩種模式**。每次重建皆會搭配快取 TTL 管控與 Smoke Test 覆蓋檢查，一旦重建失敗或超時，系統會自動回退至靜態版並發送警示，快取層不被污染。整體設計提供安全的回滾機制，保留靜態版與上一版流程，確保線上服務穩定性。

**兩種重建模式**：
1. **立即重建模式**：CSV 上傳後立即觸發提示詞重建，適合需要快速生效的場景
2. **延遲重建模式**：CSV 上傳後僅清除快取，由下次請求時自動重建，適合低優先級更新

**安全保障機制**：
- ✅ 快取 TTL（5 分鐘）自動過期保護
- ✅ Smoke Test 自動檢查新分類覆蓋度
- ✅ 重建失敗自動回退靜態版本
- ✅ 失敗計數與警示通知機制
- ✅ 快取層隔離，失敗不污染現有服務

---

## 📚 相關文件

- [新分類商品_系統調整分析.md](./新分類商品_系統調整分析.md) - 問題詳細分析
- [COPILOT_INSTRUCTIONS.md](./.github/copilot-instructions.md) - 系統架構說明
- [CATEGORY_HIERARCHY_FLOW.md](./ACTUAL_CATEGORY_HIERARCHY_FLOW.md) - 分類流程文件

---

## 📞 後續支援

如需實施此方案，請提供：
1. 確認採用方案（A 或 B）
2. 預期上線時程
3. 測試環境需求

我可以協助：
- 完整程式碼實作
- 測試案例撰寫
- 上線部署支援
