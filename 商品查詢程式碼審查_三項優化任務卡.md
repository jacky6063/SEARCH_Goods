# 商品查詢程式碼審查_三項優化任務卡

## 目標
針對目前商品查詢流程的三個主要問題提出改善任務，降低誤判、提升結果一致性，並為後續條件式視窗功能打底。

---

## 任務 1：關鍵字誤判導致清空結果

### 問題說明
當使用者輸入包含類別關鍵字時，`_compose_structured_reply` 會檢查商品文字是否包含該關鍵字，若無命中就直接回覆「找不到相關商品分類」並清空結果，造成「有結果卻顯示空」的誤判。

### 影響
- 使用者明明查到商品，卻被系統判定為無結果。
- 結果不穩定，降低信任感。

### 改善方向
- 將該邏輯改為「提示/警告」而非「硬清空結果」。
- 只有在結果為空時才使用警告文字。
- 加入測試案例，避免回歸。

### 影響範圍
- `backend/chat_router_goods_action.py`

### 驗收標準
- 有商品結果時不再被關鍵字檢查清空。
- 使用者仍可收到提醒訊息（若需要）。

### 狀態
已完成（已改為提醒訊息，不再清空結果）。

---

## 任務 2：價格區間解析成單一最大值

### 問題說明
`_extract_budget_from_query` 將價格區間只保留最大值，例如 `3000~5000` 會變成 `5000`，導致回覆與後續價格預填邏輯失真。

### 影響
- 使用者輸入的區間被誤解為單一預算。
- 後續價格區間視窗會帶錯值。

### 改善方向
- 回傳「區間結構」而非單一值，例如 `{min: 3000, max: 5000}`。
- 前端顯示需對應區間，而非單一金額。
- 加入區間格式測試（3000~5000、3000-5000、3000內）。

### 影響範圍
- `backend/chat_router_goods_action.py`

### 驗收標準
- 價格區間能正確解析並保留起訖值。
- 若只輸入單一價格，仍能正常處理。

### 狀態
已完成（回傳區間結構並同步調整回覆文案）。

---

## 任務 3：L3 分類比對不一致

### 問題說明
L3 分類在不同路徑使用不同匹配方式：
- 熱門分類與 L3-only 路徑使用「完全相等」。
- 完整路徑使用「子字串包含」。

### 影響
- 同樣的分類在不同路徑可能命中或失敗。
- 查詢結果不穩定，難以預測。

### 改善方向
- 統一 L3 匹配規則（建議使用標準化後的「相等」）。
- 對輸入與資料欄位做正規化（去空白、全形半形、大小寫）。
- 加入一致性測試，確保各路徑結果相同。

### 影響範圍
- `backend/app.py`

### 驗收標準
- 同樣 L3 分類在不同路徑回傳結果一致。
- 不再出現「相同條件但結果差異」的情況。

### 狀態
已完成（統一 L1/L2/L3 正規化比對規則）。

---

## 建議優先順序
1) 任務 1（防止誤清空結果）
2) 任務 2（價格區間解析）
3) 任務 3（L3 匹配一致性）
